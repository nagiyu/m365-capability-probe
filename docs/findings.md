# 所見

このプローブを実テナントに対して走らせて出てきたものと、それぞれがツールを変えるに足る理由。

**これらは観測であって、仕様の説明ではありません。** 出所は 1 つの Microsoft 365 テナント、1 つの
アプリ登録、1 つのサイト、共有状態や保護の違う 7 つのファイルで、日付は 2026-08-03 〜 05 です。
1 つのテナントは 1 つのテナントでしかありません。頼りにする前に、ご自身のテナントに対して走らせて
ください。以下のテナント名・サイト名・アカウント名・各種 ID は伏せ字に置き換えてあります。

**14 の所見と、事前の予測との関係はそれぞれ違います。**

| | 予測 | 結果 |
| --- | --- | --- |
| 所見 1〜3 | 立てた | **裏切られた** |
| 所見 4 | 立てた (`404` だろう) | **当たった** ― ただし当たったこと自体が所見だった |
| 所見 5・6 | **立てていない** | 立てられなかったので測った |
| 所見 5 の追試 1〜4 | **立てていない** | 詳細は所見 5。**追試 4 は、追試 2 の結論が広すぎたことを示した** |
| 所見 6 の追試 1〜4 | **立てていない** | **追試 3 は、所見 6 そのものが広すぎたことを示した** |
| 所見 7 | **測ろうとしていなかった** | 別のことを測っている最中に、道具のほうが止まった |
| 所見 8 | **立てていない** | 候補を 3 つ並べて、どれが通るかを測った |
| 所見 9 | **立てていない** | 2 つのフィールドを別々に動かして、答えがどちらに付くかを測った |
| 所見 9 の追試 1 | **立てていない** | **所見 9 が自分で挙げた弱点を、ファイルを 1 枚足して潰した** |
| 所見 10 | **立てていない** | 保護の形と名指しの形を別々に動かした。**測ろうとしていなかったものが 1 つ出た** |
| 所見 11 | **立てていない** | 付与を 2 段に分けて測った。**所見 9 の結論が広すぎたことが 3 度目に分かった** |
| 所見 12 | **立てていない** | 所有者の出所を 4 経路並べた。**3 つ目の問いは「取れない」が答えでもよかった** ― 取れた |
| 所見 13 | **立てていない** | 循環を作れるかどうかから測った。**測ろうとしていなかったものが 1 つ出た** (`null`) |
| 所見 8 の追試 1 | **立てていない** | **道具を直してから測った。** 所見 8 の「1 回で終わる」は 1 ページ分の数字だった |
| 所見 8 の追試 2 | **立てていない** | 250 枚置いて件数を越えさせた。**測ろうとしていなかった `429` が出た** |
| 所見 14 | **立てていない** | 保護の可否を安く取る道を探していて出た。**真偽値で「いいえ」と言われた** |

**広すぎた結論が、これまでに 4 つ出ています。** どれもそのときの測定は正しく、
**そこから一般化した部分だけ**が間違っていました ― 所見 5 の追試 2 (「権限は変数ではない」)、
所見 6 の本文 (「一覧はできるが中身は見せない」)、所見 9 (「名指しが無ければ拒否される」)、そして
所見 10 の 3 番目 (「ポリシーは所有者にしか見えない」)。訂正は消さずに、当該箇所に残してあります。

**4 つに共通するのは、「そのときの構成では」が抜けたこと**です。測定は 1 つの構成の中で行われますが、
結論はその外側について語りたがります。**所見 11 は、構成を動かしてそれを見せた例**です ― 同じ入力・
同じコードで、テナント側の付与 1 つだけが変わって答えが変わりました。

**2 つとも原因は同じです。変数を 1 つしか動かさずに、全体について語ったこと。** 前者はトークンが
1 種類しかなく、後者は測ったグループが 1 つでした。

所見 4 が示しているのは「`404` が返る」ことではなく、**その `404` がパスの書き間違いと区別できない**
ことです。予測が当たっても、その含意までは見えていませんでした。

全体を通じて、区別しておく価値のあるものが 1 つあります。**測定したこと** と **それを説明する読み**
は別物です。測定は再現できます。説明は測定に最もよく合う読みであり、機構を確認していない箇所は
その旨を明記しています。

## 観測を生んだ設定

環境の全体像は [docs/environment.md](environment.md) にあります。ここには所見に直接効く部分だけを
再掲します。

アプリ登録は 1 つ。管理者の同意を与えたのは、正確に次のものだけです。

| API | アクセス許可 | 種類 |
| --- | --- | --- |
| Microsoft Graph | `Sites.Read.All` | アプリケーション |
| Microsoft Graph | `Sites.Read.All` | 委任 |
| Microsoft Graph | `User.Read` | 委任 |
| SharePoint | `Sites.Read.All` | アプリケーション |

これ以外は何もありません。特に **SharePoint の委任アクセス許可は 1 つもなく、Azure Rights Management
には何も付与していません** ― この 2 つの欠落が、所見 1 と所見 2 の主題です。

資格情報は **クライアント シークレットと、自己署名証明書 1 つ**。証明書は 2026-08-04 に登録しました。
**アクセス許可は証明書を足しても 1 つも変わっていません** ― 変わったのは、アプリが自分を証明する
方法の選択肢が 2 つになったことだけです。所見 5 の追試 3 は、この 2 つを同じ実行の中で並べたものです。

「パブリック クライアント フローを許可する」は有効 (デバイス コード用)。委任側でサインインする人物は
テスト用サイトの **閲覧者** であり、管理者ロールもディレクトリ ロールも持ちません。ファイルは共有状態を
変えた 4 つで、うち 1 つは継承を切ったサブフォルダの中にあります (内訳は
[environment.md](environment.md))。

各サブコマンドは複数回実行しています。観測のための実行と、ツールを直したあとに同じ値が出ることを
確かめる実行の両方です。数字の再現については後段の「再現性」を参照してください。

---

## 所見 1 ― Microsoft Graph への同意が SharePoint に届く

### 予測

このアプリに SharePoint の **委任** アクセス許可はない。したがって SharePoint リソースへの委任
トークン要求は、Azure RMS のそれと同じように拒否されるはずである。

### 観測

成功する。しかも本物のトークンが返る。

| | 値 |
| --- | --- |
| `aud` | `00000003-0000-0ff1-ce00-000000000000` |
| `scp` | `Sites.Read.All User.Read` |

`00000003-0000-0ff1-ce00-000000000000` は SharePoint Online の第一者アプリケーション ID です ―
**この対応づけ自体は測定ではなく、広く知られた事実としての引用**です。

測定の側から言えることは 2 つあります。この `aud` を持つトークンは **ネットワーク往復を伴い、Graph の
トークンとは独立した有効期限を持ちます**。そして **実際に SharePoint REST に受け入れられます**
(所見 5)。どちらも「Graph のトークンを取り違えて返したもの」では説明がつきません。

じっと見る価値があるのは `scp` の値のほうです。`Sites.Read.All User.Read` は、**このアプリの
Microsoft Graph 委任付与の完全な写し** です ― 2 つとも、同じ順序で、それ以外は何もありません。

対比として、同じ実行の、同じ資格情報で:

| audience | モード | 結果 |
| --- | --- | --- |
| SharePoint | 委任 | トークン、`scp: Sites.Read.All User.Read` |
| Azure RMS | 委任 | 拒否、`AADSTS65001` |

違いは Graph 側の同意の有無と **対応しています** ― Azure RMS には対応する Graph の同意がなく、
SharePoint にはあります。**対応であって、因果を確かめたわけではありません。**

### これが意味すること

Microsoft Graph の委任 `Sites.Read.All` への同意が、SharePoint リソースに対する委任アクセスも生んで
います。**API アクセス許可の画面にそう書いてある箇所はありません。** その画面を読んで「このアプリは
利用者の代理で SharePoint に対して動けない」と結論するのは誤りであり、その画面をどれだけ読み込んでも
この事実は現れません。トークンを要求して中を見ることでしか表に出てきません。

### 確認していないこと

- **機構。** SharePoint リソース側での Graph の事前承認、第一者リソース間でのスコープ名の一致、
  管理者同意の副作用として作られたテナント全体の付与 ― いずれもこの結果を生みます。ここで実際に
  働いているのがどれかは特定していません。そして結果を報告するうえで、プローブがそれを知る必要は
  ありません。
- ~~そのトークンが実際に効くか。~~ **測りました。効きます** ― そして同じアプリの app-only は同じ
  エンドポイントで拒否されます。所見 5 を参照してください。
- **アプリケーション側の方向。** このアプリには SharePoint 自身の `Sites.Read.All` が明示的に付与されて
  いるため、この実行からは Graph の **アプリケーション** アクセス許可が同じように流れ込むかどうかは
  分かりません。それを知るには、SharePoint のアプリケーション付与を持たないアプリ登録が要ります。

### コードへの反映

当初この所見は `AuthProbe` の期待値表に「SharePoint / 委任は権限を保持する」と書き込む形で反映され
ました。**その期待値は後に、期待値という仕組みごと削除しました。** 3 つの所見のいずれもが、
アクセス許可の一覧から立てた予測を裏切っており、予測をコードに埋めて判定を出すことは、テナントに
ついてではなく作者の予測について報告することだったからです。いまツールが出すのは観測だけで、
期待はこの文書の「予測」節が担っています。

回帰を検知したい場合 ― 例えば Graph の委任付与を外したときにこのセルが変わることを捕まえたい場合 ―
は、2 つの実行の JSON レポートを差分で比べるのが素直です。

---

## 所見 2 ― クライアント資格情報は、何も持たないトークンを発行する

### 予測

Azure RMS のアクセス許可は両方向とも付与していないので、両方の経路が拒否されるはずである。

### 観測

2 つの経路が食い違う。

| モード | 結果 |
| --- | --- |
| 委任 | 拒否、`AADSTS65001` ― *"The user or administrator has not consented to use the application"* |
| app-only | **トークン発行**、`aud: https://aadrm.com`、`roles` なし、`scp` なし |

正しい audience 宛の、有効な、そしていかなる権限も持たないトークン。

### これが意味すること

`.default` は 2 つのフローで違うものを意味し、**ゼロ個のところで両者は袂を分かちます**。

| フロー | `.default` の意味 | 付与が 0 個のとき |
| --- | --- | --- |
| 委任 | このリソースに対してすでに同意済みのスコープ全部 | エラー ― `AADSTS65001` |
| クライアント資格情報 | このリソースに対して割り当て済みのアプリ ロール全部 | `roles` を持たないトークン |

つまりクライアント資格情報においては、トークン要求の成功が言っているのは **そのリソースのサービス
プリンシパルがテナントに存在すること** だけです。アプリがそれで何かしてよいかについては、何も言って
いません。

**そのサービス プリンシパルが存在することは、この測定自体が示しています。** 存在しなければ要求は
`AADSTS500011` (リソースが見つからない) で拒否され、`aud: https://aadrm.com` を持つトークンは返って
きません。**トークンが返ったこと自体が、相手が実在する証拠です。**

そのうえで、**このテナントの RMS はプロビジョニングされているだけではありません。** 保護つきの
秘密度ラベルが 3 つ構成・公開されており、使用権限は View / View+Print / View+DocEdit+Edit+Print+Export
の 3 段階です。マーキングも自動ラベル付けも無効なので、**このラベルは保護以外に何もしません**
([environment.md](environment.md) 参照)。

つまり空のトークンが出た相手は、**誰かが意図して組んだサービス**です。ただし「構成されている」と
「実際にラベルの付いたコンテンツがある」は別で、後者は測っていません。

(この段落は一度、「ラベルが 3 つ公開されている」だけを根拠に「RMS は実際に**使われている**」と
書いていました。**保護を適用しないラベルは RMS を使わない** ので、それは導けない推論でした。前提の
ほうを測ってから書き直しています。)

これこそ、このツール全体が踏まないために存在する罠です。「トークン発行 = 成功」と報告する能力プローブ
は、このアプリが Azure RMS に届いていると報告します。**`roles` が 1 つも無い以上、届いてはいないはず**
です ― ただしそれは推論で、下記のとおり **実際に呼んで確かめてはいません**。

### 追試 ― 空だったのは、割り当てが 0 個だったから

上の表の「クライアント資格情報の `.default` は割り当て済みのアプリ ロール全部」は、**仕様の引用で
あって測定ではありませんでした。** 引用が正しくても、このテナントでそれが理由だったとは限りません。

そこで **アプリ ロールを 1 つだけ足しました** ― Microsoft Rights Management Services の
`Content.DelegatedReader` (アプリケーション) に管理者同意。Graph と SharePoint の付与は触っていません。

| audience | 付与前 (14:04Z) | 付与後 (14:11Z) |
| --- | --- | --- |
| Graph | `roles: Sites.Read.All` | 変わらず |
| SharePoint | `roles: Sites.Read.All` | 変わらず |
| **Azure RMS** | **`no roles, no scp`** | **`roles: Content.DelegatedReader`** |

**動いたのは 1 セルだけ**で、しかもシークレットと証明書の両方が同じように動きました。同じスコープ、
同じアプリ、同じ資格情報、7 分差 ― **変えたのは付与 1 つです。**

> **トークンが空だったのは、割り当てが 0 個だったからです。** 1 つ割り当てると、同じ要求が
> それを載せて返ってきます。

これは **`.default` の一般則を証明するものではありません。** 測ったのは
**「このトークンの中身は、アプリ ロールの割り当てに追随する」** ことで、仕様の引用だった部分の
うち、このテナントで効いている因果だけが測定に変わりました。

**副産物が 1 つ。** シークレットと証明書が同じ内容を受け取ったことは、所見 5 の形とも整合します ―
**Entra が何を発行するかに名乗り方は効かず、効くのは受け取ったリソースの側**でした。

### 確認していないこと

- ~~このトークンで実際に Azure RMS の API を呼ぶと拒否されること。~~ **前提が変わりました。**
  いまは `Content.DelegatedReader` を持っているので、「空のトークンで呼ぶとどうなるか」は
  **もう測れません** ― 測るなら付与を外して戻す必要があります
- **`Content.DelegatedReader` を持ったトークンで実際に呼ぶと何が起きるか。** これがこの追試の
  目的で、まだ測っていません
- **付与を外すと空に戻るのかどうか。** 対称性は仮定していますが、測っていません

### コードへの反映

`auth` はトークンの発行だけを見るのをやめました。発行された各トークンから `roles` と `scp` のクレームを
読み、**何が付与されているか** を出力します。この変更により、このセルは初めから報告すべきだったことを
報告します: `token, but nothing granted`。

クレームは **読むだけで、検証はしません**。このツールはどのトークンの受け手でもなく、トークンに基づく
信頼の判断を一切しないので、署名の検証は誰も問うていない問いに答えることになります。そのための
トークン処理ライブラリも導入していません。

---

## 所見 3 ― 見せてもらえない権限一覧は、空の成功として返ってくる

> **【裏付け 2026-08-06】** これは仕様どおりでした。Graph のドキュメント
> ([driveItem: list permissions](https://learn.microsoft.com/graph/api/driveitem-list-permissions))
> に明記されています ―
> **「所有者以外の呼び出し元には、その呼び出し元に当てはまる共有アクセス許可だけが返されます」。**
> **測定は正しく、しかも文書化された挙動でした。** 空のリストが「共有されていない」に見えることも、
> 同じページが「このコレクションは機微な情報を含み、すべての呼び出し元に返るとは限らない」と
> 前置きしています。

### 予測

サインインする人物は閲覧者である。アイテムの権限一覧を読むことは閲覧者の領分ではないので、委任側は
`403` で拒否されるはずである。

### 観測

拒否されない。同じファイル、同じ瞬間、1 回の実行で:

| モード | site | item | permissions | 件数 | プリンシパルの種類 |
| --- | --- | --- | --- | --- | --- |
| app-only | `200 OK` | `200 OK` | `200 OK` | **4** | `sharePointGroup, siteGroup, siteUser, user` |
| 委任 | `200 OK` | `200 OK` | `200 OK` | **0** | ― |

6 つの呼び出しがすべて成功しています。Graph は委任の呼び出し元に、空のコレクションつきで `200 OK` と
答えます。

**測定はここまでです。** 「権限エントリは呼び出し元が見てよいものだけに絞られている」というのは、この
結果に最もよく合う読みですが、Graph の内部でそうなっているかを確かめたわけではありません。下の追試は
その読みを支持しますが、証明ではありません。

### これが意味すること

**ステータス コードでは、次の 2 つを区別できません。**

- このファイルは誰とも共有されていない
- このファイルの共有は自分に見せてもらえない

どちらも `200 OK` と `"value": []` です。委任側の答えだけを読んで「共有されていない」と結論するコード
は、真実の正反対を得ます ― ここでは権限エントリが 4 件あります。

これは `403` が返るより **たちが悪い** ものです。拒否は騒がしく、処理されない拒否はたいてい呼び出し元を
止めます。空の成功は静かで、次の処理にそのまま流れ込み、しかも情報のように見えます。

これは、このツールが `MeasurementStatus.NotRun` で内部的に防いでいるのと同じ危険でもあります ― *「そこまで到達
しなかったので見ていない」* を、合格のように読める空欄として保存してはならない ― ただしここでは、
Graph 自身の応答の形が 1 つ外側の層で同じ混同をやっています。

これが見えるようにしているものが 2 つあります。両方の経路が **1 回の実行** で走るので、2 つの答えは
同じアイテムの同じ瞬間を記述しており、突き合わせられます。別々の実行だったなら、この差は「その間に
何かが変わった」でいつでも説明がついてしまいます。もう 1 つは、**件数がステータスとは別に記録される**
ことです。そのおかげで `200 OK / 4` と `200 OK / 0` は互いに潰れ合いません。

### 追試 ― 絞り込みは「自分に関係するか」では決まらない

当初この所見は「閲覧者には見せてよい権限エントリが 1 件も無いから 0 件になる」と読めました。**その読みは
間違っていました。** 共有状態の違うファイルを 4 つ用意し、**1 回の実行で** すべてを両方の identity から
読んで確かめています。

| ファイル | 共有状態 | app-only | 委任 |
| --- | --- | --- | --- |
| A | 継承のみ | `200 OK` / **4 件** | `200 OK` / **0 件** |
| B | **委任ユーザー本人に直接アクセス許可を付与** | `200 OK` / **5 件** | `200 OK` / **0 件** |
| C | 組織内リンクで共有 (`link:organization`) | `200 OK` / **5 件** | `200 OK` / **0 件** |
| D | 継承を切ったフォルダ内 (閲覧者を削除) | `200 OK` / **3 件** | アイテムが `404` (所見 4) |

**B が決定的です。** このファイルには「このユーザーに閲覧を許可する」という、**サインインしている本人を
名指しした権限エントリ** があります (app-only の件数が 4 から 5 に増えていることがその証拠です)。それでも
本人には見えません。

つまり絞り込みは *「あなたに関係するエントリを見せる」* ではありません。**権限一覧を読むこと自体が
できるかどうか** で決まっており、閲覧者にはできない。自分宛の許可であっても、自分では確認できません。

C も同じで、`link:organization` のエントリは app-only にしか現れません。

app-only 側の件数は互いに整合しています。A の 4 件が基準で、B と C は付与を 1 つ足した分だけ増え、
D は閲覧者グループを外した分だけ減っています。**ツールが数えている件数が、実際に権限を触った操作を
反映している** ことの確認になります。

この追試は所見を弱めるのではなく、はっきりさせています。委任側の 0 件は「たまたま見せるものが
無かった」ではなく、**そのファイルの共有状態がどうであれ 0 件** です。したがって「0 件だったから
共有されていない」という読みは、いかなる場合にも成り立ちません。

### 確認していないこと

サイトに対してより強い権限を持つ委任呼び出し元 ― 閲覧者ではなくメンバーや所有者 ― が空でない一覧を
見るかどうか。それが確認できれば、絞り込みが呼び出し元の権限によるものだと裏付けられます。別の
アカウントで `access` を走らせるだけで済みます。

### コードへの反映

レポートは **ステータスと件数を別々に記録します**。この 2 つが独立でなければ、`200 OK / 4 件` と
`200 OK / 0 件` は互いに潰れ合います。また、成功した応答が 0 件だった場合は観測の文言に
「拒否ではなく空の一覧」と書き添え、2 つの経路が同じステータスで違う中身を返した場合もその旨を
書きます ― 読み手が数字から自力で気づくのに任せません。

なお、この所見も当初は「委任側は権限エントリを明かさない」という主張と判定の形で書かれていました。
いまは判定を出しません。**「明かさないはずだ」は予測であり、それはこの文書が担うべきものだから**です。

---

## 所見 4 ― 見えないファイルと、存在しないファイルは、同じ答えを返す

### 予測

継承を切って閲覧者グループを外したフォルダの中のファイルは、委任側から読めないはずである。`403` で
拒否されるか、`404` で無いことにされるかは分からないが、**`404` だろう** ― Graph は見る権限のない
アイテムを not found として扱うと聞く。

### 観測

`404 Not Found`、Graph のエラーコードは **`itemNotFound`**。

| モード | site | item | permissions |
| --- | --- | --- | --- |
| app-only | `200 OK` | `200 OK` | `200 OK` / 3 件 |
| 委任 | `200 OK` | **`404 Not Found` `itemNotFound`** | (到達せず) |

app-only 側の件数は 3 件で、他のファイルは 4 件です。**継承を切って閲覧者グループを外した分だけ
減っている** と読めます ― ただし **どのエントリが消えたかは記録していない** ので、対応づけは推測です。

### これが意味すること

`404 itemNotFound` は、**パスを書き間違えたときに返るのと同じ答え**です。区別する手がかりは応答の中に
ありません。

これは机上の心配ではありません。この文書を書いた作業では、読むファイルのパスの書き方を間違えて
(`/Shared Documents/test.docx` とライブラリ名を付けてしまい) **まったく同じ `404 itemNotFound` を実際に
踏んでいます**。そのときは設定の誤りでしたが、応答は今回と一文字も違いません。

つまり委任側で `404` を受け取ったコードは、次の 3 つを区別できません。

- パスが間違っている
- ファイルが存在しない (消された)
- ファイルは存在するが、この呼び出し元には見えない

3 番目だけが権限の話で、他の 2 つは違います。**「404 だったので存在しない」と結論するのは、
「0 件だったので共有されていない」(所見 3) と同じ形の間違い** です。

これを解けるのは、**同じパスを別の identity で同時に引くこと** だけです。app-only が `200` を返して
委任が `404` を返したなら、そのファイルは存在していて、見せてもらえないだけだと分かります。これは
1 回の実行で両方を叩く設計でなければ得られません ― 別々の実行なら「その間に消えた」で説明がつきます。

### 確認していないこと

- サイト自体が見えない場合に何が返るか。ここではサイトの解決は委任側でも `200` で通っています
- `403` が返る条件があるのかどうか。このテナントのこの構成では観測できていません

### コードへの反映

**終了コードの意味を直しました。** アイテムが `404` だったため権限一覧の呼び出しが行われず、その行が
`NotRun` になります。それまで `NotRun` が 1 つでもあれば終了コードは `2` (不完全) でしたが、**これは
誤りでした。** この実行は完全です ― 404 が答えであり、答えられない問いを組み立てなかったのは正しい
振る舞いです。

`NotRun` は「上流の測定結果がその先を到達不能にした」ことを意味し、その上流の測定こそが所見です。
不完全なのは 1 つの場合だけ ― **デバイス コードを出したのに誰もサインインしなかった** ときで、これは
測定ではなく操作の失敗です。終了コード `2` はそこに限定しました。

完全に有効な測定を CI が赤で表示すると、読み手は赤を無視するようになります。

---

## 所見 5 ― 付与されていない側が通り、付与されている側が拒否される

> **【裏付け 2026-08-06】** **長らく「未測定」に置いていた「なぜ `appidacr: 1` が `/_api/` で
> 拒まれるのか」は、仕様でした。** SharePoint のドキュメント
> ([Granting access via Microsoft Entra ID App-Only](https://learn.microsoft.com/sharepoint/dev/solution-guidance/security-apponly-azuread))
> に明記されています ― **「Entra ID で app-only を行う場合、SharePoint の CSOM / REST API に
> アクセスするには証明書を使わなければならない」**、そして FAQ で
> **「証明書以外の方法はすべて SharePoint Online によってブロックされ、アクセス拒否になる」。**
> ACS 経由の app-only は **2026-04-02 に廃止**されています。
>
> **追試 3 の切り分け (効いていたのは資格情報の種類) は、仕様の再発見でした。**
> ただし**この所見の価値は変わりません** ― アクセス許可の画面もトークンのクレームも
> 「フル コントロールを持っている」と言い続けており、**`401` の本文には理由が 1 文字も
> 入っていませんでした。** 仕様に書いてあることと、応答から読めることは、別です。

### 予測

立てませんでした。所見 1 で「Graph の同意が SharePoint に流れ込んで `scp` に載る」ところまでは見えて
いましたが、**そういう経路で届いたクレームを SharePoint が受け入れるかどうかは分からない**、というのが
正直なところだったので、旗を立てずに測りました。

app-only については、SharePoint 自身の `Sites.Read.All` を明示的に付与し管理者同意も与えてあるので、
通ると考えていました。

### 観測

**両方とも外れています。しかも向きが逆です。**

| | 付与されているもの | トークンが持つもの | `GET /_api/web` |
| --- | --- | --- | --- |
| app-only | SharePoint `Sites.Read.All` (アプリケーション、**同意済み**) | `roles: Sites.Read.All` | **`401 Unauthorized`** |
| 委任 | **何も無い** | `scp: Sites.Read.All User.Read` | **`200 OK`** (`Title` が返る) |

`/_api/web/currentuser` も同じ結果で、委任側は SharePoint が認識している呼び出し元として
**サインインした本人**を返します (`i:0#.f|membership|…`)。app-only 側は同じく `401`。

**アクセス許可の画面が言っていることの、正確に反対です。**

### これが意味すること

**`403` ではなく `401` である**ことが重要です。`403` なら「認証はされたが、この操作をする権限がない」
という意味になります。`401` は **そもそも資格情報として受け付けられていない**。SharePoint はこの
呼び出し元を「権限の足りない誰か」とすら見なしていません。

返ってくる `WWW-Authenticate` が **素の challenge** であることが、それを裏づけます。

```
no reason given (realm, client_id, trusted_issuers, authorization_uri)
```

**理由を述べるパラメータが 1 つもありません。** `error=` も `error_description=` も付かず、本文は空、
`x-ms-diagnostics` も返りません。あるのは realm と、リソース自身の ID と、信頼する発行者と、認証しに
行く先だけ ― **「認証してください」** と言っているだけです。

トークンを添えて呼んだのに、**認証していない相手として扱われています。** SharePoint から見て、この
呼び出しには資格情報が付いていなかったのと同じです。

(上の 1 行はレポートの実際の出力です。値ではなくパラメータ名を並べているのは、素の challenge が
realm にテナントを名乗るためで、CI のログでは値が伏せ字になります。名前は誰の秘密でもないので
そこを通り抜けます。)

そして決定的なのは、**同じ app-only トークンが Graph 経由では通っている**ことです。所見 3 の測定で、
app-only は Graph の `/permissions` から権限エントリを 4 件読めています。つまり:

> **このアプリが SharePoint に届かないのではありません。どの API を通るかで、通れるかどうかが
> 変わります。**

これは所見 1 から続く階層をもう 1 段深くします。**アクセス許可の画面**、**トークンのクレーム**、
**リソースの応答** の 3 つが食い違うことは所見 1〜4 で見てきましたが、ここではさらに
**同じ権限・同じトークンでも、経路によって答えが変わる**。`auth` の出力だけを見て
「app-only は SharePoint に `Sites.Read.All` を持っている」と読み、SharePoint REST を叩く実装を
書くと、`401` に出迎えられます。

### 追試 ― 経路の問題であることの担保と、拒否の一様さ

呼び出しを増やし、**同じ実行で Graph にも 1 本投げて対照**にしました。

| 呼び出し | app-only | 委任 |
| --- | --- | --- |
| **Graph** `GET /v1.0/sites/{host}:{path}` | **`200 OK`** | **`200 OK`** |
| `GET /_api/web` | `401` | `200 OK` |
| `GET /_api/web/currentuser` | `401` | `200 OK` |
| `GET /_api/web/sitegroups` | `401` | `200 OK` |
| `GET /_api/web/sitegroups({id})/users` | `401` | `403` (所見 6) |

**対照が効いています。** Graph は同じ実行・同じ資格情報・同じサイトに対して app-only でも `200` を
返します。したがって `/_api/` の `401` は、**アプリが壊れているのでも、資格情報が無効なのでも、
テナントに届かないのでもありません。経路が違うだけ**です。この 1 行が無ければ、拒否が並んだ画面は
何とでも読めてしまいました。

**そして app-only の拒否は一様です。** 4 つのエンドポイントすべてで `401`、いずれも同じ素の
challenge。特定の操作が禁じられているのではなく、**この経路でこの identity が受け付けられていない**。

### 追試 2 ― 権限の強さは変数ではない (**この見出しは狭めました。追試 4 参照**)

アプリの SharePoint アクセス許可を **`Sites.Read.All` から `Sites.FullControl.All` (アプリケーション)
に差し替えて管理者同意**し、同じ測定を繰り返しました。Microsoft Graph 側の付与は触っていません。

| | 1 回目 | 2 回目 |
| --- | --- | --- |
| トークンの `roles` | `Sites.Read.All` | **`Sites.FullControl.All`** |
| app-only `GET /_api/web` | `401` | **`401`** |
| app-only `GET /_api/web/currentuser` | `401` | **`401`** |
| app-only `GET /_api/web/sitegroups` | `401` | **`401`** |
| app-only `GET /_api/web/sitegroups({id})/users` | `401` | **`401`** |
| *(対照)* Graph app-only | `200` | `200` |
| *(対照)* 委任 4 本 | `200 200 200 403` | `200 200 200 403` |

**何も変わりません。**

対照が 2 つとも動いていないので、2 つの実行の間で変わったのは **SharePoint のアプリ ロールだけ** です。
したがって:

> **このトークンについては、権限の強さは変数ではありません。** テナント内の全サイト コレクションに
> 対するフル コントロールを持っていても、`/_api/` は同じ `401` を返します。

これは `403` ではなく `401` だという観測と辻褄が合います。**SharePoint はこのトークンについて
ロールを見てすらいません** ― 見ていれば、`Sites.Read.All` と `Sites.FullControl.All` で何かが違った
はずです。「権限が足りない」という説明は、**シークレットで名乗った app-only については**消えました。

**これは否定的な結果で、否定的な結果は見えにくいものです。** `Sites.FullControl.All` だけで 1 回
測っていたら、「app-only は SharePoint REST を叩けない」で終わり、**権限を足せば通るのかどうかは
分からないまま**でした。「何も変わらなかった」が所見になるのは、**同じ測定が 2 つの付与状態で
比較できる形で残っている**からです。レポートのヘッダが `roles` を持っているのは、そのためです。

> **【後日の訂正】** ここでもとは「権限の強さは変数ではありません」と、トークンの種類を限定せずに
> 書いていました。**追試 4 で、それは広すぎたことが分かりました。** 証明書で名乗った app-only では、
> `Sites.Read.All` と `Sites.FullControl.All` で答えが変わります。この追試が測ったのは
> **「`appidacr: 1` のトークンについては権限が変数ではない」** ことで、当時はそれ以外のトークンを
> 持っていなかったので、区別がつきませんでした。上の本文は、その限定を入れて書き直してあります。

### 追試 3 ― 変数は「名乗り方」だった

追試 2 が消したのは **「権限の量が足りない」** という説明だけでした。残っていた候補のうち 1 つは、
`/_api/` が受け付けるかどうかが **アプリの持つロールではなく、アプリが Entra に対してどう名乗ったか**
に依るというもので、これは権限の強弱とは別の次元です。**予測は立てずに測りました。**

アプリ登録に自己署名証明書を 1 つ登録し、ツールに **3 本目のレグ** を足しました ―
**同じアプリ登録・同じ付与のまま、シークレットの代わりに証明書で名乗る app-only** です。3 本すべてを
同じ実行の中で要求します (2026-08-04、GitHub ホストのランナー)。

| 呼び出し | app-only (シークレット) | app-only (**証明書**) | 委任 |
| --- | --- | --- | --- |
| *(対照)* Graph `/sites/{host}:{path}` | `200` | `200` | `200` |
| `GET /_api/web` | `401` | **`200 OK`** | `200` |
| `GET /_api/web/currentuser` | `401` | **`200 OK`** | `200` |
| `GET /_api/web/sitegroups` | `401` | **`200 OK`** ― 6 グループ | `200` ― 6 グループ |
| `GET /_api/web/sitegroups({id})/users` | `401` | **`403 Forbidden`** | `403 Forbidden` |

**2 つの app-only トークンは、レポート上で見分けがつきません。**

| | 付与 | トークンの `roles` | `aud` |
| --- | --- | --- | --- |
| シークレット | SharePoint `Sites.Read.All` | `Sites.Read.All` | `00000003-0000-0ff1-ce00-000000000000` |
| 証明書 | **同じ** (同じアプリ登録) | **`Sites.Read.All`** | **同じ** |

同じテナント、同じクライアント ID、同じアプリ ロール、同じ audience、同じランナー、数百ミリ秒差。
**違うのは Entra に対する名乗り方だけです。** したがって:

> **`/_api/` が app-only トークンを受け付けるかどうかを決めているのは、権限ではなく、アプリが
> どう認証したかです。** シークレットで取ったトークンは `401`、証明書で取ったトークンは `200`。
> 同じロールを持った、同じアプリの、同じ瞬間のトークンで。

**所見 5 の「経路によって変わる」という読みは、狭すぎました。** 経路 (Graph か `/_api/` か) も効いて
いますが、`/_api/` の中でさらに **資格情報の種類** が効いています。Graph は両方受け付け、`/_api/` は
片方だけを受け付けます。

### 追試 3b ― 差はトークンの中に見える

追試 3 のあと、トークンの `appidacr` クレームを記録するようにして、**同じ測定をもう一度**
回しました (2026-08-04、12 分後、同じランナー種別)。**5 本の呼び出しの結果は 1 つも変わらず**、
新しく読めるようになったのがこれです。

| レグ | 使った資格情報 (こちらが知っていること) | トークンの `appidacr` | `/_api/web` |
| --- | --- | --- | --- |
| app-only (シークレット) | クライアント シークレット | **`1`** | `401` |
| app-only (証明書) | 秘密鍵による署名 | **`2`** | `200` |
| 委任 | デバイス コード (パブリック クライアント) | **`0`** | `200` |

**3 本が 3 つとも違う値を持っていました。** そしてこの対応は、**このプローブ自身の実行の中で確かめた
もの**です ― どのレグがどの資格情報を使ったかはこちらが決めており、その 3 本に 3 つの異なる値が
返りました。値の一般的な意味は依然として**引いていません**が、**この 3 本については、クレームが
名乗り方に対応して動いた**ことが測れています。

これで言い方が 1 段正確になります。

> **`/_api/` が拒んでいるのは、`appidacr: 1` のトークンです。** `2` も `0` も通っています。
> 「証明書だけを受け付ける」ではなく、**シークレットで取ったものだけを拒む**、というのが
> 観測に合う形です。

**ただし委任の `0` は 1 変数の比較ではありません。** 委任トークンは `roles` ではなく `scp` を持ち、
背後に利用者がいます ― シークレットとの違いは `appidacr` だけではないので、この行は
「拒まれるのは `1` だけ」を **補強はしても、単独では証明しません**。1 変数で比べられているのは、
依然としてシークレットと証明書の 2 本だけです。

**証明書レグはアプリとして認識されています。** `currentuser` が返したのは:

```
i:0i.t|00000003-0000-0ff1-ce00-000000000000|app@sharepoint
```

利用者ではなく **アプリの identity** です。委任側の `i:0#.f|membership|<upn>` とは別物で、
**2 つのレグはトークン エンドポイントの上だけでなく、リソースの上でも別の identity** でした。
シークレット側は `401` なので、SharePoint がそれを誰だと思っていたかは分かりません ― **誰とも
思っていない**、というのが `401` の意味です。

### 追試 3 の限界

- **2 回の観測です。** シークレットの `401` は 5 回、委任の `200` は 5 回再現していますが、
  **証明書の `200` は 2 回** ― 12 分差の連続した 2 回で、どちらも同じジョブ形態です。日をまたいだ
  再現は取れていません
- **機構は分かりません。** 「なぜ SharePoint が `appidacr: 1` のトークンを拒むのか」は測って
  いません。この観測は **どの変数が効いているか** を示しただけで、**なぜ効くのか** は示していません
- **テナント設定か仕様かを切り分けていません。** テナント側にそういう設定があってそれが効いている
  のか、`/_api/` の恒常的な振る舞いなのかは分かりません。**片方だけを測っても区別できない**種類の
  問いで、区別するには設定を動かすか、別のテナントで測るかが要ります
- **拒否の理由は依然として返ってきません。** シークレット側の `WWW-Authenticate` は追試 1 と同じ素の
  challenge で、証明書に切り替えると通る、という手がかりは **応答の中には 1 文字もありません**
- **このテナントの話です。** 他のテナントや他の構成で同じかどうかは確かめていません

### コードへの反映 (追試 3)

トークンの **`appidacr` / `azpacr` クレーム** を読んで記録するようにしました。これは
「アプリがどう認証したか」を発行側が書き込む唯一の欄で、**追試 3 で効いた変数がトークンの中に見える
形で現れる唯一の場所** です。これが無いと、同じに見える 2 つのトークンが違う扱いを受けたという所見に、
レポートの中で引っかかるところがありません ― **追試 3b は、この列を足したから取れた測定** です。

**値は生のまま出します。** Entra は数値の意味を公開していますが、**このプローブはその対応を測って
いません**。トークンが `2` と言っているところに「証明書」と書けば、調べ物を観測として出すことに
なります ― グループの `PrincipalType` を生の数値のままにしているのと同じ規則です。

**追試 3b で分かったのは、あくまで「この 3 本については、値が名乗り方に対応して動いた」ことだけ**
です。`1` が一般に何を意味するかは、この文書のどこにも書いていません。

### 追試 4 ― 権限は変数だった。ただし門を通ったあとだけ

追試 2 (フル コントロール) と追試 3 (証明書) を **交差させました**。アプリの SharePoint アクセス許可を
もう一度 `Sites.FullControl.All` (アプリケーション) に差し替え、証明書レグを持ったまま同じ測定を
回します (2026-08-04、追試 3b の 23 分後、同じランナー種別)。Graph 側も証明書も触っていません。

| 呼び出し | シークレット (`appidacr: 1`) | 証明書 (`appidacr: 2`) | 委任 (`appidacr: 0`) |
| --- | --- | --- | --- |
| *(対照)* Graph `/sites/{host}:{path}` | `200` | `200` | `200` |
| `GET /_api/web` | `401` | `200` | `200` |
| `GET /_api/web/currentuser` | `401` | `200` | `200` |
| `GET /_api/web/sitegroups` | `401` | `200` | `200` |
| `GET /_api/web/sitegroups({id})/users` | `401` | **`200 OK` ― 0 メンバー** | `403` |

**1 セルだけ動きました。** 証明書レグのグループ メンバー列挙が、`403` から `200` に変わっています。
他の 14 本は追試 3b と 1 本も違いません。委任側は `403` のままで、アプリ ロールを使わないレグが
動かなかったことが、変えたのがアプリ側だけであることの担保になっています。

したがって、**変数は 2 つあり、順番があります。**

| | 変数 | 通らなかったとき | 証拠 |
| --- | --- | --- | --- |
| 1 段目 | **資格情報の種類** | `401` ― **権限は参照されない** | 追試 2: フル コントロールでも `401` |
| 2 段目 | **権限の強さ** | `403` | 追試 4: `Read.All` で `403`、`FullControl.All` で `200` |

> **`/_api/` は 2 段構えです。** まず「この資格情報は受け付けるか」を見て、通らなければ権限を
> 見ずに `401` を返します。通った先で初めて権限が効き、足りなければ `403` になります。
> **追試 2 が「権限は変数ではない」に見えたのは、当時のトークンが 1 段目で止まっていたからです。**

これは追試 2 の結論を **否定するのではなく、限定します**。あの実行が測っていたのは
`appidacr: 1` のトークンで、**そのトークンについては今も権限は変数ではありません** (追試 4 の
シークレット列は 4 本とも `401` のまま)。広すぎたのは、そこから「`/_api/` は権限を見ない」と
一般化した部分です。

**そして所見 6 の「何なら足りるのか」も埋まりました。** グループのメンバー列挙には
`Sites.Read.All` (アプリケーション) では足りず、`Sites.FullControl.All` で足ります ―
**証明書で名乗った app-only については**。

### 追試 4 の限界 ― `0 メンバー` が何についての事実か

**通った先にあったのは、空のグループでした。** これは 2 つのことを言っています。

- **委任側の `403` の向こうにあったのは、0 件だった。** 同じ実行、同じ瞬間、同じグループを、
  証明書レグは `200 OK` / 0 メンバーで読み、委任レグは `403` で断られています。所見 3 の
  「絞られた `200`」と裏返しで、**ここでは拒否のほうが、空であることを隠していました**
- **ただし、そのグループは選んだものではありません。** 実装は一覧の **先頭のグループ** を使い、
  この実行で先頭に来ていたのは `Limited Access System Group For List <GUID>` ― 所見 6 の副産物で
  触れた、**SharePoint が自分の都合で作ったとみられるグループ**です。`閲覧者` グループではありません

つまり **`0 メンバー` は、システム生成とみられるグループについての事実**であり、
**人が入っているグループでも同じ結果になるかは測っていません**。「メンバーを列挙できた」ことは
測れましたが、「メンバーを **見た**」とまでは言えません ― 見るべき中身がありませんでした。

**この危うさは、レポートの側で塞ぎました。** 対象グループの **名前を ID と並べて記録**します。
どのグループを読んだかで `0 メンバー` の意味は変わるのに、番号だけでは読み手に区別のしようが
ありません。

その他の限界:

- **1 回の観測です。** 追試 2 のときと同じで、付与状態を戻したあとにもう一度は測っていません
- **`FullControl` と `Read` の間は測っていません。** 足りる境界がどこかは分かりません
- **権限を戻すと `403` に戻るのか** も確かめていません (対称性は仮定していますが、測っていません)

### 確認していないこと

- ~~`401` の理由。~~ **測りました。理由はありません** ― 隠れているのではなく、SharePoint が言って
  いません。詳しくは上記。
- ~~機構。~~ **効いている変数は 2 つとも特定しました** (追試 3: 資格情報の種類 / 追試 4: 権限の強さ。
  この順に効きます)。**ただし「なぜ」は未解明** です ― 1 段目がテナント設定によるものか、`/_api/` の
  恒常的な仕様か、切り分けていません
- ~~Graph 側との対比の範囲。~~ **追試で埋めました** ― 同じ実行で Graph にも 1 本投げ、app-only でも
  `200` が返ることを確認しています。
- ~~証明書で名乗った app-only はどうか。~~ **測りました** (追試 3)。`200` です。

### 読むときの注意 ― CI のログはレポートそのものではない

この所見を取った実行で、`WWW-Authenticate` の中身は **GitHub Actions によって伏せられていました**。
ヘッダが `realm="<テナント ID>"` を含み、そのテナント ID がワークフローのシークレットとして登録されて
いるためです。**正しい動作ですが、測定値の一部が読めなくなります。**

しかも伏せ字への置換は**ログに書かれた後**に起きるので、表の桁が崩れます ― それが起きていること自体の
手がかりになります。

CI のログから読むかぎり、**レポートには写っていても読み手には届かない部分がありえます。** 識別子を
含みうる値を追うときは、シークレットの登録がないローカルで走らせるほうが確実です。

### コードへの反映

**応答ヘッダを記録するようにしました。** 本文が空の拒否について「拒否された、理由は不明」と記録して
いたのは、**理由を言っているサービスに対してこちらが聞いていなかった** ということです。「失敗は
測定値」と言いながら、測定値の半分を捨てていました。

`WWW-Authenticate` / `x-ms-diagnostics` / 相関 ID などを許可リストで拾い、本文にエラーコードが無い
場合はコンソールの「なぜ拒否されたか」欄に出します。全部ではなく許可リストなのは、応答ヘッダの大半が
運搬の事務であって、記録は読めることに意味があるからです。

**ヘッダは丸ごと出すのではなく、意味のある名前つきパラメータだけを抜き出します。** `WWW-Authenticate`
の challenge は、realm・リソース・信頼する発行者を名乗るのに最初の百数十文字を使い、`error=` があれば
その後ろに来ます。丸ごと出すと **理由の部分が切り詰めで落ちる** ので、`error` / `error_description` /
`reason` / `error_category` だけを残します。

**そして理由が 1 つも無かったときは、そう書きます。** 「ヘッダはあるが理由を含まない」と「ヘッダが
無い」と「読めなかった」は 3 つとも別の観測なので、3 つとも別の文言になります。理由が無い場合は
**値ではなくパラメータ名を並べます** ― 素の challenge は realm にテナントを名乗るので、CI のログでは
値が伏せ字になり、答えの形まで一緒に消えます。名前は誰の秘密でもないので、そこを通り抜けます。

この一連の直しの出発点は、レポートが **「拒否された、理由は不明」と「拒否された、理由は述べられて
いない」を同じ見た目で出していた** ことでした。前者はこちらの落ち度で、後者は観測です。

**この所見は、確定するまでにツールを 3 回直しています。** 1 回目は応答ヘッダを
まったく記録していませんでした。2 回目は記録したものの、定型が長すぎて意味のある部分が桁の切り詰めで
落ちました。3 回目でようやく「理由が無い」と「読めない」を分けました。**測定器が測れないものは、
測定対象の性質のように見えます。** 所見 3 と 4 で Graph について言ったのと同じことが、こちら側でも
起きていたわけです。

---

## 所見 6 ― 一覧はできるが、中身は見せない ― そして今度は `403`

> **【後日の訂正】この見出しは、サイトのグループ全体については成り立ちません。**
> この所見と最初の追試は **1 つのグループ** についての測定で、しかもそのグループは誰も選んで
> いません (実装が一覧の先頭を取っていた)。6 つ全部に訊いたところ、**中身が見せてもらえない
> のは 6 つ中 3 つだけ**で、しかもその 3 つは **SharePoint が自分の都合で作ったグループ**でした。
> `probe メンバー` `probe 閲覧者` `probe 所有者` の 3 つは、`Sites.Read.All` のままでも、
> 委任でも証明書レグでも **`200` で中身が返ります**。詳細は下の追試 2。
> 以下の観測そのものは正しく、**一般化した部分だけ**が間違っていました。

### 予測

書きませんでした。app-only が `/_api/` に通る条件を探す実行だったので、委任側は付き添いのつもりでした。

### 観測

**委任側でグループは一覧できます。中身は拒否されます。**

| 呼び出し | 委任 |
| --- | --- |
| `GET /_api/web/sitegroups` | **`200 OK`** ― 6 グループ、名前つき |
| `GET /_api/web/sitegroups({id})/users` | **`403 Forbidden`** |

`403` です。**`401` ではありません。**

### これが意味すること

**同じ実行のなかに、断られ方が 3 つ揃いました。**

| 断られ方 | どこで | 何が起きているか |
| --- | --- | --- |
| `401` + 素の challenge | app-only (シークレット)、`/_api/` の全部 | **認証されていない。** 資格情報として受け付けられていない |
| `403` | 委任、グループの中身 | **認証されている。操作が拒否された** |
| `200` + 空/絞り込み | 委任、Graph の権限一覧 (所見 3) | **認証も認可も通った。中身が絞られた** |

所見 5 で「`403` ではなく `401` である」ことを重要だと書きましたが、それは対比する `403` を持たない
主張でした。**いまは同じ実行の、同じ API から出た `403` が隣にあります。** SharePoint はこの 2 つを
実際に使い分けており、app-only に返しているのは「あなたが誰かは分かったが権限が足りない」ではなく
**「あなたが誰か分からない」** のほうだと確定します。

### 追試 1 ― `403` は委任だけのものではない (**対象は 1 グループ。追試 3 参照**)

所見 5 の追試 3 で証明書レグを足したところ、**アプリの identity も同じ形で断られました。**

| 呼び出し | app-only (証明書) | 委任 |
| --- | --- | --- |
| `GET /_api/web/sitegroups` | `200 OK` ― 6 グループ | `200 OK` ― 6 グループ |
| `GET /_api/web/sitegroups({id})/users` | **`403 Forbidden`** | `403 Forbidden` |

証明書レグは `Sites.Read.All` を **アプリケーション** ロールとして持ち、サイトを読め、グループを
一覧でき、**それでもメンバーは見せてもらえません**。この行を書いた時点で持っていた読み ―
「閲覧者はグループの中身を見られない」 ― は、**利用者の役割の話としては狭すぎました。**

> **「一覧はできるが中身は見せない」は、閲覧者という役割の性質ではありません。** 少なくとも
> このサイトの、この 1 つのグループについては、**アプリの identity にも同じ答えが返ります。**

ただし **これも 1 回、1 グループの観測** です。「サイトのメンバー全員に一律」なのか「たまたまこの
グループがそう」なのかは、**分けられていません**。

そしてもう 1 つ、否定的な形で分かったこと: **ここでは `Sites.Read.All` というアプリケーション
ロールでは足りていません** ― 足りているなら、サイトが読めた identity が同じ呼び出しで断られる理由が
ありません。

### 追試 2 ― 足りるものが分かった (**対象は 1 グループ。追試 3 参照**)

所見 5 の追試 4 で `Sites.FullControl.All` に差し替えたところ、**証明書レグのこの `403` が `200` に
変わりました。** 動いたのは 15 本中この 1 本だけです。

| 付与 | 証明書レグの `sitegroups({id})/users` | 委任 |
| --- | --- | --- |
| `Sites.Read.All` | `403 Forbidden` | `403 Forbidden` |
| **`Sites.FullControl.All`** | **`200 OK` ― 0 メンバー** | `403 Forbidden` (変わらず) |

**「一覧はできるが中身は見せない」は、アプリの identity については権限で動きます。** 委任側は
アプリ ロールを使わないので変わらず、そこは役割の話のままです。

**そして `200` の中身は 0 件でした。** 委任側の `403` が隠していたのは、**空のグループ**です ―
同じ実行、同じ瞬間、同じグループで。ただし対象は一覧の先頭に来た
`Limited Access System Group For List <GUID>` で、**上で「自動生成とみられる」と書いたそれ**です。
`閲覧者` グループではありません。詳しくは所見 5 の「追試 4 の限界」を参照してください。

そしてもう 1 つ。**「グループの一覧」と「グループの中身」は、少なくとも一部のグループについて別の
権限です。** 一覧は 6 つとも返るのに、中身が返るのは 3 つだけ ― **一覧のほうが広い**、という形は
残ります (**ただし「中身は一切見せない」ではありません**。追試 3)。

所見 3 で Graph 側について見たのと同じ形 ― *誰がアクセスを持っているか* は、アクセスを持っている
だけの人には見せない ― が、SharePoint REST でも部分的に成り立っています。**ただし現れ方が違います。**
Graph は `200` と空のコレクションで、SharePoint は `403` で。

**2 つが同じ方針から出ているのかは分かりません** ― 対象も違えば (ファイルの権限エントリと、グループの
メンバー) API も違います。言えるのは、**似た問いに対して 2 つの API が違う断り方をする** ことです。

### 確認していないこと

- **この `403` にも理由はありません。** 本文は空、エラーコードも診断ヘッダもなし。所見 5 の `401` と
  同じく、拒否そのものしか返ってきません
- ~~どのグループなら中身が見えるのか。~~ **6 つ全部に訊きました** (追試 3)。`Read.All` で見えるのは
  管理者が作った 3 つ、見えないのは自動生成とみられる 3 つです
- ~~人が入っているグループでも同じか。~~ **測りました。見えます** ― `probe 閲覧者` `probe 所有者` は
  どちらも 1 メンバーが返ります。**ただし 2 人以上のグループは 1 つも無いので、「一部だけ見える」が
  起きうるかは分かりません**
- **メンバーではなく所有者・メンバー権限の利用者ならどうか。** 所見 3 と同じ未確認事項です
- ~~何を持てばメンバーが見えるのか。~~ **自動生成とみられる 3 つには `Sites.FullControl.All`
  (アプリケーション) で足ります** (追試 4)。**間のどこが境界かは測っていません**
- **なぜ自動生成のグループだけ扱いが違うのか。** 割れ方は測りましたが、**理由は測っていません**。
  そもそも「自動生成」という区別自体が名前を見た人間の読みで、ツールの判定ではありません

### 追試 3 ― 全グループに訊いたら、所見が半分になった

ここまでの測定はすべて **1 つのグループ** についてのものでした。実装が一覧の先頭を 1 つ取っていた
ためで、**そのグループを選んだ人は誰もいません**。追試 2 で `0 メンバー` を「グループの中身」として
読みかけたのは、この設計の帰結です。

そこでツールを **全グループに訊く**ように変え、`Sites.Read.All` に戻したうえで測りました
(2026-08-04、GitHub ホストのランナー)。**予測は立てていません。**

サイトのグループは 6 つ。**答えは 3 つと 3 つに割れました。**

| id | グループ | シークレット | 証明書 | 委任 |
| --- | --- | --- | --- | --- |
| 13 | `Limited Access System Group For List <GUID>` | `401` | **`403`** | **`403`** |
| 14 | `Limited Access System Group For Web <GUID>` | `401` | **`403`** | **`403`** |
| 12 | `SharingLinks.<GUID>.OrganizationEdit.<GUID>` | `401` | **`403`** | **`403`** |
| 5 | `probe メンバー` | `401` | **`200` ― 0 メンバー** | **`200` ― 0 メンバー** |
| 4 | `probe 閲覧者` | `401` | **`200` ― 1 メンバー (`PrincipalType 4`)** | **`200` ― 1 メンバー (`PrincipalType 4`)** |
| 3 | `probe 所有者` | `401` | **`200` ― 1 メンバー (`PrincipalType 1`)** | **`200` ― 1 メンバー (`PrincipalType 1`)** |

**この所見は、半分が測定の副産物でした。**

> **「一覧はできるが中身は見せない」は、サイトのグループ全体については成り立ちません。**
> `Sites.Read.All` のままでも、**管理者が作った 3 つのグループは中身まで返ります** ―
> 委任の閲覧者にも、証明書で名乗った app-only にも。中身が見せてもらえないのは、
> **SharePoint が自分の都合で作ったとみられる 3 つ**だけです。

1 つしか訊いていなかったあいだ、**「一律に断られる」と「このグループだけ断られる」は同じに見えて
いました。** そして先頭に来ていたのが後者の側だったので、狭いほうの事実を広い主張として書いて
いたことになります。

**もう 1 つ、割り方が同じであることも観測です。** 6 マス全部で、**証明書レグと委任レグの答えが
1 つも違いません**。`Sites.Read.All` を **アプリケーション ロール**として持つ app identity は、
この呼び出しに関しては **サイトの閲覧者 1 人と同じところまでしか見えていません**。

### 追試 4 ― `FullControl` で割れ方がどう変わるか

続けて `Sites.FullControl.All` に差し替え、同じ測定を回しました (2 分後、テナントは他に無変更)。

| id | グループ | 証明書 (`Read`) | 証明書 (**`FullControl`**) | 委任 (両方) |
| --- | --- | --- | --- | --- |
| 13 | `Limited Access ... For List` | `403` | **`200` ― 0 メンバー** | `403` |
| 14 | `Limited Access ... For Web` | `403` | **`200` ― 0 メンバー** | `403` |
| 12 | `SharingLinks...` | `403` | **`200` ― 0 メンバー** | `403` |
| 5 | `probe メンバー` | `200` / 0 | `200` / 0 | `200` / 0 |
| 4 | `probe 閲覧者` | `200` / 1 | `200` / 1 | `200` / 1 |
| 3 | `probe 所有者` | `200` / 1 | `200` / 1 | `200` / 1 |

- **動いたのは、`403` だった 3 マスだけ**です。もともと通っていた 3 つは何も変わりません
- **委任は 1 マスも動きません。** アプリ ロールを使わないレグなので、変えたのがアプリ側だけで
  あることの担保になっています
- **`FullControl` にして初めて、app identity が閲覧者より先に行きます。** `Read.All` では 6 マス
  全部が一致していたのが、ここで 3 マス割れました
- **システム生成とみられる 3 つは、いずれも 0 メンバー**でした。3 つとも空だったことは測定です。
  **なぜ空なのかは測っていません**

所見 5 の追試 4 で立てた「1 段目は資格情報、2 段目は権限」という形は、**6 マスに広げても崩れて
いません** ― シークレットは 6 マスとも `401` で、どちらの付与でも変わりませんでした。

### 追試 3・4 の限界

- **`PrincipalType` の `4` と `1` が何を指すかは測っていません。** 分かるのは
  **`probe 閲覧者` と `probe 所有者` の唯一のメンバーが違う種類である**ことだけです
  ([environment.md](environment.md) には検証用利用者がセキュリティ グループ経由で閲覧者になって
  いると記録がありますが、**この数値がそれを指すとは確かめていません**)
- **`probe メンバー` が 0 件**であることは、このサイトにメンバー権限の利用者を置いていないという
  環境の記録と整合します。**整合するだけで、確かめてはいません**
- **グループの「種類」をこのツールが判定しているわけではありません。** 分けているのは名前を見た
  人間で、`Limited Access ...` や `SharingLinks...` が自動生成であるという読みは
  [所見 6 の副産物](#副産物--自分たちの実験が環境を変えていた)と同じ推測のままです
- **1 回ずつの観測**です。それぞれ 1 回だけ回しました
- **メンバーが 2 人以上のグループは 1 つもありません。** 最大でも 1 人なので、
  「一部だけ見える」という形が起きうるかは測れていません

### 副産物 ― 自分たちの実験が環境を変えていた

一覧に出た 6 つのうちいくつかは `Limited Access System Group For List <GUID>` という名前でした。
名前からすると SharePoint が自動生成したもので、**所見 4 のために `restricted` フォルダの継承を切った
ことが原因と見られます**。

**どちらも推測です。** この種のグループがいつ作られるかは調べていませんし、作られた時刻も記録して
いません。**測定として分かっているのは、当初 3 つだったものが 6 つになっていたことだけ**です。

環境は測定によって変わります。**この文書と [environment.md](environment.md) を分けて、後者に変更履歴を
持たせているのは、まさにこれが起きるから**です。

---

## 所見 7 ― 拒否が、こちらの操作と無関係に現れた

### 予測

ありません。**そもそも測ろうとしていませんでした。** 別のことを測っている最中に、道具のほうが
止まりました。

### 観測

`acl` を実行したところ、**デバイス コードのサインインが拒否**されました。ブラウザに出たのは
これです。

```
これに対するアクセス権がありません
サインインは完了しましたが、このリソースへのアクセス許可がありません。
Error Code: 530035
```

**「アクセス許可がありません」。** アクセス許可の話に見えます。この文書がずっと追ってきた種類の
文言です。**しかし権限の話ではありませんでした。**

サインイン ログの条件付きアクセスの詳細を、**成功していた実行と並べる**と、こうなります。

| | 成功時 (05:58Z) | 失敗時 (07:12Z) |
| --- | --- | --- |
| ポリシー | Security Defaults | Security Defaults |
| ポリシーの状態 | 有効 | 有効 |
| ユーザー (ミク) | 構成されていない | **一致しました** |
| リソース (Microsoft Graph) | 構成されていない | **一致しました** |
| 結果 | **適用されていません** | **失敗** |
| 制御の許可 | ― | **ブロック** |

**同じポリシーが、同じ「有効」のまま、適用対象外から適用対象内に変わり、ブロックしました。**

そのあいだに **こちらはテナントを 1 つも触っていません。** 条件付きアクセス ポリシーは前後とも
0 件、セキュリティの既定値群は前後とも有効 ― どちらも実機で確認しています。**Security Defaults の
割り当ては利用者が編集できるものではありません。**

### これが意味すること

**環境は、こちらの操作と無関係に変わります。** [environment.md](environment.md) には
「測定が環境を変えていた」という項がすでにありますが、これはその逆です ― **誰も何もしていないのに
変わりました。**

そして重要なのは、**変わったと言えた理由**です。

> **同じ測定が、変化の前と後の両方に残っていたからです。** 片方しか無ければ「昔から拒否されて
> いたのかもしれない」で終わっていました。

もう 1 つ。**この拒否は、権限の所見と見分けがつきません。** ブラウザは「アクセス許可がありません」
と言い、レポートの委任列は空になります。所見 3〜6 で追いかけてきたどの形とも同じ見た目です。
**見分けられたのは、`AADSTS` の番号が記録に残っていて、サインイン ログまで辿れたからだけ**でした。

このリポジトリはワークフローのログに毎回こう出しています。

> ここでの拒否を、そのまま権限の発見として読まないでください。条件付きアクセスがその点だけを
> 理由に拒否することがあり、レポートはそれを「権限がない」と読める形で報告します。

**その注意書きが現実になりました。** ただし、そこで想定していた形とは違います ― 想定していたのは
「ランナーとローカルで結果が違う」でしたが、実際に起きたのは **「同じ場所で、時刻だけが違う」**
でした。

### 確認していないこと

- **Security Defaults のどの規則が効いたのか。** 画面はポリシー名までしか言いません。条件はすべて
  「構成されていない」ので、条件で弾かれたのではなく **ポリシー自体の許可制御がブロック**です
- **デバイス コード フローを狙ったものなのかどうか。** **決着させる実験は 1 つ** ― 同じアプリ・
  同じユーザー・同じテナントで、**フローだけを対話型に変える**。通ればフローが変数、通らなければ
  違う。**まだ実行していません**（リダイレクト URI の登録が要ります）
- **元に戻るのかどうか**

### environment.md に、破れた一文があります

```
MFA は全利用者に強制されます。デバイス コード フローはモダン認証なので
妨げられませんが、サインインの手数には効きます。
```

**これは測定ではなく読みでした。** そして破れています ― 何かがデバイス コード フローを止めて
います。**何がとまでは言えない**ので、そう書き換えてあります。

### コードへの反映

**`Identities` 設定を足しました** (`all` / `app-only`)。`app-only` にすると委任レグは
**ネットワークに触らず**、デバイス コードも表示されません。

**行は消えません。** `NotRun` として残り、理由は「この実行は app-only に限定された」。そして
**終了コードは `0`** です ― **誰も試みていないサインインは、失敗したサインインではありません。**
デバイス コードを表示して誰も完了しなかった `2` とは、意味が逆です。

これは所見 5 で `TokenResult.Requested` を足したのと同じ区別です。あのときは「Entra が断った」と
「そもそも何も訊いていない」でした。今回は「人が来なかった」と「人を呼んでいない」です。
**同じ間違いが違う衣装を着て出てきたので、同じ形で分けました。**

GitHub Actions の `identities` 入力は **既定を `app-only`** にしました。ランナーにはブラウザも人も
いないので、そちらが既定であるべきでした ― **この拒否が起きる前から、そうであるべきでした。**

---

## 所見 8 ― ACL は束で取れる。ただし経路は 1 つだけ

### 予測

立てていません。**候補を 3 つ並べて、どれが通るかを測りました。**

### 観測

所見 3 と 4 は、権限一覧を **アイテム 1 件につき 1 回** 読んで得たものです。ファイルの数だけ呼ぶと
いうことは、ライブラリ全体の ACL を毎回読むのは、ファイルが数個のうちしか成り立ちません。安い経路が
あるかどうかを、`Sites.Read.All` と `Sites.FullControl.All` の 2 つの付与で測りました
(2026-08-04、GitHub ホストのランナー、app-only の 2 本のみ)。

| 経路 | 歩く範囲 | `Read.All` | `FullControl.All` |
| --- | --- | --- | --- |
| **基準** ― `children` + 件数だけ `/permissions` | ルート直下 | **5 回 / 4 件 / 17 エントリ** | 同じ |
| A ― Graph `children?$expand=permissions` | ルート直下 | **`400 notSupported`** | **`400 notSupported`** |
| B ― Graph `delta?$expand=permissions` | ドライブ全体 | **`501 notSupported`** | **`501 notSupported`** |
| C ― SP `items?$expand=RoleAssignments` (証明書) | リスト全体 | **`403`** | **`200` ― 2 回 / 5 件 / 18 エントリ** |
| C ― 同上 (シークレット) | リスト全体 | `401` | **`401`** |

> **束で取れます。ただし経路は 1 つだけで、条件が 3 つ揃ったときだけです** ―
> **SharePoint REST** であること、**証明書で名乗った** app-only であること、
> **`Sites.FullControl.All`** を持っていること。

### コストの形が変わります

これが問いの本体でした。**回数の「量」ではなく「形」が違います。**

| | 呼び出し回数 | 4 件のとき | 100 件のとき (外挿) |
| --- | --- | --- | --- |
| 基準 | **1 + 件数** | 5 回 / 2893 ms | 101 回 |
| C | **2 回** (探索 1 + 本体 1) | **2 回 / 662 ms** | **2 回** |

**基準は件数に比例し、C は比例しません。** 4 件で 5 回対 2 回というのは大した差に見えませんが、
**差が開き続ける**ことのほうが所見です。「毎回、全ファイルの ACL を読む」は、この経路でなら
現実的になります。

**ただし外挿は外挿です。** 測ったのは 1 ページ (`$top=100`、続きのページなし) だけで、
**ページをまたいだときに 1 ページ 1 回で済むかは測っていません。**

### A と B は権限の話ですらありません

`notSupported` は 2 つの付与状態でも、2 つの identity でも同じでした。**権限を動かしても
動きません** ― 所見 5 の追試 2 と同じ形の否定的な結果です。返しているのは Graph 自身のコードで、
「そういう組み合わせは提供していない」と言っています。

**ただし、それぞれ 1 通りの書き方しか試していません。** `$select` と併用するなど別の形なら通る
可能性は潰していません。測ったのは **「この書き方では受け付けられない」** までです。

### C は所見 5・6 の並びをもう一度なぞります

証明書レグは `Read.All` で **`403`**、`FullControl.All` で **`200`**。シークレットレグは
どちらでも **`401`**。**所見 5 の 2 段構え**（1 段目は資格情報、2 段目は権限）と、**所見 6 の追試**
（`Read.All` では足りず `FullControl.All` で足りる）を、**別の呼び出しでもう一度なぞっています。**

3 つ目の独立した例なので、あの形はこのテナントでは相当に安定していると言えます。
**なぜそうなのかは、相変わらず測っていません。**

### 件数が違うのは、食い違いではありません

C は **5 件**、基準は **4 件** を返します。**同じことを訊いていないからです。**

- 基準と A は `/drive/root/children` ― **ルート直下だけ**（3 ファイル + `restricted` フォルダ）
- C は リストのアイテム ― **入れ子も含めた全体**（そこに `restricted` の中のファイル D が加わる）

つまり C は **再帰しなくてよい**という利点も持っています。基準で同じ範囲を取るなら、フォルダごとに
`children` を辿る呼び出しがさらに要ります。

**17 と 18 というエントリ数を並べて「ほぼ一致」と読んではいけません。** 対象の集合が違ううえ、
**SharePoint の RoleAssignment と Graph の権限エントリは別のオブジェクト**です。ツールはこの 2 つを
突き合わせません ― 突き合わせれば「2 つのモデルは対応する」と主張することになり、それは測って
いません。

### コードへの反映

**経路ごとに「何を歩くか」を出すようにしました。** 5 件と 4 件が並んでいるのに、**なぜ違うのかが
レポートから読めませんでした** ― 番号だけでは食い違いに見えます。あわせて **アイテム 1 件ごとの
内訳** (名前と ACL の形) を JSON に出すので、件数を信じる代わりに確かめられます。

所見 6 で「グループの名前を ID と並べて記録する」ようにしたのと同じ理由です。**数だけでは、
何についての数かが分かりません。**

### 確認していないこと

- ~~**ページをまたいだときの挙動。** 1 ページ (`$top=100`) しか測っていません~~
  **測りました** (下の追試 1)
- **`Read.All` と `FullControl.All` の間のどこが境界か**
- **委任ではどうか。** この 2 回は app-only のみです (所見 7 のため)
- **A と B の別の書き方**
- **C が返す `RoleAssignments` が、Graph の権限エントリと同じ事実を表しているのかどうか。**
  数が近いのは測定であって、対応の証拠ではありません
- **1 回ずつの観測**です

---

## 所見 8 の追試 1 ― 束取りの経路はページを追う。1 回では終わらない

### 予測

立てていません。**ただし、測る順番には理由があります。**

所見 8 は「ページをまたいだときの挙動は未測定」と自分で書いていました。**そこを埋める前に、
道具の側を直す必要がありました** ― このツールは 1 ページ目を読んで、その回数と時間を
**「経路の費用」として報告していた**からです。

> **追えない道具で測っても、切ったのが道具かサービスかが区別できません。**

そこで **道具を直す → 直ったことを測る → 権限を上げる → 測る → 戻す → 戻ったことを測る**の順に
しました (2026-08-05、実行 #55〜#61)。

### まず、道具が追えることを確かめました

ライブラリには 7 件しかないので、**`$top=2` を渡してページを割りました。** ファイルを増やさずに
「継続リンクを追えるか」だけを切り出すためです。

| | 要求した `$top` | 基準の呼び出し | ページ | 終わり方 |
| --- | --- | --- | --- | --- |
| #55 | (未設定) | 8 | 1 | `one page, no continuation offered` |
| **#56** | **`$top=2`** | **11** | **4** | **`4 pages, then the collection ended`** |

**7 件を 2 件ずつで 4 ページ、そこに 7 本の権限呼び出しで 11。** `items` も `acl entries` も
#55 と同じ ― **ページを割っても答えは変わりません。**

**#55 は入力を取り違えた実行**です (`2` が `page_size` ではなく `page_limit` に入っていた)。
**それをログの環境変数を読むまで判別できませんでした。** レポートが「いくつで訊いたか」を
言っていなかったからで、**そこは道具の落ち度**として直しました ― 要求した `$top` と実際の上限を
レポートの頭に出します。

### 次に、権限を上げました

**4 状態を測りました。** SharePoint のアプリケーション付与だけを差し替え、**Graph と Azure RMS は
1 文字も触っていません。**

| 状態 | run | SharePoint のロール | C (証明書) |
| --- | --- | --- | --- |
| **前** | #56 / #57 | `Sites.Read.All` | **`403`** ― 2 呼び出し |
| **`FullControl.All`** | #58 / #59 | **`Sites.FullControl.All`** | **`200` ― 5 呼び出し / 4 ページ / 8 件 / 33 エントリ** |
| **戻し** | #60 / #61 | `Sites.Read.All` | **`403`** ― 2 呼び出し |

**前と戻しが一致しました。** `auth` の 3 API × 2 資格情報も、`acl` の 12 行も。

### 1. 束取りの経路もページを追います

**5 呼び出し = ライブラリ探索 1 + ページ 4。** `how the walk ended` は
`4 pages, then the collection ended`。

**追ったのが「サービスのリンク」であることは、URL の形が示しています。**

```
GET .../_api/web/GetList('%2Fsites%2Fpro...     1 ページ目 (このツールが組み立てた)
GET .../_api/web/GetList(%27/sites/probe...     2 ページ目 (SharePoint が返した __next)
GET .../_api/web/GetList(%27/sites/probe...     3 ページ目
GET .../_api/web/GetList(%27/sites/probe...     4 ページ目
```

**エンコードが違います。** 2 ページ目以降は `'` が `%27` で、パス区切りが素のまま。
**このツールが作る形ではありません** ― ページ番号を自分で組み立てたのではなく、
**返ってきたリンクをそのまま叩いている**ことが、URL だけで確かめられます。

### 2. 費用の比較が、初めて実物になりました

```
one at a time: 11 calls;  spItems: 5 calls
```

**ただし所見 8 の「2 回」は、1 ページ分の数字でした。** 実際には**ページ数だけ呼びます** ―
`$top=2` では 4 ページ、`$top=100` なら 1 ページ。**「1 回で済む経路」ではありません。**

> **束取りが安いのは、呼び出しがアイテム数ではなくページ数に比例するから**であって、
> **定数だからではありません。**

**時間は今回使えません。** #59 の呼び出し一覧には **30,750 ms** と **17,047 ms** の Graph 呼び出しが
1 本ずつあり、基準の 51,213 ms はそれで説明が付きます。**経路の性質ではなく、その瞬間の詰まり**です。
**回数のほうは読めます。**

### 3. 8 件と 7 件は食い違いではありません

C はリスト全体を入れ子まで、基準はルート直下だけを歩きます。**`walks` 列がそのためにあります** ―
所見 8 で 5 件と 4 件を並べたときに入れたものが、そのまま効いています。

### 4. シークレットは、フル コントロールでも `401` のまま

**所見 5 の追試 4 と同じ形が、また出ました。** 権限を上げても手前の門は動きません。
**効いているのは名乗り方 (`appidacr`) で、権限はその先の話**です。

### 戻しました

**`Sites.FullControl.All` は残していません。** テナント内の全サイト コレクションへのフル
コントロールを、シークレットが CI にあるアプリに持たせ続ける理由はありません ―
`Content.SuperUser` と同じ扱いです。

### 確認していないこと

- **サービスの側が切る点があるか。** **これが本命の残りです** ― 要求より小さくページを切るか、
  ある深さから先はリンクを出さなくなるか。**測るには 100 件超のファイルが要ります**
- **Graph の 2 経路 (A・B) はページを追う機会がありませんでした。** 4 状態とも 1 ページ目で
  `notSupported` です
- **委任ではどうか。** 7 実行とも app-only のみ
- **`$top` の上限。** 測ったのは 2 と 100 だけです
- **時間の比較。** 詰まりの入った 1 回しかありません

---

## 所見 8 の追試 2 ― サービスも自分でページを切る。そして 257 件で、別の断られ方が出た

### 予測

立てていません。**追試 1 が残した宿題は 1 つでした** ― 「サービスの側が切る点があるか」。
**7 件のライブラリでは、どちらに触っても 1 ページで終わってしまう**ので、越えるまで見えません。

**ライブラリに 250 枚のプレースホルダーを置きました** (`probe-bulk-001.txt` 〜 `probe-bulk-250.txt`、
中身はただのテキスト)。**A〜G と合わせて 257 件**です。**`$top` は一切渡していません** ―
見たいのは**サービスが自分で決めるページの大きさ**なので、こちらから指定したら測るものが変わります。

### 観測

| 状態 | run | SharePoint のロール |
| --- | --- | --- |
| **前** | #63 | `Sites.Read.All` |
| **`FullControl.All`** | #65 | `Sites.FullControl.All` |
| **戻し** | #66 | `Sites.Read.All` |

### 1. Graph の `/children` の既定ページは 200 でした

```
one at a time: 259 calls for 257 items
```

**259 = 257 件の権限呼び出し + 2 ページ。** `$top` を渡していないのに 2 回に分かれました ―
**200 件と 57 件**です。

**7 件のときは 1 ページでした。** 同じコード、同じ呼び出し、同じ権限で、**件数だけが変わって
ページが増えました。** サービスが自分で切る点は、**あります。**

### 2. SharePoint の束取りも切れました

```
SP items?$expand=RoleAssignments / app-only (cert)
  258 items, 258 of them carrying an ACL, 1283 entries in 4 calls
```

**4 = ライブラリ探索 1 + ページ 3。** この経路は `$top=100` を送るので、100 + 100 + 58 です。
**追い切っており、欠けはありません** (258 / 258)。

### 3. 費用の差は、件数とともに開きました

| | 呼び出し | 時間 |
| --- | --- | --- |
| 1 件ずつ | **259** | 70,529 ms |
| 束取り | **4** | **5,594 ms** |

**65 分の 1 の呼び出し、13 分の 1 の時間。** 7 件のときは 11 対 5 でした ―
**追試 1 の「ページ数に比例する」が、そのまま数字に出ています。**

---

### 測ろうとしていなかったもの ― 5 つ目の断られ方

**`FullControl` の実行 (#65) だけ、`with acl` が `items` を下回りました。**

| #65 | items | **with acl** | entries |
| --- | --- | --- | --- |
| シークレット | 257 | **223** | 893 |
| 証明書 | 257 | **238** | 953 |

そして Graph の束取り経路が ―

```
429 Too Many Requests: activityLimitReached
```

**スロットリングです。** 所見 8 からずっと `notSupported` だった経路が、**同じテナント・同じ権限で
別の答えを返しました。**

> **返ってくる拒否は、経路と権限だけでは決まりません。そのときの負荷でも変わります。**

**同じ実行の中で、同じ経路が 2 つの答えを出しています** ― `children?$expand` は
**シークレットのレグで `429`、証明書のレグで `400 notSupported`**。**呼び出し元の違いではありません。**
**先に走ったほうが枠を使った**、という順番の違いです。

### 権限ではないことを、測って切り分けました

**#66 は #65 の 1 時間後、権限を戻して、ファイルは 250 枚置いたまま**回しました。

| | items | **with acl** | entries | calls | Graph の 2 経路 |
| --- | --- | --- | --- | --- | --- |
| **#63** (`Read.All`) | 257 | **257** | **1029** | 259 | `400` / `501` |
| #65 (`FullControl`) | 257 | 223 / 238 | 893 / 953 | 259 | **`429`** が 3 本 |
| **#66** (戻し) | 257 | **257** | **1029** | 259 | `400` / `501` |

**#63 と #66 が完全に一致しました。** 同じ状態を 1 時間離して 2 回測って、**1 バイトも違いません。**

### 片付けました

**250 枚を削除し、もう 1 回測りました** (#67)。

| | items | with acl | entries | calls | pages |
| --- | --- | --- | --- | --- | --- |
| **#55** (置く前) | 7 | 7 | 29 | 8 | **1** |
| #63 / #66 (257 件) | 257 | 257 | 1029 | 259 | **2** |
| **#67** (削除後) | **7** | **7** | **29** | **8** | **1** |

**置く前と削除後が一致しました。** ページ数が 2 から 1 に戻っていることが、
**ページの数がライブラリの件数で決まっていた**ことをもう一度示しています ―
**コードも権限も、この 3 回のあいだ 1 文字も変わっていません。**

**したがって #65 の欠けは負荷であって、権限ではありません。** 権限を上げて ACL が減ったように
見えたのは、**その時間帯に 500 本以上の呼び出しを流していたから**です。

**#65 の 1 件ずつの数字 (893 / 953) は、この所見では使いません。**

### `with acl` 列が仕事をしました

**件数だけ見れば、#65 も 257 件返っています。** 一覧は完全で、どの呼び出しも例外を投げていません。
**欠けは `with acl` 列にしか出ませんでした。**

所見 8 でこの列を足したのは「展開が無視されたかどうか」を見るためで、**スロットリングを想定して
いたわけではありません。** それでも捕まりました ― **「何件返ったか」と「何件が中身を伴っていたか」を
別々に数えていたから**です。

### この道具は `429` に何もしていません

**`Retry-After` を見ません。** 返ってきた状態をそのまま記録して次のアイテムへ行きます。
**結果として「読めなかった」が静かに増えます。**

**直すかどうかは決めていません。** README の「失敗する呼び出しを成功させようとしない」に触りますが、
**`429` は拒否ではなく「あとで来い」**なので、権限の拒否とは別物という読み方もできます。
**いまは、起きたことを記録するに留めています。**

### 確認していないこと

- **`429` が出る閾値。** 出た実行と出なかった実行があるだけで、**境界は測っていません**
- **`Retry-After` に何秒入っていたか。** このツールは応答ヘッダーを保持していません
- **Graph の既定 200 が `$top` の上限でもあるのか。** 測ったのは 2・100・(未指定) だけです
- **SharePoint の `items` に `$top` を渡さないとどうなるか。** この経路は常に `$top` を送ります
- **委任ではどうか。** app-only のみです

---

## 所見 9 ― 代理を名乗る値は効く。ただし効いているのは 2 つのうち 1 つだけ

### 予測

立てていません。**`FileEngineSettings` の 2 つのフィールドを別々に動かして、答えがどちらに付いて
くるかを測りました。**

### 観測

MIP File SDK は、app-only のアプリが「誰かの代理」として保護されたコンテンツを開く道を、2 つの
フィールドで表します ― **`Identity`** と **`DelegatedUserEmail`**。**別物なので両方設定する**、
というのが SDK の求める形です。**どちらが認可に効くのかは、どこにも書かれていません。**

測定条件 (2026-08-05、GitHub ホストのランナー、実行 #33 / #34 / #35):

- ラベル **`probe-miku-only`** ― **ミク 1 人だけに権利を与える**保護ラベル。リンは付与先ではない
- ファイル **`probe-miku-only.docx`** ― **ミクが作成し保護**。したがって**ミクは所有者**
- リン ― 実在・ライセンスあり・**付与先でも所有者でもない**
- アプリ ― app-only、**証明書**で名乗る、**`Content.DelegatedReader`** (管理者同意済み)
- **リンは一度もサインインしていません。** 出てくるのは値としてだけです

| `Identity` | `DelegatedUserEmail` | 結果 | 発行先 |
| --- | --- | --- | --- |
| miku | miku | **開く** (16,928 バイト復号) | miku |
| **rin** | **miku** | **開く** (同上) | **miku** |
| miku | rin | `NoPermissionsException` | - |
| rin | rin | `NoPermissionsException` | - |
| miku | (空) | `NoPermissionsException` | - |
| rin | (空) | `NoPermissionsException` | - |
| (空) | (空) | `BadInputException` ― エンジンが作れない | - |

拒否の文言は 4 本とも同じです ― **`User does not have rights to consume the content.`**

> **列だけで結果が決まっています。行は結果を変えません。**

### これが意味すること

- **`DelegatedUserEmail` が単独で決めています。** `Identity` にリンを入れたまま
  `DelegatedUserEmail` にミクと書けば開き、**ライセンスはミクに発行されます** ― 名乗っている側では
  なく、**名指しされた側の権利**が返ります
- **`Identity` は「あること」だけが要ります。** 空だとエンジンが作れませんが、**中身は認可に
  使われていません**
- **空はフリーパスではありません。** 名指しが無ければ拒否されます
  > **【2026-08-05 の限定】** これは **`Content.SuperUser` が無いときの話**でした。あの付与があると
  > **未設定レグは開き、ライセンスはアプリ自身に `OWNER` で発行されます** (所見 11)。測定は正しく、
  > **「常に拒否される」と読んだ部分だけ**が広すぎました。

> **このアプリが到達できる範囲は、名指しできる利用者の権利の総和です。**
> 個々の利用者の権利は守られています ― リンを名乗れば、リンに無いものは開けません。
> **止めているのは「アプリが誰か」ではなく「名指しした相手が何を持っているか」だけ**で、
> **誰を名指しするかはアプリが選びます。**

**これは所見 2 の続きです。** あのとき Azure RMS のトークンは**ロールを 1 つも持っていません
でした**。`Content.DelegatedReader` を付けたらそのロールが載り (所見 2 の追試)、**今回そのロールで
実際にコンテンツが開きました。** 「トークンが出る」→「ロールが載る」→「中身が開く」の 3 段が、
初めて最後までつながっています。

### 保護されたまま届いていました

これは別に確かめる必要がありました。**Graph の `/content` が復号済みを返していたら、この実験は
成立しません。**

| | バイト数 |
| --- | --- |
| Graph が返したもの | **50,688** |
| 復号後 | **16,928** |

`protected: yes` で、復号すると縮みます。**app-only・証明書・`Sites.Read.All` で、保護された
ファイルは保護されたまま取れます。**

### 陽性対照には弱点があります

**ミクはこのファイルの所有者です** (`to owner = yes`、権利に `OWNER` が入っています)。
**所有者は常にフル権利を持つ**ので、ミクのレグが開いたのが「ラベルがミクに権利を与えたから」なのか
「ミクが所有者だから」なのかは**分かれていません**。

機構が動くことの証明としては足りていますが、**ラベルの権利付与そのものを測ったことにはなりません** ―
[environment.md](environment.md) の「この権利が実際に効くかどうか」は**未測定のままです**。

### 変数は 1 つずつ動かしました

**#33 は `Identity` と `DelegatedUserEmail` を一緒に動かしていました。** 答えは出ましたが、
**どちらが効いたのかは分かりません**。#34 で `Identity` をミクに、#35 でリンに固定し、
**動かす変数を 1 つに減らして初めて言えるようになりました。**

**#33 の未設定レグは、何も測っていませんでした。** `Identity` も空になったため `BadInputException`
(エンジンが作れない) で止まり、**保護サービスまで届いていません**。#34 / #35 では `Identity` が
有効なまま `DelegatedUserEmail` だけが空になり、**サービスまで届いた上で拒否**されました。
**同じ「未設定」でも、測れているものが違います。**

これは所見 5 の追試 2 と所見 6 で 2 回やった間違いと同じ形です ― **変数を 1 つしか動かさずに全体に
ついて語ること**。今回は書く前に気づけました。

### 確認していないこと

- **他のラベル・他のファイル・他の利用者。** 測ったのは **1 ファイル・1 ラベル・2 利用者・6 セル**です
  (**追試 1 で 2 ファイルに増えました**。ラベルは 1 つのままです)
- **`Content.DelegatedReader` を外したらどうなるか。** 「このロールがあるから開いた」は**測って
  いません** ― ロールを外した対照を取っていないので、ロールと結果の対応は所見 2 の追試 (トークンに
  載ったこと) までです
- ~~**所有者ではない、権利を与えられた利用者。** これが取れると上の弱点が消えます~~
  **取れました** (追試 1)。**弱点は解けています**
- **存在しないアドレスを `DelegatedUserEmail` に入れたら何が返るか**
- **SDK がどの URL を叩いたか。** 分かるのは SDK が**要求したトークン**の `authority` と `resource`
  (`https://login.windows.net/<テナント>` と `https://aadrm.com`) までで、**その先の呼び出しは
  見えていません**。MIP の経路は他の 4 つと違い、URL がレポートに出ません ― それは測れていない、
  ということです
- **委任 (人がサインインする経路) との比較。** `consume` は app-only だけです
- **1 回ずつの観測**です (ただし (miku, miku) と (rin, rin) は 2 回ずつ出ています)

### 追試 1 ― 所有者が別人のファイルを 1 枚足したら、権利の出どころが 2 つに割れた

上の弱点 (**ミクが所有者でもあった**) を潰すために、**ねじれた 1 枚**を作りました。

| ファイル | 保護した人 = 所有者 | ラベルの付与先 |
| --- | --- | --- |
| `probe-miku-only.docx` | ミク | ミク | ← **同一人物**（所見 9 の弱点） |
| **`probe-rin-protected.docx`** | **リン** | **ミク** | ← **別人** |

**リンに `probe-miku-only` を適用させた**ものです ― **リン自身は付与先ではありません**。2 枚を
**同じ実行・同じレグ・同じ瞬間**に測りました (2026-08-05、実行 #39〜#42、app-only・証明書)。

| ファイル | 名指し | 所有者か | 付与先か | 結果 | 発行先 | 権利 |
| --- | --- | --- | --- | --- | --- | --- |
| `probe-miku-only` | miku | **はい** | **はい** | 開く | miku | **`OWNER` + `VIEW, VIEWRIGHTSDATA, DOCEDIT, EDIT, PRINT, EXTRACT, ...`** |
| `probe-miku-only` | rin | いいえ | いいえ | 拒否 | - | - |
| **`probe-rin-protected`** | **miku** | **いいえ** | **はい** | **開く** | **miku** | **`VIEW, VIEWRIGHTSDATA, DOCEDIT, EDIT, PRINT, EXTRACT, ...`**（`OWNER` なし） |
| **`probe-rin-protected`** | **rin** | **はい** | **いいえ** | **開く** | **rin** | **`OWNER` のみ** |
| どちらも | (未設定) | - | - | 拒否 | - | - |

> **所有権は `OWNER` を、ラベルの付与はそれ以外を出しています。両方持つ者には足し算で返ります。**

#### 弱点は解けました

- **ラベルの権利付与は、所有者でない相手にも効きます。** ミクは**自分が作っていないファイル**を開き、
  **テンプレートの権利ちょうど**を受け取りました。所見 9 の「開けたのは所有者だったからでは」という
  別解釈が消えます
- **所有権だけでも開けます。** リンはテンプレートの付与先ではないのに開き、**`OWNER` 1 つだけ**を
  受け取りました
- **`OWNER` は束ではなく 1 つの権利として返ります。** 他の権利名に展開されません
- **未設定は、所有者のファイルであっても拒否されます。** 所見 9 の結論はここでも動きません
  (**この付与の下では** ― 所見 11 の限定を参照)

#### 2 枚の違いは「誰が保護したか」だけです

これは推測ではありません。**同じ実行の中で、2 枚のテンプレート ID とラベル ID が一致しています。**

```
| file                     | type          | template id                          | label id                             |
| probe-miku-only.docx     | TemplateBased | 4186576a-25e3-4788-94c7-d30e9ca2e9e8 | 17b60be9-895e-4eb1-ab09-3a13da4ede79 |
| probe-rin-protected.docx | TemplateBased | 4186576a-25e3-4788-94c7-d30e9ca2e9e8 | 17b60be9-895e-4eb1-ab09-3a13da4ede79 |
```

**ラベルが同じであることを測ってから、権利の差を語れるようになりました。** これができるまでは
「同じラベルを付けたはず」という操作者の記憶が根拠でした。

#### 付随して分かったこと

- **リンは自分に権利の無いラベルを適用できました。** 適用者が所有者になる、というのはこれで実測です
- **`named in the policy` は 3 行とも `(none listed)`。** **所有者であるリンにも、ポリシーが他に誰を
  名指ししているかは返っていません。** ライセンスに何の権利があるかは分かっても、ポリシーの中身は
  別です
- **保護されたまま届く件が 2 枚目でも再現。** 51,200 バイト → 復号 17,024 バイト
- **4 回の実行 (#39〜#42) が 1 セルも違いません。** `Identity` をミクに固定した回とリンに固定した回で
  同じ ― **所見 9 の「`Identity` は認可に効かない」が、2 枚目のファイルでも成り立ちます**

#### この追試で確認していないこと

- **権利の「足し算」がどう行われているのか。** 見えているのは返ってきた一覧だけで、
  **どちらが先に評価されるのか、衝突したらどうなるのかは測っていません**
- **`OWNER` を持つ相手に、テンプレートが権利を制限できるのか。** リンは付与先ではなかったので、
  「付与先だが `VIEW` だけ」の所有者は測れていません
- **他のラベル。** 依然として **1 ラベルだけ**です
- **ポリシーの一覧が返る条件。** `(none listed)` が「この呼び出し元には返さない」なのか「そもそも
  この形では載らない」なのかは分かれていません

---

## 所見 10 ― テンプレート以外の形でも通る。値の形も要件ではない。ただしポリシーの中身は所有者にしか見えない

### 予測

立てていません。**保護の形と、名指しの形を、それぞれ別に動かして測りました。**

### 観測

3 枚を**同じ実行**に並べました (2026-08-05、実行 #43 / #44、app-only・証明書、`Identity` はミクに固定)。

| ファイル | 保護の作り方 | 所有者 | 権利を与えられた人 |
| --- | --- | --- | --- |
| `probe-miku-only.docx` | テンプレート | ミク | ミク |
| `probe-rin-protected.docx` | 同じテンプレート | リン | ミク |
| **`probe-userdefined.docx`** | **適用者が相手を選ぶラベル** | **リン** | **ルカ**（`luka-mail@` を指定） |

**ルカは UPN とメールのローカル部が違う利用者**です ― UPN `luka@`、メール `luka-mail@`。

| ファイル | 名指し | 結果 | 発行先 | 権利 |
| --- | --- | --- | --- | --- |
| `probe-userdefined` | **`luka-mail@`** | **開く** | `luka-mail@` | `DOCEDIT, EDIT, EXTRACT, PRINT, VIEW` |
| `probe-userdefined` | **`luka@`** (UPN) | **開く** | **`luka-mail@`** | 同じ |
| `probe-userdefined` | `miku@` | `NoPermissionsException` | - | - |
| `probe-userdefined` | `rin@` (所有者) | 開く | `rin@` | **`OWNER`** |
| `probe-userdefined` | (未設定) | `NoPermissionsException` | - | - |

### 1. テンプレート以外の形でも成立します

```
| file                   | type          | template id                          | label id                             |
| probe-miku-only.docx   | TemplateBased | 4186576a-25e3-4788-94c7-d30e9ca2e9e8 | 17b60be9-895e-4eb1-ab09-3a13da4ede79 |
| probe-userdefined.docx | Custom        | 8e264004-56b2-4b80-8c4b-c454820fd8f4 | 40d17158-3d5b-4876-8a22-296391aa2c8a |
```

**`Custom` として判定され、復号も通ります**（19,042 バイト）。テンプレート方式の 2 枚が同じ実行の中で
`TemplateBased` を返しているので、**この列が保護の作り方に対応して動いている**ことも同時に見えています。

**`Custom` にもテンプレート ID があります。** テンプレート方式のものとは別の GUID です。
**「その場限りのテンプレートが作られている」というのは読みであって、測ったのは「`Custom` と名乗り、
ID を持っている」までです。**

**所有権と付与は `Custom` でも別々に効きます。** リンは `OWNER` だけ、ルカは与えられたぶんちょうど ―
所見 9 の追試 1 で見た構造が、テンプレート以外でも成り立ちました。

### 2. 値の形は必須要件ではありません

**ポリシーに保存されているのは `luka-mail@` のほうです**（下記 3 でリンの行に出ています）。それでも ―

> **UPN (`luka@`) で名指ししても開き、ライセンスは `luka-mail@` に発行されます。**

権利も復号バイト数も 1 文字違いません。**サービスは名指しを人物に解決し、答えは正規化した形で返します。**

### 3. ポリシーの中身は、`Custom` の所有者にしか見えません

**これは測ろうとしていなかったものです。**

```
| file                   | DelegatedUserEmail | named in the policy                                          |
| probe-userdefined.docx | luka-mail@...      | (none listed)                                                |
| probe-userdefined.docx | luka@...           | (none listed)                                                |
| probe-userdefined.docx | rin@...            | luka-mail@... -> DOCEDIT EDIT EXTRACT PRINT VIEW; rin@... ... |
```

**同じファイル、同じ実行、同じ瞬間。違うのは名指しした相手だけです。**

- **ルカ**（権利を与えられた利用者）→ **空のリスト**
- **リン**（所有者）→ **ポリシーの中身がそのまま返る**

**所見 3・4 と同じ形です** ― 空のコレクションは「無い」ではなく「見せてもらえない」。あちらは
Graph の権限一覧で、こちらは RMS のポリシー。**別の層で同じ形が出ています。**

**危ないところでした。** #43 では所有者のレグを入れておらず、`(none listed)` が 3 行並んでいました。
あのまま読んでいれば「**`Custom` でもポリシーの一覧は返らない**」と書いたはずで、**それは所見 6 と
同じ間違い**です ― **1 種類の呼び出し元しか測っていないのに、API の性質として語ること**。
#44 で所有者を足したから割れました。

> **【2026-08-05 の訂正】この節の見出しは狭すぎました。正しくは
> 「`Custom` の所有者にしか見えない」です。**
>
> **所有者かどうかだけでは決まりません。** リンは 2 枚のファイルで所有者ですが、
> **`TemplateBased` では `(none listed)`、`Custom` では返ります。** 実行 #53 の 1 実行の中に
> 6 セルすべてが入っていました ― **同じ表の上に反例があったのに、突き合わせていませんでした。**
>
> | ファイル | `type` | 名指し | 立場 | `VIEWRIGHTSDATA` | `named in the policy` |
> | --- | --- | --- | --- | --- | --- |
> | `probe-miku-only` | `TemplateBased` | miku | **所有者かつ付与先** | **あり** | **`(none listed)`** |
> | `probe-rin-protected` | `TemplateBased` | miku | 付与先 (非所有者) | **あり** | **`(none listed)`** |
> | `probe-rin-protected` | `TemplateBased` | **rin** | **所有者** (非付与先) | ― | **`(none listed)`** |
> | `probe-userdefined` | **`Custom`** | luka-mail | 付与先 (非所有者) | なし | `(none listed)` |
> | `probe-userdefined` | **`Custom`** | luka | 同上 (UPN 形式) | なし | `(none listed)` |
> | `probe-userdefined` | **`Custom`** | **rin** | **所有者** (非付与先) | ― | **返る** |
>
> **保護の形が変数です。そしてそれだけでもありません** ― `Custom` でも、付与先には返っていません。
>
> **原因は #44 で 1 ファイルの 3 行しか突き合わせなかったこと**です。所見 6 で学んだ「対象を 1 つに
> 絞ると穴が開く」と同じ形が、**今度は 1 つのファイルに絞ったこと**で起きました。**測定は揃って
> いて、読み方だけが足りませんでした。**

#### `VIEWRIGHTSDATA` を持っていても返りません

**ミクは `probe-rin-protected` で `VIEW, VIEWRIGHTSDATA, DOCEDIT, ...` を持っています。** それでも
`(none listed)` です。

**`VIEWRIGHTSDATA` は名前のとおりなら「その内容にどんな権利が設定されているかを見る」権利**です
(対になるのが `EDITRIGHTSDATA`)。**これは権限名の語彙についての説明で、測定ではありません。**

> **名前がそのものずばりの権利を持っている相手に、権利の一覧が返っていません。**

読みは 2 つあり、**どちらも測っていません** ―

- `VIEWRIGHTSDATA` は**別の面** (Office の「アクセス許可の表示」など) を支配していて、
  **SDK の `ProtectionDescriptor.UserRights` はそれとは別の規則で埋まる**
- そもそも**この列は権利ではなく、所有権と保護の形で決まっている**

#### 切り分けるための 1 セルは、この道具立てでは取れません

分けるには **`Custom` かつ `VIEWRIGHTSDATA` を持つ付与先**が要ります。**取れません。**

| 道 | なぜ取れないか |
| --- | --- |
| Word のアクセス許可ダイアログ | **権利の細目を選ぶ口がありません。** 閲覧者 / 制限付きエディター / エディター / オーナーの 4 つで、「その他のオプション」も有効期限・追加許可の要求先・プログラムからのアクセス・接続の要求の 4 項目だけです |
| PowerShell でテンプレートを作る | **`TemplateBased` になります。** それは既に測ってあり、`(none listed)` でした |
| MIP SDK で保護を**適用**する | 技術的には可能です (`ProtectionDescriptorBuilder` で権利を任意に指定できます)。**ただしこのツールが「保護を掛ける側」になります** ― `consume` は「開けるか」を測る道具として作ってあり、範囲の拡張には README の線を引き直す必要があります。**今回はやらないと決めました** |

**「未測定」ではなく「この道具立てでは取れない」と書いています。** 前者は「まだやっていない」に読め
ますが、**後者はなぜ取れないかが書いてある**分だけ後の人の役に立ちます ― `mip` サブコマンドが
「見つからなかったライブラリの名前を出す」ようにしたのと同じ理由です。

#### 「無関係な人」の行は原理的に取れません

この列は**開いたレグにしか出ません**。プローブは `CreateFileHandlerAsync` が成功して初めて
descriptor を読むので、**`NoPermissions` で拒否されたレグには descriptor そのものが存在しません。**

**測定の穴ではなく、測れないことの形です。** 上の表を 2×3 で描くと 3 列目が空欄になり、
**「返らなかった」と誤読されます** ― だから 2×2 で書いて、この段落を付けています。

### 4. ドメインを間違えると、別のテナントの答えが返ります

**#43 の入力に打ち間違いがありました** ― `miku@nagiyu3.onmicrosoft`（`.com` が無い）。返ってきたのは
`NoPermissionsException` ではなく ―

> **`ServiceDisabledException` ― `RMS service is disabled for this tenant.`**

**`@` の右側でテナントが選ばれています。** 知らないドメインを渡すと、**そのドメインのテナントに
ついての答え**が返り、「そんな利用者はいない」とは言われません。

**これは読み違えやすい形です。** 本物の「このテナントでは RMS が無効」と、**こちらの打ち間違い**が
同じ例外で返ります。#44 で `.com` を足したら同じ行が `NoPermissionsException` になったので、
**原因は入力側だと確定しています。**

### これが意味すること

**名指しの受け口は、思ったより寛容です。** 形式は問われず、UPN でもメールでも同じ人物に解決されます。
**厳しいのは中身のほう** ― 名指しした相手が権利を持っているかどうかだけが答えを変えます (所見 9)。

**そして、返ってくる情報量は呼び出し元によって変わります。** 開けたかどうかは全員に返りますが、
**ポリシーが他に誰を名指ししているかは所有者だけ**です。

### 確認していないこと

- **`Custom` のテンプレート ID が何なのか。** ファイルごとに変わるのか、ラベルごとなのか ―
  **1 ファイルしか測っていません**
- **所有者以外で一覧が返る条件。** `VIEWRIGHTSDATA` を持つ相手ならどうかは測っていません
  (ルカの権利にそれは入っていませんでした)
- **他のドメイン形。** 測ったのは「実在するドメイン」と「打ち間違えた 1 つ」だけです。
  **他テナントの実在ドメインを渡したらどうなるかは測っていません**
- **表示名やエイリアスでの名指し。** 測ったのは UPN とメールの 2 つです
- **1 回ずつの観測**です (テンプレート側の 2 枚は #39〜#44 で 6 回一致しています)

---

## 所見 11 ― `Content.SuperUser` は Entra の付与だけで効く。効くのは「誰も名乗らないとき」だけ

### 予測

立てていません。**付与を 2 段に分け、あいだで測りました。**

`Content.SuperUser` を有効にする手順は、独立した 2 つからなります。

| | やること | どこ |
| --- | --- | --- |
| **A** | `Content.SuperUser` をアプリに付与 → 管理者同意 | Entra |
| **B** | `Enable-AipServiceSuperUserFeature` + `Add-AipServiceSuperUser` | PowerShell |

**まとめて入れると、変わったときにどちらのせいか分かりません。** 所見 9 の #33 でやった間違いと同じ形なので、
**A → 測る → B → 測る → 戻す → 測る**の順にしました (2026-08-05、実行 #45〜#53)。

### 観測

3 枚 × 5 レグ (名指し 4 + 未設定 1)、`auth` と `consume` を各状態で 1 本ずつ。

| 状態 | run | RMS のロール | **未設定 3 レグ** | **名指し 12 レグ** |
| --- | --- | --- | --- | --- |
| **前** | #45 / #46 | `Content.DelegatedReader` | **拒否** | 基準 |
| **A** (Entra のみ) | #48 / #49 | **`Content.SuperUser Content.DelegatedReader`** | **開く** | **変化なし** |
| **A+B** (+PowerShell) | #50 / #51 | 同じ | 開く | 変化なし |
| **戻し** | #52 / #53 | `Content.DelegatedReader` | **拒否** | 変化なし |

### 1. A だけで完全に発現します

**Entra で付与して管理者同意した時点で、未設定レグは 3 枚とも開きました。**

**このとき `Get-AipServiceSuperUserFeature` は `Disabled` のまま**でした ― B で初めて `Enabled` にしています。
つまり**機能フラグが無効の状態で、スーパーユーザーとしての復号が通っています**。

### 2. B は、測れる範囲では何も足しませんでした

`Enable-AipServiceSuperUserFeature` と `Add-AipServiceSuperUser` を実行したあと (#50 / #51)、
**`auth` のトークンも `consume` の 15 レグも 1 文字も変わりません。**

**A と B をまとめて入れていたら「スーパーユーザーには PowerShell の登録が必要」と書いていたはずです。**
それは間違いでした。

**ただし範囲は狭く見ます。** 測ったのは **3 ファイル × 5 レグ・1 テナント**で、**B が別の場面 (eDiscovery
など他のツール) で効くかどうかは測っていません**。言えるのは「**この測定では差が出なかった**」までです。

### 3. 効くのは「誰も名乗らないとき」だけ

**名指しした 12 レグは、4 状態すべてで 1 セルも変わりません。**

- リンを名乗れば、`Content.SuperUser` があってもリンの権利しか出ません
- ミクが権利を持たないファイルは、`Content.SuperUser` があってもミクには開きません

**変わったのは未設定レグだけ**で、そこでの発行先は ―

```
opened; licence issued to <クライアント ID>@<テナント ID>, rights OWNER
```

**アプリ自身に、`OWNER` で発行されています。** `named in the policy` も所有者と同じく**返ります**。

> **`Content.SuperUser` は「代理として名乗る」経路には触れていません。**
> **アプリが自分自身として立つときの権限**であって、**誰かの代わりに立つときの制約を緩めるものではない**、
> というのがこの 15 レグ × 4 状態の形です。

**`Get-AipServiceSuperUser` が返す文字列と、ライセンスの発行先は同じ主体**でした ― 登録簿に載る名前と、
実際に licence が出た先が一致しています。

### 所見 9 を限定します

所見 9 と追試 1 に **「未設定はフリーパスではない。名指しが無ければ拒否される」** と書きました。
**あれは `Content.SuperUser` が無いときの話です。**

**同じ入力・同じファイル・同じコードで、テナント側の付与 1 つだけが変わって、答えが変わりました。**
所見 9 の測定は正しく、**そこから「名指しの無い呼び出しは常に拒否される」と読むのが広すぎた**、
ということです ― 所見 5 の追試 2 や所見 6 と同じ形が、これで 3 度目になります。

**ただし所見 9 の本体 (`DelegatedUserEmail` が単独で認可を決め、`Identity` は読まれない) は動きません。**
名指しした 12 レグが 4 状態で不変であることが、それを支えています。

### 外すと戻ります

**前 (#45/#46) と戻し (#52/#53) が一致しました。** ロールは 1 つに戻り、未設定 3 レグは拒否に戻ります。

**片道の観測ではありません。** 所見 7 で「誰も触っていないのに変わる」を見ている以上、
**戻したことも測らなければ言えません** ― 戻したつもりで戻っていない可能性は、測るまで残ります。

### 確認していないこと

- **B が何のためにあるのか。** この測定では差が出ませんでしたが、**差が出る場面があるかどうかは測って
  いません**
- **`Content.SuperUser` だけを持つアプリ** (`Content.DelegatedReader` なし) がどうなるか。
  今回は常に両方ありました
- **未設定レグで開いた中身が、本当に全ファイルに及ぶか。** 測ったのは**このサイトの 3 枚**だけです
- **委任 (人がサインインする経路) との比較**
- **1 回ずつの観測**です (ただし前と戻しの一致が、実質的な 2 回目になっています)

---

## 所見 12 ― 所有者は、拒否される前に、通信もトークンもなしで読める。Graph の「作った人」は別人

### 予測

立てていません。**問いは 3 つで、3 つ目は「取れない」が答えでもよいものでした。**

1. Graph の `createdBy` と RMS の所有者は一致するか
2. `NoPermissionsError.Owner` は常に入っているか
3. **復号を試みる前に**所有者は取れないか

3 番目が要るのは、拒否の中身から所有者を読んで名指しし直す ―
つまり**「試す → 断られる → 断られ方から学ぶ → やり直す」**という手当てを考えるなら、
**その 1 往復が必要なのかどうかを先に測っておかないと決められない**からです。

### 観測

**4 つの経路を、同じ 3 ファイル・同じ実行に並べました** (2026-08-05、実行 #54)。
左から順に、**必要なものが増えます**。

| file | Graph `createdBy` | publishing licence | issued licence | refusal said |
| --- | --- | --- | --- | --- |
| `probe-miku-only.docx` | `nagiyu@...` | **`miku@...`** | `miku@...` | `miku@...` |
| `probe-rin-protected.docx` | `nagiyu@...` | **`rin@...`** | `rin@...` | `rin@...` |
| `probe-userdefined.docx` | `nagiyu@...` | **`rin@...`** | `rin@...` | `rin@...` |

| 経路 | 要るもの |
| --- | --- |
| Graph `createdBy` | Graph トークン + サイトへの呼び出し |
| **publishing licence** | **ファイルのバイト列だけ** ― トークンなし、サービス呼び出しなし |
| issued licence | RMS トークン + 使用ライセンスの取得に成功すること |
| refusal said | RMS トークン + **断られること** |

`publishing licence` は `FileHandler.GetSerializedPublishingLicense` で発行証明書を取り出し、
`PublishingLicenseInfo.GetPublishingLicenseInfo` に読ませたものです。**エンジンも認証デリゲートも
要りません** ― `MipContext` だけで通ります。同じ経路の `FileHandler.GetFileStatus` が
「保護されているか」も返します。

### 1. `createdBy` は 3 枚とも所有者と食い違いました

**Graph が返すのは「アップロードした人」**です。3 枚とも操作者の名前で、
**保護した人 (ミク / リン) の名前は 1 枚も出ていません。**

わざと所有者と付与先を交差させた 1 枚だけの話ではありません。**3 枚すべてで外れました。**

これは驚くようなことではなく ― SharePoint はアイテムを誰が置いたかを記録し、RMS は
コンテンツを誰が保護したかを記録します。**別々のものを記録しているので、別々の答えが返ります。**
問題は、**両方とも「所有者」に見える名前で返ってくる**ことのほうです。

### 2. `NoPermissionsError.Owner` は、この実行では常に入っていました

**`(not carried)` は 1 度も出ていません。** 3 枚それぞれの拒否レグで、
`NoPermissionsException.Owner` が所有者を名指ししています。

**ただし範囲は狭く見ます。** 測ったのは **`NoPermissionsException` だけ**です。
所見 10 で出た `ServiceDisabledException` は別の型で、**`Owner` プロパティを持ちません** ―
「拒否には所有者が入っている」ではなく、「**`NoPermissions` の拒否には入っていた**」までです。

### 3. 取れます。しかも試す前に取れます

**3 つの RMS 側の経路が、3 枚とも同じ名前を返しました。** 発行証明書・発行済みライセンス・拒否の
中身が一致しています。

**そして一番左の RMS 経路 (`publishing licence`) は、トークンもサービス呼び出しも要りません。**

> **拒否から所有者を読む手当ては、1 往復ぶん余計です。**
> 所有者はファイルを手にした時点で分かっているので、**最初の試行の前に名指しできます。**

**「再試行が手段として有効か」という問いへの答えは、「有効だが、それを選ぶ理由がない」**です ―
拒否から得られる情報が、拒否の前に得られてしまっているので。

### この道具では何も変わりません

**この所見は `consume` の挙動を変えていません。** 測っただけです。
**そもそもこの道具は、名指しを入力として受け取ります** ― 誰を名乗るかは測定の変数であって、
道具が賢く決めるところではありません。所有者を自動で名指しし始めたら、
**「名指ししなかったときにどうなるか」が測れなくなります** (所見 9・11 はまさにそれを測ったものです)。

### 確認していないこと

- **保護した人とアップロードした人が同じファイル。** 3 枚とも操作者がアップロードしているので、
  **`createdBy` が一致する場合があるかどうかは測っていません**
- **発行証明書が読めないファイル。** 3 枚とも Office 形式で、3 枚とも読めました。
  **PFILE や、読めない形のものは測っていません**
- **`Owner` が空で返る拒否があるか。** 3 枚 3 通りの拒否では常に入っていました
- **`publishing licence` の所有者が、実際の認可に使われている値と同じ保証。** 3 枚で
  一致しましたが、**一致することが仕様なのかどうかは測れません**
- **1 回の観測**です (ただし 15 レグの側は #53 と完全に一致しています)

---

## 所見 13 ― 循環したグループは作れる。輪は畳まれ、自分自身が返る。そして `null` は「無い」ではない

> **【裏付け 2026-08-06】** `null` のほうは仕様どおりでした。Graph の
> [権限の概要](https://learn.microsoft.com/graph/permissions-overview) に明記されています ―
> **「アプリがコンテナ内の特定のオブジェクト型を読む権限を持たない場合、オブジェクト型と ID
> だけが返り、他のプロパティは `null` と示されることがあります」。**
>
> **循環のほうは、依然として出典がありません。** `transitiveMembers` が輪をどう扱うかについて、
> Microsoft の記述は見つかっていません。**測定だけがあります。**

### 予測

立てていません。**先に決着すべき問いが 1 つありました** ― 「そもそも Entra が循環を作らせるのか」。
**作らせなければ、それが答えでした。**

### 作れます

`probe-cycle-a` と `probe-cycle-b` という**セキュリティ グループを 2 つ**作り、
**互いを相手のメンバーに**しました。さらに `probe-cycle-b` にはミクを直接入れてあります。

```
probe-cycle-a  ──メンバー──▶  probe-cycle-b  ──メンバー──▶  probe-cycle-a
                                     │
                                     └──メンバー──▶  ミク
```

**ポータルは止めませんでした。** 警告も確認も出ず、「メンバーが正常に追加されました」と出て終わりです。
**輪を閉じる操作が、他の追加と区別されていません。**

### 観測

**Graph Explorer から、管理者アカウントの委任で** (2026-08-05)。同意したのは
`GroupMember.Read.All` と、あとから `User.Read.All`。

| # | 訊いたもの | 返ってきたもの |
| --- | --- | --- |
| 1 | `A/members` | `b` |
| 2 | **`A/transitiveMembers`** | **`b` / ミク / `a`** |
| 3 | **`B/transitiveMembers`** | **`b` / ミク / `a`** ― **2 と 1 バイトも違いません** |
| 4 | `A/transitiveMemberOf` | `b` / **`a`** |
| 5 | `ミク/transitiveMemberOf` | `b` / `a` / `probe-group` |
| 6 | `B/members` | ミク / `a` |

ポータルの「**すべてのメンバー**」も、A・B とも同じ 3 行 (`a` / `b` / ミク) を返しています。

### 1. 止まります

**無限に返ることも、エラーになることも、タイムアウトすることもありません。** 3 件返って終わりです。
`transitiveMembers` は**輪を検出できないと成立しない**ので、**どこかで畳んでいる**ことになります
(どう畳んでいるかは見えません)。

### 2. グループは自分自身を返します

**A の推移メンバーに `a` が入ります。** 上向き (`transitiveMemberOf`) にも `a` が入ります。
**下向きにも上向きにも、自分が自分の中にいます。**

輪を辿れば自分に戻るので、**「到達できるもの」を素直に集めた結果**と読めます。
ただし、**「グループが自分自身のメンバーである」という状態がここで作れてしまう**ことは、
グループを列挙して何かを判断する側から見れば別の話です。

### 3. 重複しません

ミクは A から見て**無限に多くの経路**で到達できます (`A→B→ミク`、`A→B→A→B→ミク`、…)。
**返るのは 1 回だけです。**

> **返っているのは「歩いた記録」ではなく「到達できる集合」です。**

### 4. 向きが消えます

**A と B の答えが完全に一致します。** 輪の中では、**どちらが親でどちらが子かという情報が残りません。**

直接メンバー (1 と 6) を見れば非対称です ― A は `b` の 1 つ、B は ミク と `a` の 2 つ。
**推移にした瞬間に、その非対称が消えます。**

**「このグループのメンバーは誰か」と「このグループは何に属しているか」が同じ答えを返す**、
という状態が成立しています。

### 5. 画面と Graph が一致しました

**これは所見 3・4・5・6 と逆向きです。** そこでは経路を変えると答えが変わりました。
ここでは**ポータルの「すべてのメンバー」と Graph の `transitiveMembers` が同じ 3 行**です。

**一致することも測定です** ― 一致しない例ばかり集めていると、それが常態に見えてきます。

---

### 測ろうとしていなかったもの ― `null` は「無い」ではありません

2 と 3 の応答で、**ミクの `displayName` だけが `null`** でした。同じ配列の中で、
グループ 2 つは名前が返っています。

`User.Read.All` に同意したあと、**まったく同じ URL** をもう一度実行しました。

| | ミクの `displayName` |
| --- | --- |
| `GroupMember.Read.All` のみ | **`null`** |
| **`User.Read.All` を追加** | **`ミク`** |

**同じ URL、同じオブジェクト、同じ配列位置。変わったのは同意だけです。**

> **`null` は「このユーザーに名前が無い」ではなく、「あなたには見せない」でした。**

**所見 3・4 と同じ形が、フィールド 1 つの粒度で出ています。** 空の一覧が「共有されていない」に見え、
`404` が「存在しない」に見えたのと同じで、**`null` は「値が無い」に見えます。**

**そして今回のほうが静かです。** 所見 3 では配列が空でした。ここでは**配列は正しく 3 件返り、
型も ID も正しく、フィールド 1 つだけが伏せられています。** `id` と `@odata.type` は返っているので、
**「そのオブジェクトが存在すること」は隠されていません。**

### この測定は、この道具では再現できません

**Graph Explorer から、人がサインインして測りました。** プローブのアプリ登録には
`GroupMember.Read.All` を**足していません。**

足せば測れます。**足さなかったのは、トークンのロールが変わって他の所見の基準が動くから**です ―
所見 11 で `Content.SuperUser` を足したときに `auth` の出力が変わったのと同じことが起きます。
**この問いは「Graph が輪をどう扱うか」であって「このアプリが届くか」ではない**ので、
**基準を動かさずに済ませました。**

**再現するには Graph Explorer が要ります。** それはこのリポジトリの他の所見と性質が違います。

### 確認していないこと

- **3 つ以上の輪** (`a → b → c → a`)。測ったのは 2 つ組だけです
- **輪の中に人が複数いる場合。** ミク 1 人だけです
- **app-only で同じ答えが返るか。** 測ったのは委任だけです
- **輪が他の面でどう扱われるか** ― SharePoint の権限、ライセンスの割り当て、条件付きアクセス。
  **測ったのは Graph のグループ API だけ**です
- **ページングとの関係。** メンバーが 3 つしかないので `@odata.nextLink` は出ていません
- **`null` が他のプロパティ・他の型でも同じか。** 測ったのは **`displayName` × ユーザー 1 件**です
- **同意を取り消すと `null` に戻るか。** **戻す測定をしていません** ― 所見 11 では戻しまで測ったので、
  ここは弱いところです
- **1 回の観測**です

---

## 所見 14 ― SharePoint が読めないファイルについて、Graph は「保護されていない」と答える

### 予測

立てていません。**「保護されているかどうかを、ファイルを落とさずに一覧で取れるか」を調べていて、
測ろうとしていなかったものが出ました。**

### 観測

同じ 4 ファイルを 4 つの面に訊きました (2026-08-06、管理者の委任で Graph PowerShell から)。
**うち 3 枚は、所見 9〜12 で暗号化されていることを測ってあります** ― ライセンスが発行され、
所有者が読め、権利が返り、名指しの無い呼び出しは拒否されました。

| 訊いた先 | `probe-miku-only` | `probe-rin-protected` | `probe-userdefined` | `test.docx` |
| --- | --- | --- | --- | --- |
| **Graph `$select=sensitivityLabel`** | **`protectionEnabled: false`** | **`false`** | **`false`** | `false` |
| **Graph `extractSensitivityLabels`** | **`415`** | **`415`** | **`415`** | **`200` `{"labels":[]}`** |
| SP REST の `MetaInfo` | `MSIP_Label_17b60be9-…` | **同じ GUID** | `MSIP_Label_40d17158-…`, `ContentBits: 8` | **`MSIP_Label_*` なし** |
| SP REST の `_IpLabelId` 他 | 空 | 空 | 空 | 空 |
| MIP SDK (所見 12) | **暗号化されている** | **暗号化されている** | **暗号化されている** | ― |

### 1. 真偽値で「いいえ」と言われました

**暗号化されていることを測ってあるファイルについて、Graph は `protectionEnabled: false` を返します。**

空でも `null` でもありません。**明言された「いいえ」です。**

### 2. ラベルの無いファイルと区別が付きません

`test.docx` にはラベルを付けていません。**返る値は 3 枚とまったく同じ `false`** です。

> **この列だけを読む道具は、暗号化された文書を「保護されていない」の列に並べます。**

### 3. 同じ Graph の別の面は、区別を保ちました

`extractSensitivityLabels` は **`415`** と **`200` `{"labels":[]}`** に割れます。

```
"code": "notSupported"
"message": "Failed to extract sensitivity labels as an unsupported user
            is attached to the assigned sensitivity label."
"innerError": { "code": "fileDecryptionNotSupported",
                "innerError": { "code": "unsupportedUser" } }
```

**エラーのほうが答えになっています** ― 「読めない」が「暗号化されている」の陽性のしるしです。

**同じテナント、同じファイル、同じ Graph。面が違うだけで、片方は区別を潰し、片方は保ちます。**

### 4. SharePoint は知っています

`MetaInfo` (`FieldValuesAsText` 経由) には**ラベルの GUID と名前がはっきり載っています**。しかも
**その GUID は所見 10・所見 9 の追試 1 で MIP SDK 経由に見た値と完全に一致**します ―
`17b60be9-895e-4eb1-ab09-3a13da4ede79` と `40d17158-3d5b-4876-8a22-296391aa2c8a`。

**まったく別の 2 経路が同じ ID を返しています。**

**にもかかわらず、`_IpLabelId` / `_EffectiveIpLabelId` / `_DisplayName` は全部空**でした。
ファイルの中の情報が、**SharePoint の列に昇格していません。**

**テナントの `EnableAIPIntegration` は `True`** です (`Get-SPOTenant` で確認)。**設定が無効だから、
ではありません。**

### 5. 理由コードは文書化されていますが、ステータスは違いました

`fileDecryptionNotSupported` は Microsoft のドキュメントに載っている 3 つのコードの 1 つです。
**ただし文書は `423 Locked` と書いていて、返ってきたのは `415 Unsupported Media Type`** でした。
そして **2 段目の `unsupportedUser` は、文書化された一覧に入っていません。**

### 断られ方の 6 つ目

| | 素朴な読み |
| --- | --- |
| `200` + 空のリスト | 共有されていない (所見 3) |
| `404` | 存在しない (所見 4) |
| `200` + `null` | 名前が無い (所見 13) |
| **`protectionEnabled: false`** | **保護されていない** |

**下に行くほど強く断言しています。** 空のリストは何も主張していません。`null` は「値が無い」と
言っています。**真偽値は「いいえ」と言い切ります** ― そして**型の上では、それが正常な値です。**

### 道具への反映

**`inventory` の保護列は 2 段にします。**

1. `$select=sensitivityLabel` ― **ページ 1 回、一覧に同乗、追加コストなし**
2. **空だったものだけ** `extractSensitivityLabels` ― 1 ファイル 1 回。`200 []` なら「ラベル無し」、
   `415` なら「読めない = 保護されている」

**1 段目が `false` でも、そのまま書きません。** 2 段目で `200 []` を確認して初めて
「保護されていない」と書けます。

### 確認していないこと

- **なぜ昇格が走っていないのか。** 候補は文書化されています ― アップロードした利用者に使用権が
  無い / Document ID が有効 / ラベル発行直後のレプリケーション / 有効期限つきラベル / DKE /
  チェックアウト中。**1 つも切り分けていません**
- **`unsupportedUser` の意味。** メッセージは「**ラベルに付いているユーザー**」と読めますが、
  「呼び出した人」とも読めます。**別人がアップロードした場合との比較を取っていません**
- **`sensitivityLabel` ファセットは文書化されていません。** v1.0 にも beta にも `driveItem` の
  プロパティ表に載っていません。実在は測りましたが、**契約はありません**
- **このファセットが正しく返る例を、まだ見ていません。** このテナントでは 7 件すべて空か `false`
  なので、**「昇格が走ったときに正しい値が返る」ことは測れていません**
- ~~**app-only では測っていません。**~~ **測りました** (実行 #68、`inventory`)。
  **app-only の証明書でも同じ形です** ― `extractSensitivityLabels` は保護 3 枚に
  `415` / `notSupported` / `fileDecryptionNotSupported` / `unsupportedUser`、`test.docx` に
  `200`。`sensitivityLabel` も 6 ファイルすべてで `protectionEnabled: false`。
  **呼び出し元を変えても答えは動きませんでした**
- **1 回の観測**です

---

## 所見 15 ― 「制限付きアクセス」は共有先の一覧に並ぶ。しかし、そのファイルは開けない

### 予測

立てていません。**共有先を一覧に出す道具を作っていて、その出力を読んでいて気付きました。**

### 観測

実行 #71 で、ミクは **5 つのアイテムに共有先として並びました**。

| アイテム | 役割 | 固有の権限か |
| --- | --- | --- |
| `b-direct.docx` | **閲覧** | **yes (固有)** |
| `test.docx` | **制限付きアクセス** | no (継承) |
| `probe-miku-only.docx` | **制限付きアクセス** | no (継承) |
| `probe-rin-protected.docx` | **制限付きアクセス** | no (継承) |
| `probe-userdefined.docx` | **制限付きアクセス** | no (継承) |

**ミクが実際に開けるのは 1 枚だけ**です。残りの 4 枚は、リストのレベルに付いた `制限付きアクセス`
を継承しているだけで、**そこに `ViewListItems` はありません**。

SharePoint はアイテム 1 つに権限を付けると、**そこへ辿り着けるように親のリストへ `制限付きアクセス`
を足します**。`b-direct.docx` の 1 件の付与が、**ライブラリ内の全アイテムにミクの名前を撒いた**、
というのが起きていることです。

> **素直に数えると、その人の到達範囲を 5 倍に報告します。** しかも 5 行とも `200 OK` で返った
> 本物の役割割り当てで、**どこにも異常はありません。**

### 名前では判定できません

`制限付きアクセス` は表示名です。**このサイトは日本語なので、役割は `閲覧` / `編集` /
`制限付きアクセス` で返りました。**

> **英語の役割名で書いた除外規則は、1 件も除外せずに最大の到達範囲を報告し、健康そうに見えます。**

判定は `RoleDefinitionBindings` の **`BasePermissions.Low` の `ViewListItems` ビット (`0x1`)**
で行えます。ロケールに依存しない数値で、実行 #72 以降**このビットは実際に届いています**
(表に `(no view)` と出ているのがその証拠です)。

### 断られ方の 7 つ目

| | 素朴な読み |
| --- | --- |
| `200` + 空のリスト | 共有されていない (所見 3) |
| `404` | 存在しない (所見 4) |
| `200` + `null` | 名前が無い (所見 13) |
| `protectionEnabled: false` | 保護されていない (所見 14) |
| **`200` + 正しい役割割り当て** | **この人はこのファイルを開ける** |

**これまでの 4 つは「答えが欠けている」形でした。これは違います。** 返ってきた値は完全に正しく、
**読み手が意味を取り違えるだけ**です。**欠けている答えより、正しい答えのほうが疑いにくい。**

### 確認していないこと

- **`制限付きアクセス` を単独で持つ人が、本当に何も開けないのか。** ミクとして実際に開こうとは
  していません。**測ったのは権限マスクだけ**です
- **他のロケールでの役割名。** 判定はマスクで行っているので影響しないはずですが、**確かめて
  いません**
- **`RoleTypeKind` を控えに使う経路が動くかどうか。** マスクが常に届いているので、**この分岐には
  一度も到達していません**

---

## 所見 16 ― 共有リンクの裏のグループ名と、Graph の対象範囲は、別の語彙で書かれている

### 予測

**立てました。そして外れました。** SharePoint は共有リンクの裏に
`SharingLinks.<アイテム>.<種別>.<リンク>` という名前のグループを作るので、**種別を名前から
読めば、1 ファイル 1 呼び出しを払わずに済む**と考えていました。

### 観測

実行 #74。同じサイトに 3 種類のリンクを作り、**グループ名**と **Graph の
`driveItem/permissions`** の両方を取りました。

| ファイル | SharePoint のグループ名の種別 | Graph の `link.scope` | `link.type` |
| --- | --- | --- | --- |
| `c link share.docx` | `OrganizationEdit` | `organization` | `edit` |
| `e-link-view.docx` | `OrganizationView` | `organization` | `view` |
| **`f-link-people.docx`** | **`Flexible`** | **`users`** | `view` |

**前の 2 つは素直に対応しています。3 つ目は対応しません。**

> **`Flexible` から `users` は導けません。** 対応表が要り、その対応表は文書化されていません。

しかも外れたのは**「特定のユーザー」のリンク** ― **名前が付く唯一の種別**で、到達範囲の報告を
誤ると最も困るものです。

### 一致していた 2 つも、保証ではありません

`OrganizationEdit` → `organization` は**このテナントのこの 2 本でそう見えた**というだけです。
`Flexible` が示すのは、**この命名がこちらの推測できる規則に従っていない**ということで、
一致して見えた 2 本も同じ規則の上にあるとは限りません。

### 到達先は、対象範囲によって形が変わります

| `scope` | 誰に届くか |
| --- | --- |
| `users` | **名前が返ります** ― `f` では `grantedToIdentitiesV2` にリン |
| `organization` | **列挙できません。**テナント内の全員が対象で、名簿はありません |
| `anonymous` | **未観測** ― このテナントは外部共有が無効で作れませんでした |

**`organization` のリンクの裏のグループは空です。** 空を「誰も開けない」と読むと**逆**になります
― 対象が母集団なので、列挙する相手がいないだけです。

### 道具への反映

**名前から読むのをやめ、`driveItem/permissions` を 1 ファイル 1 回呼びます。** 他の経路が避けて
いる形ですが、代わりが「文書化されていない名前を読む」だったので払っています。

### 確認していないこと

- **`Flexible` 以外の食い違い。** 3 本しか測っていません
- **`anonymous`。** 作れませんでした
- **`link.webUrl` と `permission.id` が、グループ名の中の GUID と対応するか。** 表には両方
  出していますが、**突き合わせていません**
- **1 回の観測**です

---

## 所見 17 ― 何も与えないリンクは、どこにも痕跡を残さない

### 予測

**リンク オブジェクトはできて、権限だけが空になる**と考えていました。**外れました。**

### 観測

実行 #74。`g-link-existing.docx` に **「既存のアクセス権を持つユーザー専用」** のリンクを作り
(操作者は [リンクのコピー] まで押しています)、3 つの経路に訊きました。

| 訊いた先 | 答え |
| --- | --- |
| Graph `driveItem/permissions` | **リンクなし** (同じ実行で他の 3 本は返っています) |
| SharePoint REST の `RoleAssignments` | **`SharingLinks.*` グループなし** |
| SharePoint の「アクセス許可を管理」画面 | **「このファイルの共有リンクはまだ作成されていません」** |

**3 つが一致しました。** 道具の見落としではありません。

このリンクは**ファイルの素の URL そのもの**で、何の権限も運びません。**運ばないので、記録される
ものがありません。**

### 同じ画面が、道具の側も裏付けました

その UI は `g-link-existing.docx` について **ユーザー・1 / グループ・3** と表示します。
道具が出したのは `ミク` と `probe 所有者` / `probe メンバー` / `probe 閲覧者` ― **一致します。**
道具はそこに **`Limited Access System Group` 2 種**を足して出しており、**UI はそれを隠しています。**

> **この 1 点に関しては、道具のほうが UI より多く見えています。**

### 監査の側では、小さくない話です

**「このファイルの URL を配った」という行為が、どの API からも見えません。**

権限を増やさないので**露出は増えません**。増えないことと、**起きたことが記録されないこと**は
別の問題です。

### 確認していないこと

- **本当に何も作られていないのか、作られたものが 3 経路とも見えないだけなのか。** 区別できて
  いません。**外から見える範囲では同じ**です
- **監査ログ。** `Unified Audit Log` は一度も見ていません。**そちらには残っている可能性が
  あります**
- **`existingAccess` という `scope` が実際に返る例。** 道具にはその分岐がありますが、**一度も
  到達していません**
- **1 回の観測**です

---

## 所見 18 ― ラベルが列に上がるかどうかは、置いた人がそのラベルの権利を持っているかで決まる

> **【2026-08-16 の精密化】** この見出しは**正しいが粗い**ことが分かりました。**所見 22** を
> 見てください ― 決めているのは「そのラベルの**静的な付与**を持っているか」ではなく
> **「その文書に対して実効の権利を持っているか」**で、実効の権利には**入口が 2 つ**あります。
> **静的な付与**と、**自分で適用したこと (発行者としての `OWNER`)** です。所見 18 が測った 5 本は
> **2 つが常に一致していた**ので、割れ方からは区別できませんでした。
>
> **【2026-08-17 の追加】** 昇格を止めるのは**権利だけではありません**。**所見 23** ―
> **パスワード保護をかけたファイルは、権利のある人が置いても昇格せず、パスワードを外して
> 置き直しても戻りません。** 所見 18 の条件 (置いた人の権利) は**直らない**条件でしたが、
> **「外せる条件なら直る」とは限らない**ということです。

### 予測

**依頼側が立てました。** 4 レグの設計書に、レグ 4 の「埋まったら」欄として
**「(埋まらなくなれば) 上げた人の権利が効く」**と書かれていました。**当たりました。**

### 観測

実行 #79 (2026-08-07)。**同じラベル `17b60be9-895e-4eb1-ab09-3a13da4ede79` を持つ 5 本を、
1 回の呼び出し列で読みました。** 昇格はバックグラウンドのジョブなので、5 本を 5 回に分けて測ると
「実行の間にジョブが走ったか」がすべての比較に混ざります。1 回なら、行の違いは**作り方の違いだけ**です。

| レグ | どう置かれたか | 置いた人 | **ラベルの権利** | 暗号化 | 経過 | **昇格** |
| --- | --- | --- | --- | --- | --- | --- |
| 1. 現状 | アップロード | nagiyu | **なし** | あり | 54 時間 | **なし** |
| 2. 作り直し | 落として上げ直し | nagiyu | **なし** | あり | 3 時間 | **なし** |
| 3. 新規 | その場で作成 | **ミク** | **あり** | **なし** | 30 分 | **あり** |
| 4. 別人が上げる | アップロード | リン | **なし** | あり | 30 分 | **なし** |
| 5. 権利者が上げる | アップロード | **ミク** | **あり** | あり | 数分 | **あり** |

ラベルの権利付与先は**ミク 1 人**です (`rightsdefinitions` に 1 件、同日 `Get-Label` で確認)。

> **5 本すべてが「置いた人が権利を持っているか」で割れます。他のどの列でも割れません。**

### 時間を置いても動きません

同じ 5 本を **5 つの時点**で読みました。`j-other-uploader.docx` (リンが置いた、権利なし) を基準に:

| 実行 | 経過 | `j` の昇格 |
| --- | --- | --- |
| #76 | 6 分 | なし |
| #77 | 17 分 | なし |
| #78 | 27 分 | なし |
| #79 | 31 分 | なし |
| **#81** | **1 時間 24 分** | **なし** |

**#81 は #79 と 1 文字も違いません。** 昇格は「まだ走っていない」のではなく、**走りません。**

### レグ 4 と 5 は、変数が 1 つしか違いません

**同じバイト列です。** レグ 4 のファイルをコピーしてリネームしただけで、Word では開いていません。
同じライブラリに、数分違いで置かれました。

| | `j-other-uploader.docx` | `k-miku-uploaded.docx` |
| --- | --- | --- |
| 中身 | **同一** | **同一** |
| ラベル | `17b60be9-…` | **同じ** |
| 暗号化 | あり | **あり** |
| **置いた人** | **リン (権利なし)** | **ミク (権利あり)** |
| **昇格** | **なし** | **あり** |

**これが直接の証拠です。**

### 消えた候補

| 候補 | どう消えたか |
| --- | --- |
| **古いファイルだけ** | レグ 4 は **30 分前**のファイルで、54 時間前のレグ 1 と同じ振る舞い |
| **時間を置けば走る** | #76 (6 分) → #77 (17 分) → #78 (27 分) → #79 (30 分) で 1 マスも動かず |
| **暗号化されていること** | **レグ 5 は暗号化されていて昇格しました** |
| **アップロードという行為** | レグ 5 はアップロードされていて昇格しました |
| **ドキュメント ID** | ライブラリに `_dlc_DocId` 列が無く、**サービスは無効** (実行 #75) |
| **`EnableAIPIntegration`** | `True` (所見 14) |
| **ラベルの有効期限 / DKE** | `contentexpiredondateindaysornever: Never`、`doublekeyencryptionurl` のキーは存在せず `protectiontype: template` |

### 所見 14 が、まるごと説明されます

**所見 14 の 3 枚は、すべて `nagiyu` がアップロードしたもの**です (`createdBy` が 3 枚とも同じ、
所見 12)。そして **`nagiyu` はこのラベルの権利付与先ではありません。**

所見 14 の「確認していないこと」に、**「このファセットが正しく返る例を、まだ見ていない」**と
書いてありました。**見ました** ― レグ 3 と 5 で `protectionEnabled: true` と `id` が返り、
`extractSensitivityLabels` が `200` とラベルを返し、`_IpLabelId` に GUID が入りました。
**壊れていたのはテナントではなく、置いた人でした。**

### 列は、値が無いと存在ごと消えます

`_IpLabelId` は、**昇格した 2 本には届き、昇格しなかった 3 本には届きません。** 空文字ではなく、
**キーそのものがフィールド袋に入っていません。**

**「列が空」と「列が無い」を分けようとしていたのに、この経路では同じでした。** 全ファイルに共通して
届くラベル系のキーは `_DisplayName` 1 つだけで、`_IpLabelId` は共通キーに入りません。

### 追試 ― MIP SDK から見ると、3 本とも同じ

実行 #82 (`consume`)。**昇格したものとしなかったものを、保護サービス側から開きました。**
`MipIdentityEmail` はミクに固定し、**動くのは `DelegatedUserEmail` だけ**にしてあります (所見 9)。

| ファイル | 昇格 | `DelegatedUserEmail` = ミク | = リン | 未設定 |
| --- | --- | --- | --- | --- |
| `probe-miku-only.docx` | **なし** | **開けた**。ライセンスはミクへ、`OWNER/VIEW/…` | 拒否 | 拒否 |
| `i-new.docx` | **あり** | **開けた**。同上 | 拒否 | 拒否 |
| `j-other-uploader.docx` | **なし** | **開けた**。同上 | 拒否 | 拒否 |

**3 本とも同じです。** 保護サービスから見て、昇格したファイルと昇格しなかったファイルに**違いは
ありません**。

> **差は SharePoint 側にしかありません。** 文書の中身でも、ラベルでも、保護サービスの判断でも
> ありません。

所見 9 (「`DelegatedUserEmail` が単独で認可を決める」) も、**別の 3 ファイルで再現**しました。

**`k-miku-uploaded.docx` も同じでした** (実行 #85) ― ミクは開け、リンは拒否、未設定は拒否。

**ただし同じ実行が、この所見の前提を 1 つ崩しました。**

| | ダウンロード サイズ |
| --- | --- |
| `j-other-uploader.docx` (昇格せず) | **55,296 バイト** |
| `k-miku-uploaded.docx` (昇格した) | **43,520 バイト** |
| `i-new.docx` (昇格した) | **43,520 バイト** |

**`k` は `j` ではなく `i-new` と同じ大きさで返ってきます。** 「`j` をコピーしてリネームしただけ」なら、
配られる姿も同じはずでした。

### 追試 3 ― 昇格は、置かれたファイルを書き換える

上の食い違いを、実行 #86 が解きました。**`j` と `k` の保管された姿を読みました。**

| | 保管サイズ | `extractSensitivityLabels` | ダウンロード サイズ | 昇格 |
| --- | --- | --- | --- | --- |
| `j-other-uploader.docx` | **55,296** (上げたまま) | **`415`** ― 復号できない | 55,296 | **なし** |
| `k-miku-uploaded.docx` | **20,056** | **`200`** ― 復号されている | 43,520 | **あり** |
| `i-new.docx` (参考) | **20,056** | `200` | 43,520 | あり |

**`k` の保管サイズは `i-new` と同じ数字**で、上げたときの 55,296 ではありません。

> **昇格したファイルは、置かれたバイト列のまま残りません。** SharePoint が復号して**保管し直し**、
> 配るときに暗号化し直します (追試 2)。昇格しなかった `j` は、上げたままの 55,296 で手つかずです。

**これで所見 18 の対は保たれます** ― レグ 4 と 5 は置いた時点では同じバイト列で、いま違うのは
**昇格の結果**であって原因ではありません。

**書き換えは、最終更新に現れません。** `k` の `modified` は `2026-08-07T06:07:59Z` ― アップロードの
時刻のままです。**中身は置き換わったのに、更新の記録は動いていません。** 書き換えが一瞬で終わって
同じ時刻になったのか、時刻に触れていないのかは、**分けていません。**

### 追試 2 ― SharePoint は復号して保管し、暗号化して配る

同じ実行が、思っていなかったものを測りました。

| | `promotion` が見た保管サイズ | `consume` が落としたサイズ |
| --- | --- | --- |
| `i-new.docx` | **20,056 バイト** (`ContentBits`: 暗号化なし) | **43,520 バイト** |
| `j-other-uploader.docx` | 55,296 バイト (暗号化あり) | **55,296 バイト** |

**`i-new.docx` は、保管されている姿と配られる姿が違います。** ライブラリ上では復号されて置かれ
(だから `extractSensitivityLabels` が `200` を返した)、**ダウンロードすると暗号化されて出てきます**
(だから MIP SDK が開けて、リンには拒否された)。

**上げられたファイルは変わりません** ― `j` は 55,296 バイトのまま。

これが「その場で作ったものは暗号化されていない」の中身です。**暗号化されていないのは保管時だけ**で、
**保護が無いわけではありません。** ここを取り違えると「ライブラリ上のファイルは平文だから読める」と
いう危険な読みになります ― **なりません。配布経路が暗号化します。**

### 断られ方の 8 つ目

| | 素朴な読み |
| --- | --- |
| `200` + 空のリスト | 共有されていない (所見 3) |
| `404` | 存在しない (所見 4) |
| `200` + `null` | 名前が無い (所見 13) |
| `protectionEnabled: false` | 保護されていない (所見 14) |
| `200` + 正しい役割割り当て | この人は開ける (所見 15) |
| **キーが応答に無い** | **その列は存在しない** |

**実際には「この文書については値が無い」です。** 同じ呼び出しが、同じライブラリの別のファイルに
ついては同じキーを返します。**1 件だけ見ていると、区別する材料がありません。**

### 道具がこの所見に至るまでに 4 回間違えました

記録しておきます。**どれも、間違えた実装がまったく健康そうに見えていました。**

1. **区切り文字**。4 本のパスが 1 本になり `404`。全角の `｜` で書いたのはこちらです
2. **列を読まずに「一致しない」と判定** (#76)。比較の相手を一度も取得していないのに `no` を出した
3. **動いていた呼び出しを消した** (#77)。`$expand=FieldValuesAsText` が効いていないと推論して外し、
   `MetaInfo` を失って**4 本とも「ラベルを持っていない」**と書いた
4. **根拠を書かない判定** (#78)。「a column names it」とだけ出し、**どの列が何を持っているか**を
   出していなかった

**2 と 3 は、この道具が 17 の所見で警告してきた形そのもの**です ― 読めていないものを「無い」と書く。

### 確認していないこと

- **仕組みは測っていません。** 「昇格のために SharePoint が文書を復号する必要があり、それを置いた人の
  権利で行う」という説明は筋が通りますが、**測ったのは相関だけ**です。**追試 1 が「差は SharePoint 側に
  しかない」ところまでは狭めました**が、そこから先は測っていません
- ~~**`j` と `k` が置かれた時点で同じバイト列だったか。**~~ **追試 3 で解けました** ― 違いは昇格の結果です
- **書き換えがいつ起きたか。** `k` の `modified` はアップロードの時刻のままで、**書き換えの時刻は
  どこにも出ていません**
- **書き換えられた中身が元と同じか。** サイズと `extractSensitivityLabels` の答えしか見ておらず、
  **中身を突き合わせてはいません**
- **権利の種類。** ミクは `VIEW,DOCEDIT,EDIT,PRINT,EXTRACT,…` を全部持っています。
  ~~**どの権利が要るのかは分けていません**~~ **狭まりました** (所見 22) ― 昇格した
  `m-rin-applied.docx` で、置いた本人が持っていたのは **`OWNER` だけ**でした。
  **`OWNER` で足ります。** ほかの権利が単独で足りるかどうかは、まだ分けていません
- ~~**後から走るかどうか。**~~ **測りました** (実行 #81)。**1 時間 24 分後も 1 マスも動きません。**
  ただし測ったのは**1 時間 24 分まで**で、それ以上は見ていません
- ~~**所有者の扱い。**~~ **分けました** (所見 22)。`disableautomaticownerrights: false` のとおり、
  **保護した本人は付与先でなくても `OWNER` を得て、その文書を開けます**
- **1 つのラベル、1 つのライブラリ**での測定です

---

## 所見 19 ― `Prefer: hierarchicalsharing` は、どの権限でも通り、どの権限でも何も足さない

### 予測

**立てていません。** 依頼側が明示的に外しました ―「弱い権限でも通る」も「強い権限が要る」も
答え、と。**公式のドキュメントはこのヘッダーの要求権限を書いていません。**

### 観測

実行 #87・#88・#89 (2026-08-07 / 2026-08-16)。同じサイトの既定のドキュメント ライブラリに対し、
**トークン無しの `delta`** を、**ヘッダーの有無**で 2 本ずつ。アプリ登録は `capability-inventory`、
名乗りは証明書です。

| 実行 | Graph のロール | SharePoint のロール | 最初の状態 | `Preference-Applied` | items | 相異なるキー | ヘッダー側だけに出たキー |
| --- | --- | --- | --- | --- | --- | --- | --- |
| #87 | `Sites.Read.All` | `Sites.FullControl.All` | `200 OK` | **返らない** | 18 / 18 | 18 / 18 | **0** |
| #88 | `Sites.Read.All` | **`Sites.Read.All`** | `200 OK` | **返らない** | 18 / 18 | 18 / 18 | **0** |
| #89 | **`+ Sites.FullControl.All`** | `Sites.FullControl.All` | `200 OK` | **返らない** | 18 / 18 | 18 / 18 | **0** |

各行の「items / キー」は**ヘッダー無し / ヘッダー有り**です。**3 実行 6 レグ、すべて同じ 18 件・
同じ 18 キー**でした。

```
@odata.type  cTag  createdBy  createdDateTime  eTag  file  fileSystemInfo  folder  id
isAuthoritative  lastModifiedBy  lastModifiedDateTime  name  parentReference  root
shared  size  webUrl
```

> **権限を上げて何か変わるか ― 変わりませんでした。**

### 「拒否されなかった」は「受理された」ではありません

`200 OK` が返り、本文も返り、エラー コードも出ません。**それでもヘッダーが効いた証拠には
なりません。** `Prefer` は**知らない設定は黙って無視する**と定められた仕様のヘッダーで、
受け取った側が応じたときにだけ `Preference-Applied` で名乗り返します。

**その名乗りが返っていません。** つまり観測されたのは、仕様どおりの**「受け取って、無視した」**の
形そのものです。この道具は `200 OK` を「受理」と書きません ― レポートは
`the service said nothing` と書きます。

**ヘッダーが送られたことは確かめてあります。** 「叩いた呼び出し」表の `sent` 列に
`Prefer: hierarchicalsharing` が出ており、その 1 本だけに出ています。

### 共有情報は、ヘッダーの有無に関係なく最初から付いています

見たかったのは「共有情報がどのアイテムに付いて返るか」でした。**返っているのは `shared` だけ**で、
それは**ヘッダー無しの既定でも 18 件の側に出ています。**「階層のルートと、明示的に変更のあった
アイテムだけ」という形は**現れませんでした。**

### この道具は、名前を探さずに引き算しました

「共有情報が増えたか」を測るのに、`sharedWith` のような**それらしい名前を探しに行くと、
名前を知っているものしか見つかりません。** この実装は 2 レグのキー集合をまるごと集めて
**引き算**します。**ヘッダーが何を足すかを知らなくても、足されたものは差として出ます。**

差は 0 でした。**探し方が悪くて見つからなかったのではなく、差が無い**と言えるのは、この形だからです。

### SharePoint の権限は、この経路の上にありません

**#88 の差し替えは、この問いには答えていません。** 3 本の呼び出しはすべて `graph.microsoft.com`
宛で、**SharePoint トークンは 1 度も使われていません。** #88 から言えるのは
「**共有情報が返るかどうかを SharePoint のアプリケーション権限は左右しない ― その権限は
この経路上に無い**」であって、「`Sites.Read.All` で足りた」ではありません。

**答えたのは #89 です。** Graph 側をテナント全体のフル コントロールまで上げても、1 マスも
動きませんでした。

### 所見 8 とは別の断られ方です

`$expand=permissions` は `501 notSupported` で塞がっています (所見 8)。**こちらは `200` で通り、
そのうえで何も足しません。** 前者は「その機能はここに無い」、後者は「その要求は聞き流された」で、
**どちらも権限の話ではありません。** 引きずられないように分けて記録します。

### 断られ方の 9 つ目

| | 素朴な読み |
| --- | --- |
| `200` + 空のリスト | 共有されていない (所見 3) |
| `404` | 存在しない (所見 4) |
| `200` + `null` | 名前が無い (所見 13) |
| `protectionEnabled: false` | 保護されていない (所見 14) |
| `200` + 正しい役割割り当て | この人は開ける (所見 15) |
| キーが応答に無い | その列は存在しない (所見 18) |
| **`200` + ヘッダーを付けても応答が 1 バイトも変わらない** | **ヘッダーが受理された** |

**実際には「無視された」です。** そして `Preference-Applied` が返らないこと以外に、
**受理と黙殺を分ける材料が応答の中にありません。**

### 確認していないこと

- **要求権限は決まっていません。** 測ったのは「この 3 通りの付与ではどれも同じ」までで、
  **ヘッダーが効く付与があるのかどうかは分かりません**
- **委任では測っていません。** 3 実行とも app-only です
- **この経路だけ**です。`/drives/{id}/root/delta` と `/me/drive/root/delta`、`sites/{id}/lists/{id}/items/delta`
  では試していません
- **トークン付きの `delta` では測っていません。** 3 実行ともトークン無しの完全列挙で、
  ヘッダーが「変更のあったアイテムだけ」に関わるなら、**変更を伴う 2 回目こそが本番**でありえます
- **18 件・1 ページ・フォルダー 1 段**のライブラリでの測定です。「階層のルート」に意味が出るのは
  **もっと深い階層**かもしれません
- **`Preference-Applied` を返す `Prefer` を、この経路で 1 つも確かめていません。**
  つまり「この経路はそもそも `Preference-Applied` を返さない」可能性を潰していません ―
  **対照になる既知のヘッダーを 1 本通せば分かります**
- **綴りは 1 通りしか試していません** (`hierarchicalsharing`)

---

## 所見 20 ― この経路は `Prefer` を読んでいる。そして、読んだことを一度も言わない

> **依頼への答えは追試 3 にあります。** 本文は「このヘッダーが受理されたかどうかを
> どうやって知るか」で、追試 3 が「では何が返るのか」です。

### 予測

**立てていません。** 所見 19 が自分で挙げた穴を潰しにいっただけです ―
**「この経路が `Preference-Applied` をそもそも返すのかを確かめていない」。** 返さないなら、
所見 19 の沈黙は「無視された」の証拠になりません。

### 観測

実行 #90・#91 (2026-08-16)。同じ呼び出しを **8 レグ**、1 回の実行の中で。ヘッダー無し、
測る `hierarchicalsharing`、**対照 6 本**。

| leg | 何のために | ステータス | `Preference-Applied` | pages | items | keys |
| --- | --- | --- | --- | --- | --- | --- |
| なし | 基準線 | `200 OK` | ― | 1 | 18 | 18 |
| `hierarchicalsharing` | 測るもの | `200 OK` | **無し** | 1 | 18 | 18 |
| `odata.maxpagesize=5` | 効いたらページ数が動く | `200 OK` | **無し** | **1** | 18 | 18 |
| `deltashowremovedasdeleted` | この経路について文書化 | `200 OK` | **無し** | 1 | 18 | 18 |
| `deltashowsharingchanges` | 同上・いちばん近い隣 | `200 OK` | **無し** | 1 | 18 | 18 |
| **`deltatraversepermissiongaps`** | 同上 | **`400 Bad Request`** | **無し** | ― | 0 | 0 |
| `include-unknown-enum-members` | Graph 全体について文書化 | `200 OK` | **無し** | 1 | 18 | 18 |
| **`no-such-preference-e3f1`** | **この実行のために作った名前** | `200 OK` | **無し** | 1 | 18 | 18 |

### 読んでいます

**1 本が拒否されました。** ヘッダーを捨てている経路は、中身に文句を言えません。

```
{"error":{"code":"invalidRequest","message":"Must also prefer deltashowremovedasdeleted when preferring deltatraversepermissiongaps"}}
```

> **この 1 文は、パーサーが 2 つの名前と、その 2 つの関係を知っていることを示します。**

### そして、送った全部について黙っています

**`Preference-Applied` は 1 本も返りません。** 文書化されているものも、Graph 全体のものも、
**自分が名指しで拒否したものも**、そして **追試で受理されたことが確かめられたものも**です。

**所見 19 の沈黙は、証拠になりません。** 「無視された」と読んではいけないと書いたのは正しく、
ここでその理由が付きました ― **この経路は、適用したかどうかを言う口を持っていません。**

### `hierarchicalsharing` は、でっち上げた名前と区別が付きません

| | ステータス | echo | pages | items | keys |
| --- | --- | --- | --- | --- | --- |
| `hierarchicalsharing` | `200 OK` | 無し | 1 | 18 | 18 |
| **`no-such-preference-e3f1`** | `200 OK` | 無し | 1 | 18 | 18 |

**1 マスも違いません。**

> **これは「このヘッダーは実装されていない」ではありません。** 測ったのは
> **「この経路・このアプリからは、両者を分ける材料が 1 つも返ってこない」**までです。
> 認識されていて、このライブラリではやることが無かった、でも同じ絵になります。

### echo を信じない側の証拠も、同じことを言いました

`odata.maxpagesize=5` は **18 件を 1 ページで返しました。** 効いていれば 4 ページです。
**ページ数はヘッダーが隠せる値ではない**ので、これは echo とは独立に「適用されていない」を
意味します ― **この経路は、名前を読んだうえで、ほとんどの要求を黙って捨てます。**

### 断られ方の 10 個目

| | 素朴な読み |
| --- | --- |
| `200` + 空のリスト | 共有されていない (所見 3) |
| `404` | 存在しない (所見 4) |
| `200` + `null` | 名前が無い (所見 13) |
| `protectionEnabled: false` | 保護されていない (所見 14) |
| `200` + 正しい役割割り当て | この人は開ける (所見 15) |
| キーが応答に無い | その列は存在しない (所見 18) |
| `200` + 応答が 1 バイトも変わらない | ヘッダーが受理された (所見 19) |
| **`Preference-Applied` が返らない** | **サービスがそのヘッダーを知らない** |

**実際には「このサービスは、知っているヘッダーについても何も言わない」です。**
**沈黙が一様なとき、沈黙は何の情報も運びません。**

### 道具がまた 1 つ間違えました

**#90 は、読むために用意した 1 文を列幅で切りました。**

```
{"error":{"code":"invalidRequest","message":"Must also prefer deltashowremo...
```

**続きを記憶から書けば 5 回目**です (所見 18 に 4 回分あります)。書かずに道具を直しました ―
`ProbeReport` に `Quotes` を足し、**拒否の本文は表の外に全文で**出します。#91 がその全文です。

> **「本文をそのまま引用する」と決めていても、出力側が切れば同じことです。**
> 方針は、それを運ぶ経路の全部に届いていないと方針になりません。

### 追試 ― サービスの言い値どおりに送っても、やはり何も言いません

実行 #92 (2026-08-16)。#91 が読ませてくれた条件を、そのまま送り返しました。

| leg | ステータス | `Preference-Applied` | pages | items | keys |
| --- | --- | --- | --- | --- | --- |
| `deltatraversepermissiongaps` **単独** | **`400 Bad Request`** | 無し | ― | 0 | 0 |
| **`deltashowremovedasdeleted, deltatraversepermissiongaps`** | **`200 OK`** | **無し** | 1 | 18 | 18 |

**`400` が `200` に変わりました。** サービスは自分で述べた規則を、実際に適用しています ―
**読んで、検証して、受理した**ということです。

> **それでも `Preference-Applied` は返りません。**

**「この経路は preference について何も言わない」は、これで推測ではなくなりました。**
受理されたことが**別の経路（ステータスの変化）から確かめられた** preference についてすら、
黙っています。

**効果の側は、この対照では何も言えません。** 組み合わせが返したのは基準線と同じ 18 件・18 キーで、
**削除されたアイテムを見せる preference に、見せるべき削除が無かった**だけかもしれません。
トークン無しの全件列挙には、そもそも「消えたもの」がありません ― **ここは測れていない**と
書いておきます。

### 追試 2 ― しおりを渡すと、1 本だけ別の顔になります

実行 #93・#94 (2026-08-16)。#93 でしおりを取り、**ライブラリを 1 つ動かしてから**、
そのしおりを渡して #94 を回しました。**同じ 8 レグ、同じ 1 回の実行**です。

| leg | しおり無し (#93) | **しおり有り (#94)** |
| --- | --- | --- |
| なし (基準線) | `200 OK` / 18 件 | `200 OK` / **2 件** |
| `hierarchicalsharing` | `200 OK` / 18 件 | `200 OK` / **2 件** |
| `odata.maxpagesize=5` | `200 OK` / 18 件 | `200 OK` / 2 件 |
| `deltashowremovedasdeleted` | `200 OK` / 18 件 | `200 OK` / 2 件 |
| **`deltashowsharingchanges`** | `200 OK` / 18 件 | **`410 Gone` `resyncRequired`** |
| `deltatraversepermissiongaps` | `400 invalidRequest` | `400 invalidRequest` |
| **`deltashowremovedasdeleted, deltatraversepermissiongaps`** | `200 OK` / 18 件 | **`410 Gone` `resyncRequired`** |
| `include-unknown-enum-members` | `200 OK` / 18 件 | `200 OK` / 2 件 |
| `no-such-preference-e3f1` | `200 OK` / 18 件 | `200 OK` / 2 件 |

**18 件が 2 件になりました。** しおりは効いていて、この経路は確かに「その時点から動いたもの」に
答えています。

```
{"error":{"code":"resyncRequired","message":"Resync required. Replace any local items with the server's version (including deletes) if you're sure that the service was up to date with your local changes when you last sync'd. Upload any local changes that the server doesn't know about."}}
```

### これが、いちばん効く 1 行です

**#90〜#93 では「どの preference も何もしなかった」ように見えていました。** そこからは
「この経路では preference が一切効かないのかもしれない」も否定できません。

**#94 で、1 本が動きました。**

> **`deltashowsharingchanges` は、しおりの続きとしては受け付けられません。**
> **同じ呼び出しが、しおり無しなら `200`、しおり有りなら `410` です。**

サービスは**この preference を、ストリームの中身を変えるものとして扱っています** ―
そういう preference を途中から足すことは許さず、**取り直しを要求します。**
`deltashowremovedasdeleted` との組み合わせでも同じ `410` になりました。

**そして `hierarchicalsharing` は、`410` を引き起こしません。**

| | しおり有りでの答え |
| --- | --- |
| **`deltashowsharingchanges`** (本物の共有関係の preference) | **`410` ― ストリームを変えるので取り直せ** |
| `hierarchicalsharing` | `200` / 2 件 ― 基準線と同じ |
| `no-such-preference-e3f1` (作った名前) | `200` / 2 件 ― 基準線と同じ |

**`hierarchicalsharing` は、いまも作った名前と同じ側にいます。** 変わったのは、
**反対側に本物の例ができた**ことです ― この経路で「効く preference はこう見える」が
1 件分かった以上、**`hierarchicalsharing` がそう見えていないことは、もう「材料が無い」では
ありません。**

**ただし `410` は、なお echo ではありません。** 8 本のうち 1 本も `Preference-Applied` を
返さないのは #94 でも同じで、**拒否したものについてすら黙る**という所見 20 の本文は動きません。

### 断られ方の 11 個目

| | 素朴な読み |
| --- | --- |
| `Preference-Applied` が返らない | サービスがそのヘッダーを知らない (所見 20) |
| **`410 Gone`** | **その資源はもう無い** |

**実際には「その続きからは答えられない ― 最初から取り直せ」です。** 資源は在ります。
**無いのは、しおりとその要求の組み合わせのほう**で、**同じ呼び出しがしおり無しなら通ります。**

### 追試 3 ― 共有情報は、ヘッダー無しで、変更のあったアイテムに付いて返ります

実行 #95・#96 (2026-08-16)。同じしおりのまま、**差分の中身を名前で出しました。**
動かしたのは 1 つ、`delta-scratch.docx` の追加で、**共有リンクは作成時に付いています**
(1 リンク・特定の人が編集可)。

**基準線 ― `Prefer` を 1 つも付けていないレグ:**

| item | kind | **`shared`** |
| --- | --- | --- |
| `root` | folder | **キーが無い** |
| `delta-scratch.docx` | file | **`{"scope":"users"}`** |

**そして `hierarchicalsharing` を付けたレグは、同じ 2 件・同じ 18 キー・追加ゼロです。**

### これが依頼への答えです

依頼の 4 つに、順番に。

| 訊かれたこと | 測ったこと |
| --- | --- |
| ヘッダーが受理されるか | **`200 OK`。ただし `Preference-Applied` は返らず、作った名前と区別が付きません** |
| **共有情報がどのアイテムに付いて返るか** | **変更のあったアイテムに付きます。階層のルートには付きません** |
| **「階層のルートと、明示的に変更のあったアイテムだけ」という形か** | **返るアイテムの集合はその形です。ただしヘッダーとは無関係で、既定でそうなります** |
| ヘッダーの有無で件数がどう変わるか | **変わりません** ― 2 件と 2 件、18 キーと 18 キー |
| 権限を上げて何か変わるか | **変わりませんでした** (所見 19、3 通りの付与) |

> **この道具立てで測れる範囲では、`hierarchicalsharing` は何も足しません。**
> **共有の変わったアイテムが目の前にあり、そのアイテムが現に共有情報を担いでいて、
> それでも 1 バイトも変わりません。**

**「材料が無かった」はもう使えません。** #90〜#93 では全件列挙だったので「見せるべきものが
無かっただけ」が残っていました ― **残っていません。**

### 返ってくる共有情報は、対象範囲だけです

`{"scope":"users"}` で終わりです。**リンクも、付与先も、リンクの種類も入っていません。**

**そして所見 16 がここでも効きます。** 画面では「リンクを知っている**個人**のユーザーが編集
できます」と出ているリンクが、Graph では **`users`** です ― **名前と `scope` は別の語彙**という
所見 16 の指摘が、別のリンク・別の経路で再現しました。

`delta` から共有の中身を知りたければ、**アイテムごとに `permissions` を引く**しかありません。
そしてその束取りは `501` で塞がっています (所見 8)。**この 2 つは、同じ壁の両側です。**

### 確認していないこと

- ~~**2 件が何だったのかを、この実行は名前で出していません。**~~ **出しました** (実行 #96)
- **`hierarchicalsharing` が実装されているかどうかは、なお分かりません。** `410` を
  引き起こさないことは「ストリームの中身を変えない」までで、**「何もしない」とは別**です
- **共有を「変更」した場合は測っていません。** 動かしたのは**追加**で、そのとき同時に
  リンクが付きました。**既にあるアイテムのリンクだけを後から足す / 外す**は別の変化です
- **`root` に `shared` が付く条件を測っていません。** このライブラリのルートには付きませんでした
  が、**付く構成があるかどうかは分かりません**
- ~~**サービスが求めた組み合わせを測っていません。**~~ **測りました** (実行 #92)。
  **`400` が `200` になり、それでも echo しません**
- **`hierarchicalsharing` が実装されているかどうかは分かりません。** 分かったのは、
  **この経路からは作った名前と区別が付かない**ことだけです
- ~~**トークン付きの `delta` では測っていません。**~~ **測りました** (実行 #93・#94)。
  **1 本だけ別の顔になりました** ― 追試 2
- **委任では測っていません。** 8 レグとも app-only です
- **`odata.maxpagesize` が「効かない」のか「この件数では見えない」のか**は分けていません。
  18 件・5 件指定で 1 ページなので効いていないと読めますが、**下限がある可能性は潰していません**
- **18 件・1 ページ・浅い階層**のライブラリ 1 つでの測定です

---

## 所見 21 ― アプリ単独の `/permissions` は、リストが持っている付与を落としていない

### 予測

**依頼側が明示的に外しました** ―「期待値は立てない。**『アプリ単独でも全部返った』も答え**」。

### 依頼の基準は取れませんでした

依頼書はこう組まれていました。

| レグ | 名乗り方 |
| --- | --- |
| 1 | アプリ単独 (証明書) |
| **2** | **そのアイテムの所有者本人の委任** ← **基準** |
| 3 | 所有者でない利用者の委任 |

**2 と 3 は取れません。** デバイス コードの委任サインインが
**`AADSTS530035` / `Access has been blocked by security defaults.`** で拒否されます ―
**所見 7 と同じ**で、**2026-08-16 に nagiyu とミクの 2 アカウントで測り直しても同じ**でした。
所見 7 の「元に戻るのかどうか」は、**戻っていない**が答えです。

**そこで基準を差し替えました。**

| | 依頼書の設計 | 実際に測ったもの |
| --- | --- | --- |
| 比べる相手 | 所有者の委任 | **SharePoint 自身の役割割り当て** |
| 根拠 | 公式が「所有者なら全部」と書いている | **実際に載っているものを読む** |
| 答えられる問い | アプリ単独は所有者相当か | **Graph が、リストの持っているものを落としていないか** |

**別の問いです。** そして**根拠としてはこちらのほうが強い** ― 公式の記述を信じる必要がありません。
ただし**依頼された問いではない**ので、レポートが毎回 1 行でそう言います。

### 観測

実行 #100 (2026-08-16)。3 ファイルを **1 回の実行**で、Graph と SharePoint の両方から読みました。

| | 種類 |
| --- | --- |
| `delta-scratch.docx` | **固有の権限**を持ち、共有先が複数 (`HasUniqueRoleAssignments: yes`) |
| `f-link-people.docx` | リンクの裏が **`Flexible`** ＝ Graph の **`users`**、**実際にリンが到達している** |
| `e-link-view.docx` | リンクが **`OrganizationView`**、裏のグループに**メンバー 0** |

**3 本とも同じ形でした。**

| 付与 | Graph | SharePoint | 側 |
| --- | --- | --- | --- |
| `probe 所有者` | あり | あり | **両方** (`login:probe 所有者` で一致) |
| `probe 閲覧者` | あり | あり | **両方** |
| `probe メンバー` | あり | あり | **両方** |
| **1 人の利用者** (`user` + `siteUser`) | **あり** | **なし** | **Graph だけ** |
| 共有リンク | **あり** (`link` エントリ) | あり (`SharingLinks.…` グループ) | 突き合わせ不能 |
| Limited Access のシステム グループ | なし | あり | 片方しかモデル化していない |

> **ディレクトリの主体を名指しした付与で、Graph に無いものは 0 件でした。**

**むしろ Graph のほうが 1 件多い**ところがあります ― 役割割り当てに対応する行が無い利用者を、
Graph は `user` + `siteUser` として返しています。

### 【2026-08-17 の追試】 0 件は「行が無い」ではなく「与える行が無い」

**別の 4 ファイル (所見 23 の材料) で同じ引き算をしたら、`0` ではありませんでした。**

```
across 4 file(s), 4 grant(s) naming a directory principal are in SharePoint...
```

**4 本とも同じ 1 行です** ― **ミク**、`user`、**SharePoint 側にだけ**。**この 4 本を置いた
本人**です。ここで**所見 21 が崩れたか**を決めるのは、**その付与が何であるか**です。

| 実行 | 見出しの数 | ミクの付与 |
| --- | --- | --- |
| #106 | `4 grant(s) ... in SharePoint and not in Graph` | **表に出ていない** |
| **#109** | 同じ 4 件 | **`制限付きアクセス (no view)`** ― 4 本とも |

> **所見 21 は崩れていません。** これは**所見 15** です ― 並ぶが、何も与えない。
> **実際に何かを与える付与で、Graph に無いものは 0 件のまま。**

**#100 の 3 本で `0` が出たのは、この 4 本と違って「行そのものが無かった」から**です。
**同じ `0` でも意味が 2 つある**ということで、**所見 21 の主張は後者 ―「与える付与は落ちていない」**
に絞られます。**前者は言えません。**

**ミクがこの 4 本の置いた本人でありながら、明示的な付与を 1 つも持っていない**ことも、
ここで見えています。彼女がファイルに届くのは **`probe メンバー` 経由**です。

### 道具が、また同じところで間違えました

**#106 の見出しの一行は、表を直したあとも「4 件足りない」と読めるままでした。**
実際、**私はそう読みかけました** ― #99 と同じ間違いです。

**原因は列幅です。** コンソールはこのセルを 78 文字で切ります。**決め手は文の後ろにあり、
切られた先にありました。**

> **「書いてある」と「届く」は別です。** 所見 20 の追試で拒否の本文が切れたときと同じ形の
> 欠けが、**今度は自分の書いた要約の側で起きていました。**

直し方は**数を並べ替えること**です ― **与える付与の数を先頭に置き**、`制限付きアクセス` の
数は括弧に回します。**切られても、先に立つ数は残ります。**

### リンクも落ちていません

`f-link-people.docx` について、Graph 側はこう返しました。

```
view link, scope users - roles read; names リン
```

**`users` スコープのリンクについて、Graph は名指しの相手を名前で返します。** 所見 16 が
「名前と `scope` は別の語彙」と測ったその経路で、**中身は落ちていない**ということです。

### 突き合わせは、名前ではなく鍵でしました

**所見 16 が効くのはここです。** 同じリンクが SharePoint では `…Flexible…`、Graph では `users`。
**名前で突き合わせれば、語彙の違いを差として報告します。**

両側が**候補の鍵を複数**出して、**集合が交われば同一**。実際に効いたのは
**`login:`**（Graph の `siteUser.loginName` / `siteGroup.loginName` ↔ SharePoint の
`Member.LoginName`）で、**どの鍵で一致したかを毎行に印字**します。

### 道具が、また 1 つ間違えました

**実行 #99 の見出しはこうでした。**

```
7 joinable grants are in SharePoint and not in Graph
```

**内訳は `SharingLinks.…` が 3 件と `Limited Access System Group*` が 4 件**でした。
**どちらも Graph が落とした付与ではありません** ― 前者は Graph が **`link` エントリ**として
返しており、後者は **SharePoint 自身の内部機構**で Graph はモデル化していません。

> **「突き合わせない」と宣言したものを、片側だけ欠損として数えていました。**

所見 18 に記録した 4 つと同じ家族です。**測定は正しく、そこから作った数字だけが間違っていました。**
直したうえで #100 を回し、**`0`** になりました。

### 断られ方の 12 個目

| | 素朴な読み |
| --- | --- |
| `410 Gone` | その資源はもう無い (所見 20) |
| **片方の API にしか無い行** | **もう片方が隠している** |

**実際には「2 つの API が別のものをモデル化している」ことがあります。** 共有リンクは
**両方が持っていて、識別子が噛み合わないだけ**。Limited Access のグループは
**片方にしか概念が無い**。**どちらも「隠された付与」ではありません。**

### 確認していないこと

- **「所有者相当か」は答えていません。** 所有者を 1 人も訊いていないので、**公式の言葉づかいでは
  答えられません**。これは道具の限界ではなく、**このテナントで委任が取れない**ことの帰結です
- **絞り込みが実際に起きることの確認 (依頼書のレグ 3) は、新しくは取れません。**
  **所見 3 が持っています** ― 委任の閲覧者に `200` + **0 件**。ただし**フローが塞がれる前**の
  測定で、撮り直せません
- **Graph 側にだけ出た 1 人が何なのかは分けていません。** アイテムの所有者かもしれませんが、
  **測っていません**
- **共有リンクの同一性は突き合わせていません。** 両側に同じ本数があることは、**同じリンクである
  ことの証明ではありません**
- **3 ファイル、1 ライブラリ、1 テナント**です。**外部共有は無効**のままなので、
  **`anonymous` のリンクは 1 本も含まれていません**
  (**追試で 4 ファイル追加**。合わせて 7 本、同じライブラリ)

---

## 所見 22 ― 発行者は、ラベルの権利設定と別に `OWNER` を得る。昇格はそれで足りる

### 予測

**依頼側が明示的に外しました** ―「期待値は立てない。**『適用しても昇格しない』も答え**」。

### 何を分けたかったのか

所見 18 は「置いた人がそのラベルの権利を持っているか」で 5 本が割れました。**ただし権利を
持っていたミクは、そのファイルにラベルを適用した人でもあります。**

> **静的な付与を持っているから**なのか、**適用した本人だから**なのかが分かれていませんでした。

**分け方は 1 つです** ― **ラベルの権利付与先ではない人 (リン) に、自分で適用させる。**

| レグ | 適用した人 | 置いた人 | 静的な付与 |
| --- | --- | --- | --- |
| 1 | ミク | リン | **なし** ← 所見 18 のレグ 4 (`j-other-uploader.docx`、既存) |
| **2** | **リン** | **リン** | **なし。ただし適用者本人** (`m-rin-applied.docx`) |

### 観測 ― 昇格 (実行 #101)

**4 本を 1 回の呼び出し列で読みました。** 昇格はバックグラウンドのジョブなので、実行を分けると
「その間にジョブが走ったか」が全部の比較に混ざります (所見 18 と同じ理由)。

| ファイル | 適用した人 | 置いた人 | 静的な付与 | **昇格** |
| --- | --- | --- | --- | --- |
| **`m-rin-applied.docx`** | **リン** | リン | **なし** | **あり** |
| `j-other-uploader.docx` | ミク | リン | なし | **なし** |
| `k-miku-uploaded.docx` | ミク | ミク | あり | あり |
| `i-new.docx` | ミク | ミク | あり | あり |

> **`3 promoted, 1 did not`**

**`m` と `j` は、置いた人も、ラベルも、置き方も同じです。適用した人だけが違います。**

### 観測 ― 発行者 (実行 #102)

| ファイル | Graph `createdBy` | **発行者 (publishing licence)** | 発行された licence |
| --- | --- | --- | --- |
| `m-rin-applied.docx` | リン | **リン** | リン |
| `j-other-uploader.docx` | リン | **ミク** | ミク |

**置いた人は両方リン。発行者だけが違います。** 所見 12 が「置いた人と保護した人は別でありうる」と
測ったものが、ここでは**意図して 1 変数に絞った対照**になっています。

### 観測 ― 復号 (同じ実行 #102)

| | リン指定 | ミク指定 | 未設定 |
| --- | --- | --- | --- |
| **`m-rin-applied.docx`** | **開いた** ― `rights OWNER` | 開いた ― `VIEW/VIEWRIG…` | 拒否 |
| `j-other-uploader.docx` | **拒否** | 開いた ― `OWNER/VIEW/V…` | 拒否 |

`…` はコンソールの列幅で切れた箇所です。**`m` / リンだけは切れていません** ―
`rights OWNER,` の直後がバイト数なので、**持っていた権利は `OWNER` 1 つ**です。

> **リンは `probe-miku-only` の権利付与先ではありません。**
> **それでも自分が適用したファイルは開け、ミクが適用したファイルは開けません。**

そして**ミクの権利がファイルによって違います** ― 自分が発行した `j` では `OWNER` 付き、
付与先として与っている `m` では `VIEW/VIEWRIGHTSDATA…`。**静的な付与と発行者権限が、
別々に効いているのが 1 つの表の中で見えています。**

未設定レグは両方とも拒否で、**`Content.SuperUser` を外したままであることの対照**になっています
(所見 11)。

### 答え

> **昇格の条件は「静的な付与」ではなく「実効の権利」です。**
> **そして実効の権利には入口が 2 つあります ― 静的な付与と、自分で適用したこと。**

**所見 18 は正しく、粗かった**ということです。あの 5 本では **2 つの入口が常に一致していた**ので、
割れ方からは区別できませんでした。**1 本足すだけで割れます。**

**そして `OWNER` だけで足ります。** リンが `m` について持っていたのはそれだけです。

### 断られ方の 13 個目

| | 素朴な読み |
| --- | --- |
| 片方の API にしか無い行 | もう片方が隠している (所見 21) |
| **同じ人が、同じラベルの 2 つのファイルで開けたり拒否されたりする** | **権限が不安定** |

**実際には「その人がどちらの文書の発行者か」で割れています。** ラベルは同じ、付与も同じ、
**文書ごとに違うのは誰が保護をかけたか**だけ。**ラベルの設定だけを見ていると、この差は
どこにも現れません。**

### 確認していないこと

- **`OWNER` 以外の権利が単独で足りるかどうか。** 昇格した 2 通り
  (`OWNER` だけのリン / 全権のミク) しか測っていません
- **発行者権限がいつまで残るのか。** 取り消せるのか、ラベルを付け替えたらどうなるのかは
  測っていません
- ~~**リンがこのラベルを適用できたこと自体**は、**発行スコープが広い**ことを意味します。
  権利付与先が 1 人であることと、**誰が適用できるか**は別の設定で、**後者は測っていません**~~
  **読みました** ― ラベル ポリシー `probe` が 5 ラベルすべてを **3 アカウント**に発行しています
  ([environment.md](environment.md))。**発行先は 3 人、権利付与先はミク 1 人。**
  **画面は 3 人を名前で出さない**ので、リンが含まれることを示しているのは**この所見の測定そのもの**です
- **1 ラベル・1 ライブラリ・1 テナント**です
- **昇格の時刻は分かりません。** #101 は置いた後の 1 回だけで、**いつ走ったかは
  どこにも出ていません** (所見 18 の「確認していないこと」と同じ)

---

## 所見 23 ― パスワード保護は、外して置き直しても昇格を戻さない。1 時間 22 分、3 回、値は 1 つも動かない

### 予測

**依頼側が明示的に外しました** ―「期待値は立てない。**『どれも恒久的』も答え**」。

### 何を分けたかったのか

所見 18 と 22 で「**置いた人が権利を持たないと昇格しない**」までは確定しています。**それは
直りません** ― 置いた人は後から変えられないからです。**残りの条件はどうなのか。**

> **解除すれば直るのか、置き直しても直らないのか。**
> **直るなら再試行で拾えます。直らないなら報告するしかありません。**

**測り方は「本来なら昇格する形」から始めて、条件を 1 つずつ足すことです。**

| レグ | 条件 | 解除できるか |
| --- | --- | --- |
| 1 | なし (基準) | ― |
| 2 | チェックアウトしたままにする | できる |
| **3** | **パスワード保護をかける** | **できる** |
| 4 | 12MB 超を別サイトへコピー/移動 | できない |

**このうち回したのはレグ 3 です** (依頼側の指定 ―「とりあえず A だけで」)。**4 本とも
ミクが置き、ミクが適用しています** ― 所見 22 の言う「実効の権利がある形」で、**割れる原因を
条件だけに絞ってあります。**

### 観測 ― 条件が付いている間 (実行 #103)

`p-password.docx` は **作成 → 秘密度 → パスワード**、`p-password-2.docx` は **作成 →
パスワード → 秘密度**。**順序を変えて 2 本**あるのは、順序が効くかどうかを同時に見るためです。

| ファイル | `vti_decryptskipreason` | `_IpLabelId` | **昇格** |
| --- | --- | --- | --- |
| `n-baseline.docx` | 0 | ラベル GUID | **あり** |
| `o-checkedout.docx` | 0 | ラベル GUID | **あり** |
| `p-password.docx` | **1** | 空 | **なし** |
| `p-password-2.docx` | **1** | 空 | **なし** |

**順序では割れませんでした。** 両方とも同じ値です。

### 観測 ― 解除した後。3 回、間を空けて

**解除は「パスワードを外して置き直す」**です (`00:27:39Z`、`publication` が `versionId 2.0`
に上がっています)。**外れたことは Word で確かめてあります** ― ダウンロードして開き、
**パスワードを訊かれませんでした**。

| 読み | 実行 | 解除からの経過 | `p-password` / `p-password-2` |
| --- | --- | --- | --- |
| 1 回目 | #104 | **+1 分** | `decryptskipreason: 1` / `_IpLabelId` 空 |
| 2 回目 | #105 | **+27 分** | 同じ |
| **3 回目** | **#107** | **+1 時間 22 分** | **同じ** |
| 4 回目 | #108 | **+2 時間 6 分** | 同じ |

4 回目は**間違って回った**ものです (`permissions` のつもりで `promotion` が回りました)。
**それでも 1 点は 1 点**なので、捨てずに載せています。**`vti_streamSyncTokenSequenceNumber` は
3 回目・4 回目とも `213`** ― **その間に新しい解析が走った形跡もありません。**

**3 回目 (`01:50:33Z`) の全部の面**です。**どの面も割れ方が一致しています。**

| ファイル | `decryptskipreason` | `iplabelpromotionversion` | `supportsZipIt` | `_IpLabelId` | `sensitivityLabel` | `extractSensitivityLabels` |
| --- | --- | --- | --- | --- | --- | --- |
| `n-baseline.docx` | **0** | **キーが無い** | true | GUID | `protectionEnabled: true` | `200 OK` |
| `o-checkedout.docx` | **0** | 2 | true | GUID | `protectionEnabled: true` | `200 OK` |
| `p-password.docx` | **1** | **0** | **false** | 空 | `protectionEnabled: false` | **`415`** |
| `p-password-2.docx` | **1** | **0** | **false** | 空 | `protectionEnabled: false` | **`415`** |

`415` の中身は `notSupported` / `fileDecryptionNotSupported` です ― **所見 14 で測った
「SharePoint が読めないファイルについて Graph は保護されていないと答える」がそのまま
再現しています。**

### 答え

> **レグ 3 は恒久的です。** **パスワードを外して置き直しても、昇格は戻りません。**
> **そして原因はファイルの側です** ― **別名の新しいアイテムに置いても、同じところで止まります。**

依頼文の「**時間を置いても動かないことを確かめてから『恒久的』と書きたい**」は、
**+1 分 / +27 分 / +1 時間 22 分の 3 点**で満たしました (**+2 時間 6 分**で 4 点目)。
**動いた面は 1 つもありません。**

**「恒久的」の中身は 2 通りありました。**

| | 何がそうさせているか | 直し方 |
| --- | --- | --- |
| **A** | **ファイルの側が、サービスから見て開けないまま** | 中身を作り直せば直る |
| B | 一度スキップと判定されたアイテムが判定を持ち続けている | ファイルを作り直しても直らない |

**分け方は 1 本足すだけです** ― **パスワードを外した中身を、別名の新しいファイルとして置く。**
昇格すればアイテム側の記憶 (B)、しなければファイル側 (A)。

### 切り分け ― A です

`p-password-removed.docx` / `p-password-removed-2.docx` を **03:38:48Z に新規アップロード**
しました (`versionId 1.0`、置いたのはミク、**バイト数は元と同じ 82432 / 71680**)。
**ラベルは付け直していません** ― 付け直せば「発行者が自分で適用した」ことになり (所見 22)、
変数が変わります。

| 読み | 実行 | 上げてから | 結果 |
| --- | --- | --- | --- |
| 1 回目 | #113 (03:44:51Z) | **+6 分** | `1 promoted, 3 did not` |
| **2 回目** | **#114 (04:20:56Z)** | **+42 分** | **同じ** |

| | 新しい 2 本 | `n-baseline` (同じ呼び出し列の対照) |
| --- | --- | --- |
| `vti_decryptskipreason` | **1** | 0 |
| `vti_iplabelpromotionversion` | **0** | キーが無い |
| `vti_supportsZipIt` | **false** | true |
| `_IpLabelId` | **空** | ラベル GUID |
| `sensitivityLabel` | `protectionEnabled: false` | `protectionEnabled: true` |
| `extractSensitivityLabels` | **`415`** | `200 OK` |

> **このアイテムには「前の判定」がありません。それでも同じ値です。**
> **B では説明が付きません。**

**解析が走らなかった線も消えています。**

```
vti_mediaservicemetadata: ... "generationTime":"2026-08-17T03:39:00.5655582Z" ...
                              "aipLabelExtractionStatus":2
```

**上げた 12 秒後**です。`vti_streamSyncTokenSequenceNumber` も **`141`** ― 元の 2 本の
`213` とは別の、このアイテム自身の番号です。

### ついでに、留保が 1 つ消えました

新しいアイテムの `MetaInfo` に、**元のファイルに付いた時刻と ActionId** が入っています。

```
MSIP_Label_..._SetDate:SW|2026-08-17T00:05:35Z
MSIP_Label_..._ActionId:SW|dd2dfd85-f548-4e3a-af16-b49ebe8ef3d2
```

**このアイテムは一度もその値を持ったことがありません。** 貼り付いたキャッシュ
(`vti_stickycachedpluggableparserprops`) ではありえず、**ファイルの中から読み出されています。**

> **SharePoint はラベルのメタデータをファイルから読めています。読めていないのは中身のほうだけです。**

`p-password` について「`MetaInfo` のラベルはキャッシュの残りかもしれない」と留保していた点は、
**この 1 本で消えました。**

**それでも、なぜ開けないのかは測っていません。** `vti_supportsZipIt: false` が
**昇格した側で `true`、しない側で `false`** に綺麗に割れていること、バイト数が 82432 / 71680
に対して基準が 19936 であることは**パスワードの容れ物が残っている**ことを示唆しますが、
**示唆であって測定ではありません。** この調査は**同じファイルのバイト構造の読みで一度外して
います** (`StrongEncryption*` の名前はパスワードを外した後も残る)。**分かっているのは
「ファイル側である」ところまでです。**

### レグ 2 について ― 交絡したままです

`o-checkedout.docx` は **`publication` にいまも `checkedOutBy: ミク` が入ったまま、
`decryptskipreason: 0` で昇格しています。**

**これを「チェックアウトは昇格を止めない」と読んではいけません。** チェックアウトしたのは
**アップロードの後**なので、**昇格が先に終わっていた可能性を排除できていません。**
**正しく測るにはライブラリをチェックアウト必須に設定する必要があります** ― **していません。**

### 断られ方の 14 個目

| | 素朴な読み |
| --- | --- |
| **条件を外したのに値が戻らない** | **外せていない / 反映が遅れている** |

**この 2 つは「時間を置く」だけでは切れません。** 切ったのは**回数と間隔**です ―
1 分では「まだ走っていない」が残り、27 分でも残り、**1 時間 22 分で 3 回同じ値**になって
初めて「遅れている」が薄くなります。**そして薄くなっただけで、消えはしませんでした** ―
残った A / B を切ったのは時間ではなく、**判定の履歴を持たないアイテムを 1 つ作ったこと**です。

### 確認していないこと

- **なぜファイルが開けないのか。** 「ファイル側である」ところまでで、**パスワードの容れ物が
  残っているかどうかは測っていません**
- **レグ 4 (12MB 超・別サイト)。** 2 つ目のサイトが要ります
- **レグ 2 を交絡なしで。** ライブラリをチェックアウト必須にしてから置く必要があります
- **`decryptskipreason` の値の意味。** `0` と `1` しか観測していません。**`1` が
  「パスワード」を指すのか「読めなかった」一般を指すのかは、この 6 本からは決まりません**
- **1 ラベル・1 ライブラリ・1 テナント**です

---

## 再現性

### `access` の 4 ファイル

**別々の日に、別々の実行環境から、同じ値が出ています。**

| | 実行環境 | A | B | C | D |
| --- | --- | --- | --- | --- | --- |
| 1 回目 | ファイルごとに個別実行 (GitHub ホストのランナー) | 4 / 0 | 5 / 0 | 5 / 0 | 3 / `404` |
| 2 回目 | **4 ファイルを 1 回の実行で** (GitHub ホストのランナー) | 4 / 0 | 5 / 0 | 5 / 0 | 3 / `404` |

さらに A については、**ローカルの開発機からの実行でも同じ 4 / 0** でした。

これは 1 つの留保を外します。GitHub ホストのランナーは送信元 IP もデバイスもローカルの開発機とは
違うので、そこで見えた拒否が「権限の所見」ではなく「条件付きアクセスがその条件で断った」だけである
可能性は常にあります (ワークフローのログはその旨を毎回表示します)。**この環境については、両方から
同じ値が出たことでその線が消えました** ― 環境側でも条件付きアクセス ポリシーが 0 件であることを
確認済みです ([environment.md](environment.md))。

一方、1 回の実行にまとめたことで **2 つの identity の答えの時間差が数百ミリ秒に縮まりました**。
所見 3 と 4 はどちらも「同じ対象を 2 つの identity で見比べる」ことでしか成り立たないので、その差が
「その間に何かが変わった」で説明できる余地は、実行を分けていたときより狭くなっています。

### `sharepoint`

こちらは 5 回実行しています。**シークレットで名乗った app-only の `401` と、委任の `200` は 5 回とも
同じ**でした。

| | 構成 | 変えたもの | シークレット | 証明書 | 委任 |
| --- | --- | --- | --- | --- | --- |
| 1 回目 | `Sites.Read.All` | ― (2 呼び出しのみ) | `401` | ― | `200` |
| 2 回目 | `Sites.Read.All` | 呼び出しを 5 本に増やした | `401` | ― | `200` |
| 3 回目 | **`Sites.FullControl.All`** | アプリのアクセス許可 | `401` | ― | `200` |
| 4 回目 | `Sites.Read.All` に戻した | **証明書を登録し、3 本目のレグを追加** | `401` | **`200`** | `200` |
| 5 回目 | 同上 (テナント側は無変更) | `appidacr` を記録するようにした | `401` | **`200`** | `200` |
| 6 回目 | **`Sites.FullControl.All`** | アプリのアクセス許可 (所見 5 の追試 4) | `401` | **`200`** | `200` |
| 7 回目 | `Sites.Read.All` に戻した | **全グループに訊くようにした** (所見 6 の追試 3) | `401` | `200` | `200` |
| 8 回目 | **`Sites.FullControl.All`** | アプリのアクセス許可 (所見 6 の追試 4) | `401` | `200` | `200` |

**証明書の `200` は 3 回**、シークレットの `401` と委任の `200` は 6 回。4〜6 回目は **35 分の間に
続けて回した実行**で、独立性の高い再現ではありませんが、1 回きりではなくなりました。
**5 回目は 15 本の呼び出しが 1 本残らず 4 回目と同じ**、**6 回目は 15 本中 1 本だけが違い**、
その 1 本が追試 4 の所見です。

**`Sites.FullControl.All` での測定は 3 回あり、最初の 1 回だけ証明書がありません** ― 3 回目
(所見 5 の追試 2)、6 回目 (同 追試 4)、8 回目 (所見 6 の追試 4)。同じ付与で app-only の答えが
`401` 4 本から `200` 中心に変わったことが、**変数が権限ではなかったこと**をこの並びだけでも
示しています。

**シークレットの `401` は 8 回、委任の `/_api/web` `200` は 8 回**。証明書の `/_api/web` `200` は
5 回になりました (4〜8 回目)。**ただし 8 回すべて GitHub ホストのランナーからで、同じ日です。**

**すべて GitHub ホストのランナーからで、ローカルからは実行していません。** `access` の A で確かめた
「ランナーとローカルで同じ値が出る」は、この測定については確かめていません。ただし所見 5 の app-only
`401` については、**同じ実行の Graph が `200` を返している**ので、実行環境そのものが原因である線は
消えています。追試 3 についてはさらに強く、**3 本すべてが同じジョブの同じ数秒間** に走っているので、
送信元 IP もデバイスも定数です。

---

## 23 の所見を貫くもの

いずれも同じ間違いが違う衣装を着たものです。**手前の層が言っていることを、奥の層の事実として
扱うこと。**

| 受け取った答え | 素朴な読み | 実際に何だったか |
| --- | --- | --- |
| Azure RMS のトークンが発行された | このアプリは RMS に届く | 権限を 1 つも持たないトークン |
| SharePoint の委任アクセス許可は一覧にない | 利用者の代理では SharePoint に届かない | `Sites.Read.All` を持つ SharePoint 宛の委任トークン |
| 権限一覧が `200 OK` / 0 件 | このファイルは共有されていない | 見せてもらえないので空になった一覧 |
| アイテムが `404 itemNotFound` | このファイルは存在しない | 存在するが、この呼び出し元には見えない |
| app-only が `Sites.Read.All` を保持 | SharePoint REST を叩ける | Graph では叩けるが、`/_api/` では `401` |
| `401` なら権限を足せば通る | 強い権限なら通る | フル コントロールでも同じ `401` |
| 2 つの app-only トークンが同じロール・同じ `aud` | 同じように扱われる | 片方は `401`、片方は `200` ― 効いたのは名乗り方 (`appidacr` だけが違う) |
| フル コントロールでも `401` だった | この API は権限を見ない | 見ます ― ただし資格情報が通ったあとだけ |
| グループを一覧できた | メンバーも見られる | 一覧は 6 つ、中身が返るのは 3 つ |
| メンバーが `403` なのは閲覧者だから | アプリの identity なら見える | アプリでも `403` ― ただし権限を上げると `200` |
| メンバーが `403` | 見せてもらえない中身がある | 中身は 0 件だった |
| 束取りの経路が 2 つ拒否された | 権限が足りない | `notSupported` ― 権限の話ですらない |
| 束取りが 2 回の呼び出しで答えた | この経路は 1 回で済む | **ページ数だけ呼ぶ** ― 測っていたのは 1 ページ分の費用 |
| 7 件が 1 ページで返った | このサービスは切らない | **切ります** ― 200 件を越えるまで見えなかっただけ |
| 共有先の一覧にその人の名前がある | その人はそのファイルを開ける | **`制限付きアクセス`** ― 隣のファイルへの付与がリストに撒いた名前 |
| リンクの裏のグループ名が `OrganizationEdit` | 名前を読めば対象範囲が分かる | **`Flexible` は `users`** ― 名前と `scope` は別の語彙 |
| リンクを作ったのにどこにも出てこない | 道具が見落とした | **何も作られていない** ― 権限を運ばないリンクは記録されない |
| 上げたファイルがそこにある | 置いたバイト列がそのまま保管されている | **昇格すると書き換わる** ― しかも最終更新は動かない |
| ラベルの列が空 | テナントの設定か、ラベルの作り | **置いた人がそのラベルの権利を持っていなかった** |
| 応答にキーが無い | その列は存在しない | **この文書については値が無い** ― 別のファイルには同じキーが返る |
| 権限を上げたら ACL の数が減った | 強い権限のほうが見えない | **スロットリング** ― 1 時間後に戻して測ったら元に戻った |
| 同じ経路が 2 つの identity に違う拒否を返した | 呼び出し元で扱いが違う | **先に走ったほうが枠を使った** ― 順番の話 |
| 束取りが 5 件、1 件ずつが 4 件 | 答えが食い違っている | 訊いた範囲が違う ― 片方は入れ子も歩く |
| 1 つのグループが `403` だった | グループの中身は見せてもらえない | 6 つ中 3 つは見える ― **測ったのが残りの 3 つ側だった** |
| RMS のトークンにロールが載った | このアプリは保護コンテンツを読める | 読めるのは**名指しした利用者が読めるもの**だけ |
| `Identity` と `DelegatedUserEmail` を両方設定した | 両方が認可に使われる | **使われるのは片方だけ** ― もう片方は空でなければ何でもよい |
| ポリシーの名指し一覧が空で返った | この形では一覧は載らない | **載っている** ― 返る条件がある |
| 所有者には一覧が返り、付与先には返らなかった | 見えるかどうかは呼び出し元で決まる | **保護の形も変数だった** ― `TemplateBased` では所有者にも返らない |
| 「権利の一覧を見る権利」を持っている | 権利の一覧が見える | **返りません。** 名前と、その名前が支配する面は別 |
| `RMS service is disabled for this tenant` | このテナントで RMS が無効 | **アドレスのドメインを打ち間違えていた** ― 別のテナントについての答え |
| 名指しの無い呼び出しが拒否された | 名指しが無ければ常に拒否される | **その付与の下では、が抜けていた** ― `Content.SuperUser` があると開く |
| スーパーユーザーの手順が 2 段ある | 2 つとも要る | **Entra の付与だけで完全に発現する** ― PowerShell 側は測れる範囲で何も足さない |
| Graph が `createdBy` を返した | このファイルの所有者 | **置いた人** ― 保護した人とは 3 枚とも別だった |
| 所有者は拒否の中身から読める | 試して断られる必要がある | **試す前に読めた** ― トークンもサービス呼び出しもなしで |
| グループを循環させようとした | 止められる | **止められない** ― 輪を閉じる操作が他の追加と区別されない |
| 推移メンバーに自分自身が入っていた | 壊れている | **輪を辿れば自分に戻る** ― 到達できる集合としては正しい |
| `displayName` が `null` で返った | このユーザーに名前が無い | **見せてもらえなかった** ― 同意を 1 つ足すと名前が出る |
| `protectionEnabled: false` が返った | このファイルは保護されていない | **SharePoint が読めていないだけ** ― 同じファイルの復号は測ってある |
| ラベル無しのファイルと同じ値だった | 同じ状態 | **片方は暗号化されている** ― この列では区別が付かない |
| `Prefer` を付けて `200 OK` が返った | ヘッダーが受理された | **黙って捨てられた** ― 応答が 1 バイトも変わらない |
| 権限を上げても応答が同じだった | この経路は権限を見ない | **その権限がこの経路の上に無かった** ― 使われないトークンを差し替えていた |
| `Preference-Applied` が返らない | サービスがそのヘッダーを知らない | **知っているものについても言わない** ― 自分で拒否した名前についてすら黙る |
| `410 Gone` | その資源はもう無い | **その続きからは答えられない** ― 同じ呼び出しが、しおり無しなら通る |
| 片方の API にしか無い行 | もう片方が隠している | **2 つの API が別のものをモデル化していた** ― 隠された付与ではない |
| 引き算が 0 でなくなった | 先の所見が崩れた | **並んだだけの行だった** ― `制限付きアクセス` は数に入り、何も与えない |
| 決め手を表に足した | 読者に届く | **見出しの一行は直っていなかった** ― 列幅で切られる側に置いていた |
| ラベルの権利付与先でない人が開けた | 付与が漏れている | **その人が発行者だった** ― ラベルの設定の外にある入口 |
| 同じ人が 2 つのファイルで開けたり拒否されたり | 権限が不安定 | **どちらの文書の発行者かで割れていた** |
| 拒否の本文を「そのまま」出していた | 読者は全文を読める | **列幅で切れていた** ― 方針は、運ぶ経路の全部に届いて初めて方針になる |
| 条件を外したのに値が戻らない | 外せていない / 反映が遅れている | **恒久的だった** ― しかも原因はアイテムの記憶ではなく**ファイルの側**だった |
| ラベルのキーが `MetaInfo` に並んでいる | ファイルから読めている | **貼り付いたキャッシュかもしれない** ― 履歴の無いアイテムに置いて初めて切れる |
| チェックアウト中のファイルが昇格していた | チェックアウトは昇格を止めない | **順番を測っていない** ― 上げた後にチェックアウトしたので、先に終わっていた線が残る |

層はこう並んでいます。

1. **アクセス許可の画面** ― 誰かが何を意図したか
2. **トークンのクレーム** ― Entra が実際に何を発行したか
3. **リソースの応答** ― そのリソースがそれをどう扱うか
4. **どの API を通ったか** ― 同じ権限・同じトークンでも答えが変わる
5. **どう名乗って取ったトークンか** ― クレームが同じでも答えが変わる
6. **渡した値のうち、どれが実際に読まれているか** ― 同じ呼び出しでも、効く値と効かない値がある
7. **誰が訊いたか** ― 同じ対象の同じ呼び出しでも、返ってくる情報の量が呼び出し元で変わる

**この 7 つはどれも別物です。** ここに記録した実行では、隣り合う層がすべて食い違いました。
5 番目は**上の 4 つを全部そろえても、まだ答えが割れる**ことを示し、6 番目は**トークンより先、
呼び出しの中身にも同じ隙間がある**ことを示しています ― `Identity` は設定を要求されながら、
認可には一度も使われていませんでした。7 番目は所見 3・4 が Graph で見せたもので、**所見 10 で
RMS の側にも同じ形が出ました** ― 空のリストは「無い」ではなく「見せてもらえない」。

そして所見 3・4・5・6 には共通の解き方があります。**同じ対象を、同じ瞬間に、複数の identity で
引くこと。** 片方だけを見ているかぎり、空も、`404` も、`401` も、事実に見えます。所見 5 の追試 3 は
**その 2 つを 3 つに増やしただけ**で、やり方は何も変わっていません ― そして、増やした 1 本が
所見 5 を解きました。

**もう 1 つ、同じ形の教訓があります。identity だけでなく、対象も 1 つに絞ると同じ穴が開きます。**
所見 6 は 6 つのグループのうち 1 つしか測っておらず、しかもその 1 つは誰も選んでいませんでした
(実装が先頭を取っていた)。**「一律に断られる」と「このグループだけ断られる」は、1 つしか訊いて
いないあいだ区別がつきません。** 全部に訊いた結果、所見の半分は測定の副産物でした。
**identity を並べるのと同じ理由で、対象も並べる必要があります。**

**断られ方は 6 種類あり、区別が要ります。**

| | 意味 |
| --- | --- |
| `401` | 認証されていない ― この経路でこの資格情報は受け付けられていない |
| `403` | 認証されている ― 操作が拒否された |
| `200` + 空 | 認証も認可も通った ― 中身が絞られた |
| **`200` + `null`** | **件数も型も ID も正しい ― フィールドが 1 つ伏せられた** (所見 13) |
| **`200` + `false`** | **型の上では正常な値 ― 内容は事実に反する** (所見 14) |
| **`429`** | **権限の話ではない ― 「いまは多すぎる」** (所見 8 の追試 2) |

**上の 4 つは静かさの順**で、下に行くほど誤読しやすくなります。4 つ目は所見 13 で出たもので、
**配列の件数すら変わりません** ― 応答の形だけを見ているかぎり、何も断られていないように見えます。
**5 つ目 (所見 14) はもう静かではありません。断言しています** ― `false` は「見せてもらえなかった」の
顔をしておらず、**型の上では立派な答え**です。

**5 つ目は別の軸です。** 上の 4 つが「**何を見せてもらえるか**」なら、`429` は「**いま見せて
もらえるか**」で、**同じ呼び出しが 1 分後には通ります。** そして厄介なことに、**まとめて流す
測定の中では静かに現れます** ― 所見 8 の追試 2 では、257 件のうち 34 件の ACL が
`with acl` 列にしか出ませんでした。**一覧の件数は 257 のまま**です。

所見 5 の追試 3 のあと、この 3 つは **1 つの identity の中に全部そろいます**。証明書で名乗った
app-only は、サイトを `200` で読み、あるグループの中身を `403` で断られます。同じアプリの
シークレット側は `401`。**「アプリが SharePoint に届くか」という問いには、少なくとも 3 つの
別々の答えがある**ということです。

そして追試 4 で、**この 3 つが順番に並んでいる**ことが見えました。`401` は資格情報の門、`403` は
その先の権限の門、`200` + 空はさらにその先の絞り込み。**手前の門で止まっているあいだ、奥の門の
状態は測れません** ― 追試 2 が「権限は変数ではない」に見えたのは、それが理由でした。

**さらに、同じ identity が同じ呼び出しで両方を受け取ります。** 証明書レグは `Sites.Read.All` の
まま、6 つのグループのうち 3 つに `200` を、3 つに `403` を返されます ― **1 本の呼び出しの
ステータスは、identity の性質でも API の性質でもなく、対象ごとの答え**でありうる、ということです。

---

## この調査が確立していないこと

所見は縁のほうが中身と同じくらい重要なので、1 箇所にまとめます。

- ~~**Azure RMS のトークンで実際に呼ぶとどうなるか。** RMS の API は一度も呼んでいません~~
  **呼びました** (所見 9)。ただし呼んだのは **MIP SDK 経由**で、**どの URL を叩いたのかは見えて
  いません** ― 分かるのは SDK が要求したトークンの `authority` と `resource` までです
- **なぜ `appidacr: 1` のトークンが `/_api/` で拒まれるのか。** 追試 3・3b で **効いている変数** は
  特定しましたが、**その理由は測っていません**。テナント設定なのか、`/_api/` の恒常的な仕様なのかも
  切り分けていません
- **証明書の `200` の独立した再現。** 2 回観測していますが、**12 分差の連続した実行**です。日を
  またいだ実行も、ローカルからの実行もありません
- **`appidacr` の値が一般に何を意味するか。** このプローブが測ったのは、**自分の 3 本のレグについて
  値が名乗り方に対応して動いたこと**だけです
- **`access` (Graph 経由) が証明書で変わるかどうか。** そちらは 2 本のままにしてあるので、
  **未測定です**。所見 3・4 は「シークレットで名乗った app-only」についての測定です
- **なぜ自動生成とみられるグループだけ扱いが違うのか。** 割れ方は測りましたが、理由は測って
  いません。**「自動生成」という区別自体、名前を見た人間の読み**であってツールの判定ではありません
- **メンバーが 2 人以上のグループでの挙動。** このサイトには 1 つもありません
- ~~ACL を 1 ページ単位でまとめて取れるかどうか。~~ **測りました** (所見 8)。取れるのは 1 経路
  だけで、**ページも追えます** (追試 1)。**サービスも自分で切ります** ― Graph の `/children` は
  200 件で (追試 2)
- **`429` が出る閾値** (所見 8 の追試 2)。出た実行と出なかった実行があるだけで、境界は未測定です
- **このツールが `429` に何もしていないこと** (同上)。`Retry-After` を見ておらず、
  直すかどうかは決めていません
- **`Content.DelegatedReader` を外したときに所見 9 の 6 セルがどう変わるか。** ロールを外した対照は
  取っていません
- ~~**`Content.SuperUser` を足すと何が変わるか。**~~ **測りました** (所見 11)。**未設定レグだけが開き、
  名指しレグは 1 つも変わりません**。ただし **B (PowerShell 側) が何のためにあるのかは未測定**です
- **`Custom` のテンプレート ID がファイル単位なのかラベル単位なのか** (所見 10)
- **`Custom` かつ `VIEWRIGHTSDATA` を持つ付与先に一覧が返るか** (所見 10 の訂正)。
  **この道具立てでは取れません** ― 理由は所見 10 に書いてあります
- **`VIEWRIGHTSDATA` が実際に何を支配しているのか。** 名前が指すものと、`ProtectionDescriptor` を
  埋める規則が同じかどうかは切り分けていません
- **保護した人とアップロードした人が同じファイルで `createdBy` が一致するか** (所見 12)。
  測った 3 枚はすべて操作者がアップロードしています
- **発行証明書が読めないファイルがあるか** (所見 12)。測った 3 枚は Office 形式で、3 枚とも
  読めました
- **3 つ以上の輪** (所見 13)。測ったのは 2 つ組だけです
- **輪が Graph のグループ API 以外でどう扱われるか** (所見 13) ― SharePoint の権限、ライセンス
  割り当て、条件付きアクセス
- **`null` で伏せられるフィールドの範囲** (所見 13)。測ったのは `displayName` × ユーザー 1 件です
- **同意を取り消すと `null` に戻るか** (所見 13)。**戻す測定をしていません**
- ~~**保護の権利が、所有者ではない利用者に対して効くかどうか。** 所見 9 で開いた 1 人は所有者
  でもありました~~ **測りました** (所見 9 の追試 1)。**効きます** ― ただし測ったのは 1 ラベルだけです
- 所見 1 の背後にある機構
- Graph の **アプリケーション** アクセス許可が、委任のものと同じように SharePoint へ流れ込むかどうか
- 閲覧者より強い権限を持つ委任呼び出し元が、権限一覧に何を見るか
- 権限一覧の読み取りが `403` を返す条件があるのかどうか
- サイト自体が見えない委任呼び出し元に何が返るか
- これらのいずれかが、別のテナント、別のクラウド、あるいは別の構成のアプリ登録でも成り立つかどうか

## 再現するには

```bash
dotnet run --project src/CapabilityProbe.Cli -- auth
dotnet run --project src/CapabilityProbe.Cli -- access
dotnet run --project src/CapabilityProbe.Cli -- sharepoint
dotnet run --project src/CapabilityProbe.Cli -- acl
```

所見 9 の 2 つは **Linux が要ります** (MIP SDK のネイティブが Linux のものしかありません)。
Docker か GitHub Actions のランナーで動きます。

```bash
dotnet run --project src/CapabilityProbe.Cli -- mip       # 設定もテナントも要らない
dotnet run --project src/CapabilityProbe.Cli -- consume \
  --ProtectedSiteFile=/probe-miku-only.docx \
  --DelegatedUserEmails="<ミク>|<リン>" --MipIdentityEmail=<どちらか>
```

設定とアプリ登録の準備は [README](../README.md) にあります。どのサブコマンドも `reports/` の下に
タイムスタンプつきの JSON レポートを書きます。そこには叩いた全 URL、送ったヘッダ、返ったステータス、
そして **実際にサインインした主体** が入っています ― 数か月後の実行結果が依然として価値を持つのは、
そのためです。

デバイス コードのプロンプトが出たら、意図した非管理者アカウントでサインインしてください。所見 3〜6 の
委任側は **閲覧者 1 人についての測定** なので、別のアカウントで入れば別のものを測ることになります
(何が出るかは確かめていません)。ツールは実際にサインインした主体を記録しますが、止めはしません。
